# 前后端协作约定：npm 主版本保持一致（>=11），避免 package-lock.json 因工具版本
# 差异反复重写（npm 10 会给 optional 平台包写冗余的 "dev": true，npm 11 不写）。
# 与 package.json 的 engines 字段配合：版本不符时 npm install 直接报 EBADENGINE 拦截。
engine-strict=true
