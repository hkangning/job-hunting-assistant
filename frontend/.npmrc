# 前后端协作约定：npm 版本须 ≥ 11.12.1，避免 package-lock.json 因工具版本
# 差异反复重写。实测依据（2026-09-24）：npm 11.6.1 会给 optional 平台包写冗余的
# "dev": true / "peer": true 标记，11.12.1 与 12.1.0 都不写、且产出一致。
# 与 package.json 的 engines 字段配合：版本不符时 npm install 直接报 EBADENGINE 拦截。
engine-strict=true
