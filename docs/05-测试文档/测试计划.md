# 个人求职助手 —— 测试计划

| 项 | 内容 |
|---|---|
| 文档版本 | v1.5 |
| 文档状态 | 定稿 |
| 维护人 | 胡康宁 |
| 最后更新 | 2026-09-24 |
| 上游依据 | 需求规格说明书 / 系统设计文档 / 接口文档 |
| 关联文档 | 项目结构 / 开发计划 |

## 变更记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| v1.0 | 2026-09-23 | 首版：测试策略 + mock 方案 + 48 条用例清单 + 验收测试脚本 | 胡康宁 |
| v1.1 | 2026-09-24 | AI 接入改"供应商+模型"两级：TC-48 更新、新增 TC-49（模型列表动态拉取），用例 48→49 | 胡康宁 |
| v1.2 | 2026-09-24 | 配合开发计划 v2.0（LLM 客户端独立成步）：新增 TC-50（llm_client 单元测试），用例 49→50 | 胡康宁 |
| v1.3 | 2026-09-24 | 第 4 节执行方式补齐虚拟环境激活步骤，命令改为 PowerShell 下可直接复制执行；头部"上游依据"字段去掉版本号 | 胡康宁 |
| v1.4 | 2026-09-24 | 新增 §2.4 数据库层冒烟与 TC-51（建表冒烟：15 表存在/字段与索引一致/枚举默认值/种子题库 ≥120 题）；§1.1 隔离方式表述放宽为"临时目录下独立 SQLite 文件"（模块级隔离早于 fixture 建立，用 `tempfile`） | 卢世君 |
| v1.5 | 2026-09-24 | TC-02 补「流转至 `CLOSED` 须带结束原因、缺失拒绝」与「原因仅在 `CLOSED` 态有值」两项验收点（对应 SRS v1.4 / 接口文档 v1.2 的 `close_reason`）；TC-26 补 `close_reason` 字段口径。**本次仅改用例描述口径，用例编号与数量不变** | 卢世君 |

---

## 1. 测试策略

### 1.1 总原则

- **LLM 调用 100% mock**：测试离线可跑、不产生 API 调用费用、结果确定（SRS NFR-012）。真实供应商联调用**一次手动冒烟**验证（见 5.1），不入自动化；
- 测试金字塔：服务层单元测试 > API 集成测试 > 前端手工验收；SSE 流式链路有专项集成测试；
- 测试数据库用**临时目录下的独立 SQLite 文件**，不碰开发库。具体方式：`conftest.py` 模块级以 `tempfile.mkdtemp()` 建临时目录（必须在任何 `app.*` 导入之前设置 `DATABASE_URL`，此时 fixture 尚不存在）；fixture 作用域内可按需改用 `tmp_path`。

### 1.2 工具与框架

| 项 | 选型 |
|---|---|
| 测试框架 | pytest + pytest-asyncio |
| API 测试 | FastAPI TestClient（依赖注入替换 LLM） |
| Mock | `FakeLLMProvider`（见下）；测试期不启用定时调度器 |
| 覆盖率 | pytest-cov，目标：services 层 ≥ 80% |

### 1.3 LLM Mock 方案

`app/clients/llm_provider.py` 定义抽象 `LLMProvider`，生产实现走 DeepSeek。测试 `conftest.py` 注入 `FakeLLMProvider`：

- `stream_chat()` 返回**预设 token 序列**（按场景预置：JD 五段报告、面试点评+下一题、八股点评等），可断言落库内容；
- `chat_json()` 返回预设 dict（面经条目/错题判定）；
- `list_models()` 返回预设模型列表（或 `fail=True` 模拟拉取失败，验证回退内置表路径，见 TC-49）；
- 可配置 `fail=True` 模拟 LLM 异常，验证兜底路径（错误码 10010/10011/40002）；
- 可配置 `delay=0.001` 模拟流式节奏，验证 SSE 事件顺序。

**注入方式**：`app.main` 的依赖容器（`get_llm_provider`），TestClient 覆盖依赖即可，生产代码零改动。

## 2. 测试用例清单

> 编号 TC-XX。优先级 A=必须通过（对应 SRS 验收标准 AC）；B=重要；C=边缘。

### 2.1 服务层单元测试

| 编号 | 模块 | 用例 | 优先级 |
|---|---|---|---|
| TC-01 | application_service | 新增投递：字段合法入库，默认状态 APPLIED | A |
| TC-02 | application_service | 状态流转：合法流转（APPLIED→WRITTEN→INTERVIEW→OFFER）成功；非法流转拒绝；**流转至 CLOSED 须带结束原因（`FAILED`/`DECLINED`/`EXPIRED`），缺失拒绝；原因仅在 CLOSED 态有值，转其他状态时清空** | A |
| TC-03 | application_service | 批量导入：解析 xlsx 合法行入库；非法行返回行级错误不中断 | A |
| TC-04 | application_service | 批量导入：缺表头/缺必填列 → 20001 | B |
| TC-05 | interview_service | 创建会话：带 application_id 自动带入公司岗位；手填路径正常 | A |
| TC-06 | interview_service | 作答落库：FakeLLM 点评流 → interview_qa 落库（question/answer/score/review 完整） | A |
| TC-07 | interview_service | 跳过：skipped=1、score 为空、序号推进 | B |
| TC-08 | interview_service | 总结：会话置 FINISHED、summary 落库；重复调用不重复生成 | A |
| TC-09 | practice_service | 抽题：方向过滤正确、不含 answer 字段、count 上限 | A |
| TC-10 | practice_service | 点评：评分 <6 自动入错题本（review_stage=1、next_review_at=+1 天）；≥6 不入 | A |
| TC-44 | practice_service | 点评入本边界：该题已在错题本 → 更新档位与 wrong_count，不重复插入（UNIQUE 约束） | A |
| TC-11 | wrong_question_service | 档位推进：答对 1→2→3→4→mastered_at 置位；答错回 1、wrong_count+1 | A |
| TC-12 | wrong_question_service | 到期判定：next_review_at ≤ now 计入到期集合 | A |
| TC-13 | wrong_question_service | 客观题规则判定：标准答案比对，不调 LLM | B |
| TC-14 | experience_service | 保存原文 + FakeLLM 提取条目落库；提取失败原文保留 | A |
| TC-15 | experience_service | 检索：keyword LIKE 命中 question/公司联查 | B |
| TC-45 | wrong_question_service | 面试知识点入本：question_id 形态与 {content,answer,direction} 形态均成功（后者自动建题） | A |
| TC-16 | agent_service | 意图路由：四类意图样例各 3 条路由正确 | A |
| TC-17 | agent_service | 工具调用：FakeLLM 返回 tool_call → 确认卡片数据正确（写操作不直接落库） | A |
| TC-18 | agent_service | 工具执行：create_application/update_application_status 入库成功 | A |
| TC-19 | agent_service | LLM 失败：抛出业务异常映射 10010，历史消息不丢 | B |
| TC-20 | jd_service | 报告落库：五段文本 + score 提取（"综合匹配度：85 分"格式） | A |
| TC-21 | reminder_engine | 纯规则判定：3 天无进展/错题到期/7 日面试，各构造边界数据断言 | A |
| TC-22 | reminder_engine | 文案：LLM 失败回退模板文案；任务互斥不重入 | B |
| TC-23 | reminder_engine | 就业网抓取失败：静默降级，保留上次数据 | B |
| TC-24 | profile_service | 画像读写；skills 逗号分隔序列化正确 | B |
| TC-50 | llm_client | 单元测试：stream_chat 逐块输出 / chat_json 解析 / test_connection / list_models（缓存 24h 内不重拉、失败回退内置表）/ 超时重试 / 异常映射 LLMError | A |

### 2.2 API 集成测试（TestClient）

| 编号 | 用例 | 优先级 |
|---|---|---|
| TC-25 | 统一响应体：成功/校验失败(10001)/资源不存在(10002)/系统异常(10000) 四态 | A |
| TC-26 | 投递 CRUD 全流程：创建→查询→编辑→删除；分页 total 正确；DTO 含 `close_reason` 字段（仅 CLOSED 态有值，其余为 null） | A |
| TC-27 | GET /overview 聚合：统计数量、跟进列表（构造 >3 天数据）、置顶排序 | A |
| TC-46 | GET /applications/trend：按日 GROUP BY 计数正确、含 0 值日期、days 边界 | B |
| TC-28 | GET /settings 不回显敏感值（仅 *_key_set 布尔） | A |
| TC-47 | POST /settings/llm-test：FakeLLM 连通成功返回 ok；失败返回 10013 与原因；测试不落库 | A |
| TC-48 | 供应商+模型两级切换：llm_client 按 config 表 llm_provider/llm_model 选择 base_url/model；切回旧供应商配置仍在 | A |
| TC-49 | 模型列表动态拉取：FakeLLM GET /models 返回列表并写缓存；拉取失败回退内置表（标注"内置列表"）；缓存 24h 内不重拉 | A |
| TC-29 | PUT /profile 全字段更新回读一致 | B |
| TC-30 | 提醒：POST /reminders/run 生成记录幂等（同日不重复）；PUT check 标记 | B |
| TC-31 | 语音转写：FakeASR 返回文本（P2 开发后启用） | C |

### 2.3 SSE 流式专项测试

| 编号 | 用例 | 优先级 |
|---|---|---|
| TC-32 | /stream/jd-analysis：事件顺序 start → delta×N → done；start 在 300ms 内推送；done 携带 record_id；落库文本与流拼接一致 | A |
| TC-33 | /stream/interview-chat：首题（空 answer）→ 点评+下一题；满题量 done 带 session_finished | A |
| TC-34 | /stream/practice-review：done 带 wrong_question_id（低分时） | A |
| TC-35 | /stream/agent-chat：start → delta/tool_call 事件顺序；写操作出确认卡片 | A |
| TC-36 | 流中 LLM 异常：发 error 事件（code 10010），已收 delta 内容保留 | A |
| TC-37 | 客户端断连：模拟中途断开，后端终止不抛未捕获异常，半成品落库 | B |

### 2.4 数据库层冒烟

| 编号 | 用例 | 优先级 |
|---|---|---|
| TC-51 | 建表冒烟（对应开发计划步骤 2）：应用启动后 15 张表存在；字段（名/类型/可空/主键）、显式索引、唯一约束、外键与数据库设计文档 §3 一致；SQLite WAL 模式与外键开关开启且**约束真生效**（非法外键/重复唯一值被拒）；初始行正确（`user_profile` id=1、`config` 默认项）；枚举与整型默认值正确（**插入读回**验证，非查 DDL——SQLAlchemy `default=` 不产生 DDL `DEFAULT` 子句）；种子题库 ≥120 题且 Java/MySQL/网络/OS 各 ≥30；`init_db()` 幂等 | A |

> 实现见 `backend/tests/test_database_init.py`（15 条断言用例）+ `backend/tests/expected_schema.py`（文档固化的期望结构）。设计依据见 `docs/superpowers/specs/2026-09-24-步骤2建表冒烟测试-design.md`。

## 3. 前端手工验收要点（无自动化，按清单执行）

| 编号 | 检查点 | 对应验收 |
|---|---|---|
| TC-38 | 打字机渲染：SSE 逐 token 上屏，无"等完整回复"白屏 | NFR-001 |
| TC-39 | Agent 乐观更新：一句话录入 → 先出卡片 → 确认入库 → 失败红色重试 | FR-011 |
| TC-40 | LLM 断网兜底：错误提示 + 重试；投递管理不受影响 | AC-3 |
| TC-41 | 语音：录音 → 转写回填可编辑 → 提交；TTS 开关默认关 | AC-6 |
| TC-42 | 重启持久化：全部数据在重启后完整 | AC-7 |
| TC-43 | 浏览器兼容：Chrome/Edge 最新版全页面无报错 | NFR-011 |

## 4. 执行方式

```powershell
cd backend
.\.venv\Scripts\Activate.ps1    # PowerShell 先激活虚拟环境（未激活则把 pytest 换成 .\.venv\Scripts\python.exe -m pytest）
pytest                          # 全量（离线、无外网依赖）
pytest --cov=app/services       # 覆盖率报告
pytest tests/test_sse_streams.py -v   # 流式专项
```

前端启动后按第 3 节清单手工过一遍（开发步骤 7 验收时执行）。

## 5. 联调与冒烟（一次性，手动）

### 5.1 真实供应商冒烟（默认 DeepSeek，配置 .env 或设置页后）

1. `python -c` 调用 llm_client.stream_chat 发一条消息，确认流式返回；
2. 设置页连通性测试通过（接口 /settings/llm-test）；
3. 页面走一遍 JD 分析全流程，看首字延迟（目标 <2s，NFR-001）。

### 5.2 真实讯飞冒烟（P2 开发后）

录音 5s → /asr/transcribe → 转写文本正确（<1.5s，NFR-002）。

## 6. 通过标准（对应 SRS 验收标准）

- 全部 A 级用例通过（B/C 级完成度 ≥ 90%）；`pytest` 全绿；
- services 覆盖率 ≥ 80%；
- 手工验收清单全通过；
- 满足 AC-1 全流程演示无断点。
