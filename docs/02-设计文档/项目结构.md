# 个人求职助手 —— 项目结构文档

| 项 | 内容 |
|---|---|
| 文档版本 | v1.41 |
| 文档状态 | 定稿 |
| 维护人 | 胡康宁 |
| 最后更新 | 2026-09-26 |
| 上游依据 | 系统设计文档 |
| 关联文档 | 接口文档 / 数据库设计文档 |

## 变更记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| v1.0 | 2026-09-23 | 首版：前后端目录树与文件职责 | 胡康宁 |
| v1.1 | 2026-09-24 | ①第 1 节改写为"仓库与分支形态"（分支隔离协作、公共文件同步规则、各分支独有配置文件清单）；②工程骨架落地对齐：目录树补 pytest.ini、依赖清单补 pytest-cov | 胡康宁 |
| v1.2 | 2026-09-24 | 公共文件改为以 `docs` 分支为唯一权威源：新增 docs 分支行与四步变更流程，公共文件收敛为 docs/、README.md、.gitignore 三样 | 胡康宁 |
| v1.3 | 2026-09-24 | **撤销 v1.2 的 docs 分支权威源方案**，改回"两分支各持公共文件副本、相互拉取同步、最后合入 main"；目录形态确定为仓库根下 `docs/`、`backend/`、`frontend/` 三个平级子目录（文档与代码同仓同目录） | 胡康宁 |
| v1.4 | 2026-09-24 | 后端启动命令改写为 Windows PowerShell 可直接复制的形式：补虚拟环境激活步骤，附"禁止运行脚本""python 不在 PATH"两类常见报错的处理；头部"上游依据"字段去掉版本号 | 胡康宁 |
| v1.5 | 2026-09-24 | ①前端目录树对齐实际：新增 `src/styles/tokens.css`（设计 token 单一出处）、`components/SideNav.vue`、`components/AppHeader.vue` 三项登记（卢世君）；②第 1 节新增「跨职责文件区」约定：`backend/tests/` 目录归后端、内容归测试侧，含改动前置动作与冲突处理口径（胡康宁）；③第 3 节「前端启动」补环境要求 npm ≥ 11、Node `^20.19.0 \|\| >=22.12.0` 并修正 `&&` 写法（PowerShell 5.1 不支持）（胡康宁） | 卢世君 / 胡康宁 |
| v1.6 | 2026-09-24 | 第 5 节前端依赖清单补登 `@element-plus/icons-vue`（`src/components/AppHeader.vue` 实际在用 Fold / Expand / Bell 图标，原清单漏记） | 卢世君 |
| v1.7 | 2026-09-24 | 第 3 节「前端启动」npm 版本要求收紧：`≥ 11` → `≥ 11.12.1`（逐版本实测：11.6.1 会写冗余 `dev`/`peer` 标记、11.12.1 与 12.1.0 不写且产出一致；原门槛拦不住 11.6.1，锁文件仍会互改） | 胡康宁 |
| v1.8 | 2026-09-24 | 第 3 节「前端启动」补 npm 12.x 的 Node 前提：`npm install -g npm@latest`（12.1.0）要求 Node `^22.22.2 \|\| ^24.15.0 \|\| >=26.0.0`，Node 低于此报 `EBADENGINE`（实测 Node 24.11.0 被拒），须先升 Node 或改装 `npm@11.12.1` | 卢世君 |
| v1.9 | 2026-09-24 | ①§5 依赖清单补登 `vuedraggable`、`echarts`（步骤 4 引入，原清单缺 2 项）；②§3 目录树登记 `constants/application.js` 与 3 个投递组件（`ApplicationFormDialog` / `ApplicationImportDialog` / `TrendChart`）；③**顺带修正头部版本号滞后**：v1.8 条目加入时漏改头部（头部停在 v1.7、变更记录已到 v1.8），本次一并校正 | 卢世君 |
| v1.10 | 2026-09-24 | 第 1 节「跨职责文件区」增补**问题记录**约定：`docs/07-工作日志/问题记录.md` 内容由测试负责人（卢世君）统一维护，后端侧不主动修改 | 胡康宁 |
| v1.11 | 2026-09-24 | 第 1 节新增「**职责边界**」：明确后端侧（`backend/` 代码、数据库、除前端 / 测试外的文档章节）与前端测试侧的负责范围，后端侧不主动改动 `frontend/` 下文件、测试与前端专属文档内容 | 胡康宁 |
| v1.12 | 2026-09-24 | §3 目录树登记 `components/CloseReasonDialog.vue`（「标记为已结束」时的结束原因选择，对应 SRS v1.4 / 数据库设计 v1.2 的 `close_reason`） | 卢世君 |
| v1.13 | 2026-09-24 | §3 目录树登记 `constants/regions.js`（全国省市选项，34 省级 / 747 城市，供投递城市**级联选择**——筛选与表单共用同一份）。数据自 `element-china-area-data@6.1.0` **构建期提取一次**并内嵌，**运行时不含该依赖**；第二级含县级市（昆山、常熟等），直辖市不设二级 | 卢世君 |
| v1.14 | 2026-09-25 | §3 目录树更新 `constants/regions.js` 说明：第二级由「地级市 + 县级市」**收敛为只到地级市**，数据源改用同包的 `pcaTextArr`（省-市-区三级）**第二级**并排除「省直辖县级行政区划」占位项。可选中城市 747 → **346**（二级选项 741 → 340，一级直达 6 项不变） | 卢世君 |
| v1.15 | 2026-09-25 | **多账号体系目录登记**（对应系统设计 v1.5 / 接口文档 v1.3）：①§2 后端目录树——`models/` 表数 15 → **17** 并新增 `user.py`（User / LlmProviderConfig）；新增 `deps.py`（鉴权依赖 `get_current_user`）、`utils/security.py`（JWT/bcrypt/Fernet/头像压缩）；`schemas/` 增 `auth.py`、`llm_provider.py`；`routers/` 增 `auth.py`、`llm_providers.py`（`settings.py` 注释去掉 LLM 连通性测试）；`services/` 增 `auth_service.py`、`llm_provider_service.py`；`clients/llm_provider.py` 注册表 5 → **12** 家；`tests/` 补登 `test_auth.py`、`test_llm_providers.py`（文件名登记，用例内容归测试侧）；②§2 后端启动段补 `.env` 的 `APP_SECRET_KEY` 说明；③§5 依赖清单补 `PyJWT` / `bcrypt` / `cryptography` / `Pillow`（头像压缩）；④**前端目录树（§3）本轮未同步**——新增页面（Welcome/Login/Register/Profile/AiConfig）、组件（UserMenu）、store（user.js）与路由守卫登记由前端侧更新 | 胡康宁 |

| v1.16 | 2026-09-25 | **语音方案目录登记**（对应系统设计 v1.6 / 接口文档 v1.4）：①§2 后端目录树——`routers/asr.py` 更名为 **`routers/voice.py`**（职责扩为转写 / 合成 / 音色列表）；`clients/` 的 `asr_client.py` 注释改为「ASR 抽象 + **FunASR 本地流式（默认）** + 讯飞云端（备选）」，新增 **`clients/tts_client.py`**（edge-tts 合成 + sha1 缓存 + 音色表）；`tests/conftest.py` fixture 注释补 **FakeTTS**；②§5 依赖清单新增 **`funasr`**（依赖 torch，体积数 GB，**步骤 22 才装**）与 **`edge-tts`**，`websockets` 注释明确为「讯飞实时转写（备选）」，`httpx` 注释去掉讯飞（其职责只剩就业网抓取）；③配套：目录树两行注释（`uploads/avatars/`、`cache/tts/`）与实际 `.gitignore` 规则对齐——`backend/uploads/`、`backend/cache/` 已入仓库 `.gitignore`（经讨论确定） | 胡康宁 |
| v1.17 | 2026-09-25 | **前端目录树与依赖清单同步**（补齐 v1.15 遗留的「前端侧更新」与系统设计 v1.7 点名的 `driver.js` 登记）：①§3 目录树——`views/` 补 5 个新页面（Welcome / Login / Register / Profile / AiConfig），`Settings.vue` 说明改写（画像移入个人中心、LLM 字段迁出）；`components/` 补 `UserMenu.vue`、`GuideTour.vue`，`AppHeader.vue` 补「右侧用户菜单」、`VoiceInput.vue` 说明随语音方案改为「连续录音 + 前端 VAD 静音断句 + 逐句转写回填」；`stores/` 补 `user.js`；`api/` 补 `auth.js`、`llmProviders.js`、`voice.js`，并更新 `request.js`（注入 Authorization + 全局登出）与 `system.js`（去掉语音）职责；`router/index.js` 补路由守卫；`App.vue` 挂载位补 `<GuideTour/>`；②§5 依赖清单补 **`driver.js`**（新手指引高亮，约 5KB 零依赖，**P2、步骤 24 才装**），并补注「**语音前端不引依赖**」——VAD 用 Web Audio API `AnalyserNode`、录音用原生 `MediaRecorder`、播报用原生 `<audio>` | 卢世君 |
| v1.18 | 2026-09-25 | **§3 前端目录树按步骤 7 实际落地校正**（v1.17 登记的是规划名，落地后有出入，逐项对齐实际代码）：①新增 **`mock/` 目录块**（dev-only 接口 mock：Vite 插件中间件，5 个文件，不进生产构建）；②`api/` 把原 `system.js`（概览/画像/设置/提醒）**拆为 `profile.js` + `overview.js`**——单资源单文件，与既有 `applications.js` 风格一致；**提醒接口的落点待步骤 20 再定**；③`components/` 补 `AuthShell.vue`（登录/注册共用外壳）；④新增 **`utils/` 目录**与 `avatar.js`；⑤`vite.config.js` 说明补「mock 开启时不注册 `/api` 代理，二者互斥」；⑥`SideNav.vue` 说明「3 组菜单」→「**2 组菜单**」（设置迁入用户菜单后「系统」组移除）；⑦`App.vue` 说明补「`meta.layout='blank'` 的账号页走全屏壳」 | 卢世君 |
| v1.19 | 2026-09-25 | **步骤 5 落地后的口径自查**：§2 后端目录树 `deps.py` 说明由「JWT 校验 → 注入当前 **user_id**」改为「JWT 校验 → 注入当前账号 **User 对象**」——实现中 `get_current_user` 注入的是完整 user 实体（`/auth/me`、`llm_configured` 等直接读该对象），服务层再从对象取 `user_id`，原表述与实现不符 | 胡康宁 |
| v1.20 | 2026-09-25 | **AI 面试教练口径同步**（对应系统设计 v1.11 / 数据库设计 v1.7 / 接口文档 v1.7 / 开发计划 v2.8）：①§2 后端目录树登记练习模式与表达力分析的新增文件——`models/drill.py`（DrillTopic / DrillAttempt）、`schemas/drill.py`、`routers/drill.py`、`services/drill_service.py`、`utils/speech_metrics.py`（表达力指标纯函数模块，不触 LLM），`models/` 表数 17 → **19**；②**练习模式的技术标识统一用 `drill`**——项目内 `practice` 已专指八股陪练（FR-009），两者不可混用；③**前端目录树（§3）本轮未同步**——`/drill`、`/drill/:topicId` 两个新页面与 `api/drill.js` 的登记由前端侧更新 | 胡康宁 |
| v1.21 | 2026-09-25 | **§3 前端目录树补齐练习模式登记 + mock 静态路径口径修正**（对应后端「待前端处理事项」台账 #5 / #12）：①`views/` 补 `DrillList.vue`（我的题目列表）/ `DrillDetail.vue`（历次记录时间线与跨次进步对比），`api/` 补 `drill.js`；②`mock/index.js` 说明行的静态路径由 `/static/avatars/*` 改为 **`/uploads/avatars/*`**，与后端挂载点一致（IS-22 口径定案 = `/uploads`） | 卢世君 |
| v1.22 | 2026-09-26 | **§2 后端目录树按步骤 6 实际落地校正**（对应系统设计 v1.13）：①`services/` 补 **`settings_service.py`**（设置读写：账号级偏好 + 系统级抓取配置，两级 config 表）；②`clients/llm_client.py` 说明去掉 `test_connection`，改为「`stream_chat` / `chat_json` / 超时 / 重试 / 注入点（**步骤 10**）」——探测类职责按「是否产生对话内容」拆分；③`clients/llm_provider.py` 说明补 **`test_connection` 连通性探测（步骤 6 落地）**，并去掉「24h 缓存」表述——缓存读写实际在服务层（`llm_provider_service._load_cache` / `_store_cache`），模块只负责一次探测；④同条去掉已不存在的 `FakeLLMProvider` 登记（测试替身由测试侧在 conftest 内实现，不在该模块） | 胡康宁 |
| v1.23 | 2026-09-26 | **职责边界显式纳入测试计划文档**：①第 1 节职责表「卢世君（前端 + 测试）」行的负责范围补「（含**测试计划文档**）」；②后端侧不主动改动清单第 2 条的「测试计划」改为「**测试计划文档**」；③第 1 节「跨职责文件区」新增**测试计划文档**段——它与问题记录同型（文档位于公共文件区 `docs/` 下、内容归测试负责人维护），补齐只读与知会口径。原表述只写「文档中前端 / 测试相关的章节」，测试计划作为独立文档未被点名，易被误读为无人归属 | 胡康宁 |
| v1.24 | 2026-09-26 | **测试计划维护节奏显式化**：§1「跨职责文件区」的测试计划文档段补一条——该文件由测试侧**随实现与用例同步直接修订**（自 2026-09-26 起不再走事前确认流程，目的是减少合并冲突），改后留记录即可；**其余交付文档不在此列**（需求规格说明书 / 系统设计 / 数据库设计 / 接口文档 / 开发计划 / 项目结构 / 根 `README.md` / `docs/README.md` 仍由后端维护，测试侧确需改动时先知会）。后端侧对该文件「不主动修改、引用只读」的约束不变 | 胡康宁 |
| v1.25 | 2026-09-26 | **§3 前端目录树补登 3 个已落地文件**（对应问题记录 IS-26）：①`api/` 补 **`settings.js`**（设置接口 GET/PUT，设置页出口）；②`components/` 补 **`ProviderCard.vue`**（AI 配置页的供应商卡片：折叠头 + 展开编辑区 + 就地探测结果，步骤 8 新增）；③`components/` 补 **`ApplicationStats.vue`**（投递统计条：总数/本周新增 + 状态占比堆叠条，受控组件不调接口，步骤 3 产物）——③为核查时一并发现的同类漏登。**§5 依赖清单无需联动**：三者均为自研文件、不引入新依赖 | 胡康宁 |
| v1.26 | 2026-09-26 | **§1「跨职责文件区」新增前端待做事项段**：`docs/07-工作日志/前端待做事项.md` 自[后端工作日志](../07-工作日志/后端工作日志.md) 末表独立成文（#1~#26 原样迁入）——**条目由后端侧登记、完成状态由前端 / 测试侧回标**，与问题记录、测试计划的「内容归测试侧」相反，三者互不替代。后端工作日志末表迁出后只留指引、「维护约定」补第 6 条；`docs/README.md` 文档清单与目录性质说明联动 | 胡康宁 |
| v1.27 | 2026-09-26 | **§2 后端目录树与依赖清单按「校招情报」模块登记**（对应 SRS v1.11 FR-021 / FR-022、系统设计 v1.14 §5.9、数据库设计 v1.8、接口文档 v1.12）：①`clients/` 新增 **`crawler/`** 包（`base.py` 适配器协议 + `adapters/` 一厂商一解析器 + `processor.py` 处理层纯函数 + `compliance.py` 合规组件）；②`services/` 的 **`crawler.py` 行删除**（原单站点抓取升级为上述包），新增 **`campus_service.py`**（采集调度编排 / 投喂抽取 / 匹配打分 / 订阅匹配与推送 / 日历聚合）；③`models/` 新增 **`campus.py`**（`CrawlSource` / `JobPosting` / `Subscription`，表数 19 → **22**）、`schemas/` 新增 **`campus.py`**、`routers/` 新增 **`campus.py`**（`/campus-events`、`/job-postings`、`/calendar`）/ **`crawl_sources.py`** / **`subscriptions.py`**——`reminders.py` 说明去掉「宣讲会」（该接口迁出）；④§5 依赖清单补 **`lxml`**（SSR 页面解析，**步骤 21 才装**）、`httpx` 注释改「校招信息采集（多源）」、`funasr` 与 `driver.js` 的「步骤 XX 才装」口径随步骤顺延同步（22 → **24**、24 → **28**）；⑤`cache/tts/` 注释步骤号同步（22 → 24）。**§3 前端目录树归前端侧**（校招情报相关页面由卢世君登记），本文件未动 | 胡康宁 |
| v1.28 | 2026-09-26 | **§1「跨职责文件区」问题记录段补「谁处理谁标注」（2026-09-26 起）**：条目处理完毕后由**实际处理方**直接更新「处理」「结论」「状态」三栏 + 同步文末「状态统计」——后端侧闭环的条目由后端侧自行标注 ✅ 已解决并写明依据，**不再等测试侧结案**；编号分配、表结构与新条目登记仍由测试负责人主导（后端侧对新增条目只读）。同步落地：[问题记录](../07-工作日志/问题记录.md) 维护约定补第 8 条并按此标注 IS-25 / IS-26、[前端待做事项](../07-工作日志/前端待做事项.md) 维护约定补第 6 条（问题记录条目不再登记为该表事项）、`docs/README.md` 清单说明列 | 胡康宁 |
| v1.29 | 2026-09-26 | **§5 依赖清单的 VAD 口径同步为「按语速自适应」**（对应系统设计 v1.14 §5.6）：原文「连续静音约 1.2s 切句」是多账号改造前的旧口径，改为「**按语速自适应**：语速快 1.6s / 正常 2.65s / 慢 3.6s 切句」并补指向系统设计 §5.6。该行描述前端实现、属前端章节（[前端待做事项](../07-工作日志/前端待做事项.md) #30 转出，后端侧未动）；`docs/README.md` 版本清单联动 | 卢世君 |
| v1.30 | 2026-09-26 | **数据库切换 MySQL + 题库体系升级**（对应数据库设计 v1.9 / 接口文档 v1.14 / 开发计划 v2.14）：①§2 目录树 `database.py` 注释由「WAL 与外键 PRAGMA」改为「连接池参数（`pool_pre_ping` / `pool_recycle`）」、`seed/questions.json` 注释由「4 方向各 ≥30 题」改为「**585 题 / 5 技术栈 / 18 领域 / 含 152 场景题**，首次启动 **upsert** 导入」；②§5 依赖清单补 **`pymysql`**（MySQL 驱动）一行。**§3 前端目录树归前端侧**（题库筛选面板相关改动由卢世君登记），本文件未动 | 胡康宁 |
| v1.31 | 2026-09-26 | **§2 后端目录树补登步骤 9 落地文件**：`schemas/overview.py`（今日概览聚合响应 DTO——含状态计数与个人中心计数）与 `services/overview_service.py`（概览聚合：面试提醒 / 状态计数 / 跟进提醒 / 错题与面试计数）。`routers/overview.py` 此前已登记，本次补齐同层配套两文件；概览接口的判定口径同步补入接口文档 v1.14 §3.4 | 胡康宁 |
| v1.32 | 2026-09-26 | **§2 目录树 `llm_client.py` 按步骤 10 落地校正**（对应系统设计 v1.16 §5.4）：注释由「LLM 封装：stream_chat/chat_json/超时/重试/注入点（步骤 10）」补全为「LLM 对话封装：配置解析 / stream_chat / chat_json / 超时重试 / 异常映射 / `get_llm_client` 注入点（步骤 10 落地）」——注入点落名为 **`get_llm_client`**（不叫 `get_llm_provider`，避免与探测模块 `llm_provider.py` 混淆），探测类仍归 `llm_provider.py`。**测试计划 §1.3 的对应命名由测试侧对齐**（已登记[前端待做事项](../07-工作日志/前端待做事项.md)） | 胡康宁 |
| v1.33 | 2026-09-26 | **§2 后端目录树登记 SSE 协议层落地文件**（对应步骤 11）：①`utils/` 新增 **`sse.py`**（事件构造器 `SSE` + 响应包装器 `sse_response` + 独立会话 / 断连终止 / 异常分派）；②`schemas/` 新增 **`stream.py`**；③`routers/stream.py` 注释补自检端点（由「全部 SSE 端点」改为「demo 自检 / jd-analysis / interview-chat / …」——本步骤交付 `POST /stream/demo` 作协议靶子，业务链路后续步骤逐个落入本文件）。**§3 前端目录树归前端侧**（`sse.js` 封装与 `StreamText` 组件由卢世君登记），本文件未动 | 胡康宁 |
| v1.34 | 2026-09-26 | **§3 前端目录树随步骤 11 落地登记与订正**（承接 v1.33 的「前端侧登记」）：①**新增** `utils/sse.js`（SSE 流式客户端：fetch + ReadableStream 解析、事件回调、中止）与 `views/DevStream.vue`（流式协议调试页，靶子 `POST /stream/demo`，不进导航）；②**订正 `sse.js` 的归属**——原登记在 `api/` 段，但它是 `fetch` 封装而非 axios 接口，与 `request.js` 并列易被读成同类，实际落在 `utils/`；③**订正 `StreamText.vue` 的注释**——原写「按 section 分段」，实现为**纯展示**组件（rAF 平滑追赶传入文本），section 分段由调用方处理；④**补登 `utils/datetime.js`**（步骤 9 真联调时抽出，前次登记遗漏）。**§2 后端目录树本次未动** | 卢世君 |
| v1.35 | 2026-09-26 | **§2 后端目录树按步骤 12 落地校正**（对应系统设计 v1.19 / 接口文档 v1.18）：①`utils/` 新增 **`section_splitter.py`**（流式段落切分：按标题行把增量文本切段并标注 `section`，纯函数、可独立单测）；②`utils/sse.py` 注释补「步骤 12 补响应层显式关闭生成器」（Starlette 对同步生成器只停止拉取、不关闭，缺此步业务清理与上游连接释放均失效）；③`services/jd_service.py` 注释细化（补 prompt 组装与列表/详情查询职责）；④`schemas/stream.py` 注释由「协议自检端点」扩为「协议自检 / JD 分析 / …」、`schemas/application.py` 注释补「JD 分析报告列表项与详情」（两个报告 DTO 随投递 DTO 同文件，与 `models/application.py` 的模型归属一致）。`routers/jd_analysis.py`、`services/jd_service.py`、`prompts.py` 三处此前已登记、本次仅细化注释。**§3 前端目录树归前端侧**（JD 分析页对接相关登记由卢世君更新） | 胡康宁 |
| v1.36 | 2026-09-26 | **§1 新增「公共文档的冲突预防」约定**（两次合并冲突的对策沉淀，经讨论确定）：①**汇总类内容降频**——版本清单表只在阶段合并 / 验收时由合并方统一核对，待处理摘要不列具体编号、改为指向「状态」列；②**版本号撞号由后合并方顺延**（本次与后端侧的 v1.34 撞号即为例，后端侧顺延为 v1.35）；③**动公共文档前先 `git fetch`**，对方已改则先合再改；④**冲突取舍口径**——汇总表逐项取较新、表格式追加保留双方并按编号排序。依据：2026-09-26 两次合并的冲突全部落在这三处，正文零冲突 | 卢世君 |
| v1.37 | 2026-09-26 | **§3 前端目录树随步骤 12 登记**：`api/` 段新增 **`jdReports.js`**（JD 分析报告接口：列表 / 详情）；`views/JdAnalysis.vue` 此前已登记，注释「JD 匹配分析（FR-006）」与实际职责一致，本次未动。**§2 后端目录树本次未动**（步骤 12 的后端目录变更已在 v1.35 由后端侧登记） | 卢世君 |
| v1.38 | 2026-09-26 | **§2 后端目录树随 AI 配置免费模型档校正**（对应系统设计 v1.21 / 数据库设计 v1.11 / 接口文档 v1.20）：①`config.py` 注释补 `SHARED_KEY_*`（平台免费模型共享 Key）与 `LLM_TRUST_ENV`（出网代理开关，默认直连）；②`database.py` 注释补「启动同步平台共享配置行（`user_id=0`）」；③`clients/llm_client.py` 注释补「免费档取平台行 Key」、与 `clients/llm_provider.py` 同补**自建 httpx 客户端**（代理由 `LLM_TRUST_ENV` 控制，系统设计 v1.21 §5.4）；④`routers/llm_providers.py`、`schemas/llm_provider.py`、`services/llm_provider_service.py` 三处注释补免费模型清单与选用。**无新增文件**（本次均为既有文件的功能增补）。**§3 前端目录树归前端侧**（AI 配置页改两步入口相关登记由前端侧更新，见[前端待做事项](../07-工作日志/前端待做事项.md) #48） | 胡康宁 |
| v1.39 | 2026-09-26 | **§2 后端目录树与依赖清单随公开免 Key 服务校正**（对应系统设计 v1.22 / 接口文档 v1.21）：①`clients/llm_provider.py` 注册表家数 12 → **13**，补「含公开免 Key 服务，`is_public` 标记驱动免费清单与降级」；②`clients/llm_client.py` 配置解析注释由「免费档取平台行 Key」改为「**免费档两类来源**：公开免 Key 服务直接可用 / 平台共享 Key 取平台行」，并补「含建流失败自动降级」；③`services/llm_provider_service.py` 免费模型档注释补「清单两类来源」；④§5 依赖清单 `openai` 行家数同步为 13。**无新增文件、无新增依赖** | 胡康宁 |
| v1.40 | 2026-09-26 | **§3 前端目录树随 AI 配置页改造同步**（台账 **#50**，对应 SRS v1.13/v1.14 FR-018）：①`components/` 的 **`ProviderCard.vue`**（12 家供应商卡片形态）**退役删除**，改登记 **`ProviderFormDialog.vue`**（配置弹窗：「添加自定义模型」与行内「修改」共用，注册表退为弹窗内的供应商预选项）；②`AiConfig.vue` 注释由「12 家供应商卡片：Key/模型/测试/激活」改为「**免费模型下拉 + 我的配置列表**：选免费模型即生效 / 加自己的 Key，两条路径共用激活位」。**§2 后端目录树本次未动** | 卢世君 |
| v1.41 | 2026-09-26 | **§2 后端目录树随免费档摘除还原**（对应系统设计 v1.23 / 接口文档 v1.23 / 数据库设计 v1.13）：①`config.py` 注释删 `SHARED_KEY_*`（平台共享 Key 配置项随免费档移除）；②`schemas/llm_provider.py` 注释删「免费模型清单与选用 DTO」、`routers/llm_providers.py` 注释删同项；③`services/llm_provider_service.py` 注释删「免费模型档（清单两类来源·选用·档位互转）」；④`clients/llm_client.py` 配置解析注释改为单一路径（账号生效行解密 → `.env` 兜底 → 10012）、删自动降级；⑤`clients/llm_provider.py` 家数 13 → **12**、删 `is_public` 标记说明；⑥§5 依赖清单 `openai` 行家数同步为 12。**无新增文件、无新增依赖**。**§3 前端目录树待前端自行更新**（`AiConfig.vue` 需撤掉免费下拉相关注释——见[前端待做事项](../07-工作日志/前端待做事项.md)） | 胡康宁 |
| v1.42 | 2026-09-27 | **§3 前端目录树随免费档摘除还原**（对应接口文档 v1.23、台账 **#56**）：`AiConfig.vue` 注释由「免费模型下拉 + 我的配置列表：选免费模型即生效 / 加自己的 Key，两条路径共用激活位」改为「**「我的配置」单一入口**：每行一家供应商、行首标记当前使用中，注册表退到弹窗里当预选项」——平台免费模型档已摘除，AI 能力统一由账号自配 API Key。**§2 后端目录树本次未动**（已由 v1.41 还原） | 卢世君 |

---

## 1. 仓库与分支形态

**远端仓库**：`https://github.com/hkangning/job-hunting-assistant`

采用**单仓库 + 分支隔离**：仓库根下三个平级子目录 `docs/`、`backend/`、`frontend/`；两人各用一份本地工作目录，各自 checkout 自己的分支，代码写在自己负责的子目录内。

| 分支 | 承担内容 | 代码落位（仓库根下） | 负责人 |
|---|---|---|---|
| `backend` | 后端代码 + 公共文件副本 | `backend/`（后端全部文件）、`docs/`、`README.md`、`.gitignore` | 胡康宁 |
| `frontend` | 前端代码 + 公共文件副本 | `frontend/`（前端全部文件）、`docs/`、`README.md`、`.gitignore` | 卢世君 |
| `main` | 汇合分支（三目录齐全，可运行完整系统） | `docs/`、`backend/`、`frontend/` | — |

**职责边界**：两人各管一摊，**不越界改动对方范围内的内容**。

| 负责人 | 负责范围 |
|---|---|
| 胡康宁（后端） | `backend/` 全部代码、数据库设计；文档中**除前端 / 测试外**的章节（后端 / 数据库 / 共性内容） |
| 卢世君（前端 + 测试） | `frontend/` 全部代码、测试工作；文档中**前端 / 测试相关**的章节（含**测试计划文档**） |

后端侧**不主动改动**下列内容，需变更时知会对方、由对方执行：

- `frontend/` 下任何文件（含 `package.json`、`package-lock.json`、`.npmrc`、构建配置）；
- 测试相关：`backend/tests/` 内容、**测试计划文档**、测试日志；
- 前端 / 测试专属文档内容：前端工作日志、问题记录，及本文档中的前端目录树、前端依赖清单、前端启动等章节。

反向同理：前端侧不主动改动 `backend/` 下的后端代码与数据库相关文档章节。

**公共文件**（`docs/`、`README.md`、`.gitignore`）在 `backend`、`frontend` 两条分支上**各持一份**，任一侧改后需**相互拉取**同步（只取公共文件，不带对方代码）：

```bash
git fetch origin
git checkout origin/<对方分支> -- docs README.md .gitignore
git add -A && git commit -m "文档同步：<变更内容>"
git push origin <自己分支>
```

改公共文件前先知会对方；两侧副本内容保持一致（git 入库均为 LF，工作区行尾差异不影响）。

**公共文档的冲突预防**（2026-09-26 起）：公共文件在两条分支上并行演进，合并冲突**几乎全部集中在三处**——末尾追加（变更记录、事项表尾）、汇总行与汇总表（版本清单、待处理摘要）、版本号递增；**正文极少冲突**（两次合并实测）。约定：

- **汇总类内容降频**：根 `README.md` 与 [docs/README.md](../README.md) 的「版本清单表」、[前端待做事项](../07-工作日志/前端待做事项.md) 的「当前待处理」摘要行，**日常开发不改**——版本清单表只在**阶段合并 / 验收时由合并方统一核对一次**；待处理摘要**不列具体编号**，改为指向表内「状态」列。这两处是快照：改它的收益只是「一眼看得方便」，代价是每轮必冲突；
- **版本号撞号由后合并方顺延**：双方并行递增，撞号是必然而非偶然（2026-09-26 项目结构 **v1.34 两侧各写一份**）。**后合并的一方负责把撞号的改成下一个号**，并在自己的变更记录里写明顺延事实；
- **动公共文档前先拉**：改动 `docs/`、`README.md` 前先 `git fetch origin`；若 `git log HEAD..origin/<对方分支> -- <目标文件>` 非空，**先合再改**。基于旧版写是冲突的直接成因——2026-09-26 合并时后端侧 README 里的测试计划版本为 **v1.24**，而前端侧已到 **v1.26**；
- **冲突时的取舍口径**：汇总行 / 汇总表**逐项取较新**（不整块取某一边——2026-09-26 README 六行中三行取后端、一行取前端）；表格式追加冲突**保留双方、按编号排序**（2026-09-26 前端待做事项：前端侧 #41~43 已完成行 + 后端侧新增 #44/#45）。

**各分支独有文件天然分属不同子目录**（`backend/`、`frontend/`），合并时自动并存，无需同步。不入库：`backend/.venv/`、`frontend/node_modules/`、`*.db`、`frontend/dist/`、`.env`（真实密钥）。

**跨职责文件区**：`backend/tests/` 在目录上归 `backend/`，但测试代码**内容**由测试负责人（卢世君）维护——**目录归属与内容归属不同**。约定：

- 测试侧改动此目录前，先 `git fetch origin` 取后端侧最新版再叠加，**不基于本地旧版重写**；
- 后端侧因修 fixture、补测试隔离逻辑等需改动此目录时，**改前先知会测试侧**；
- 该目录的合并冲突**以后的版本为准**（与公共文件同口径）。

依据：2026-09-24 `conftest.py` 在合并中被拼成两套测试库隔离方案并存，`import` 丢失致测试全线失败（见[问题记录](../07-工作日志/问题记录.md) IS-09 / IS-13）。

**问题记录**：`docs/07-工作日志/问题记录.md` 虽位于 `docs/` 下（属公共文件），但**编号分配、表结构与新条目登记由测试负责人（卢世君）主导维护**；**条目的状态标注按「谁处理谁标注」**（2026-09-26 起）。约定：

- 后端侧发现问题时，先记入[后端工作日志](../07-工作日志/后端工作日志.md)并知会测试负责人，由对方分配编号（`IS-XX`）登记进问题记录；
- 后端侧**新增条目与表结构只读**；确需修改时先知会测试侧，由对方执行；
- **条目处理完毕后由实际处理方直接标注**（2026-09-26 起）：后端侧闭环的条目由**后端侧自行**把「处理」「结论」「状态」三栏改为最终口径并同步文末「状态统计」，写明依据（文件 / 版本号 / 命令结果），**不再等测试侧结案**；前端 / 测试侧闭环的条目由测试侧标注；原始「问题」「影响」描述保持原样。

**测试计划文档**：`docs/05-测试文档/测试计划.md` 属公共文件，**内容由测试负责人（卢世君）维护**（用例编号与口径的权威出处），后端侧**不主动修改**。约定：

- 后端侧发现用例口径需随实现调整时，先记入[后端工作日志](../07-工作日志/后端工作日志.md)并知会测试负责人，由对方修订；
- 后端侧引用测试计划时**只读**；确需修改时先知会测试侧，由对方执行；
- **维护节奏（2026-09-26 起）**：本文件由测试侧**随实现与用例同步直接修订**（不再走事前确认流程，目的是减少合并冲突），改后在[测试日志](../07-工作日志/测试日志.md)留记录即可；**其余交付文档不在此列**——需求规格说明书 / 系统设计 / 数据库设计 / 接口文档 / 开发计划 / 项目结构 / 根 `README.md` / `docs/README.md` 仍由后端维护，测试侧确需改动时先知会。

**前端待做事项**：`docs/07-工作日志/前端待做事项.md` 属公共文件，**条目由后端侧登记、完成状态由前端 / 测试侧回标**（2026-09-26 起自[后端工作日志](../07-工作日志/后端工作日志.md) 末表独立成文）。约定：

- 后端侧产生、需前端 / 测试侧配合的事项，一律追加到该文档表尾，不再散落对话或写进后端工作日志正文；
- 前端 / 测试侧完成后，自行把「状态」列改为「已完成（日期，执行人）」并写明依据（文件 / 版本号 / 命令结果），**不删行**；
- 后端侧定期 `git fetch origin` 核对事项在 `origin/frontend` 的实际落地情况并回标，**不等对方通知、不等提醒**；
- 该文档与问题记录、测试计划的归属**相反**（**内容归后端、回标归前端**），三者互不替代：问题记录仍是问题编号（`IS-XX`）的权威出处，测试计划仍归测试侧。

**合入 main**：两分支都稳定后，在任一工作目录执行 `git checkout main && git merge backend && git merge frontend && git push origin main`。

## 2. 后端（backend/）

```
backend/
├── requirements.txt           # Python 依赖清单
├── .env.example               # 环境变量模板（真实 .env 已 gitignore）
├── pytest.ini                 # pytest 配置（pythonpath/testpaths/asyncio_mode）
├── seed/
│   └── questions.json         # 八股种子题库（585 题 / 5 技术栈 / 18 领域 / 含 152 场景题，首次启动 upsert 导入）
├── uploads/avatars/           # 头像文件（步骤 5 落地；已入 .gitignore）
├── cache/tts/                 # TTS 合成缓存（步骤 24 落地；已入 .gitignore）
├── app/
│   ├── main.py                # FastAPI 入口：lifespan 建库/导种子/启动定时器/注册路由
│   ├── config.py              # 配置加载（pydantic-settings，读 .env；含 APP_SECRET_KEY、LLM_TRUST_ENV（出网代理开关，默认直连））
│   ├── database.py            # engine/SessionLocal/Base 声明；连接池参数（pool_pre_ping / pool_recycle）；启动同步平台共享配置行（user_id=0）
│   ├── deps.py                # FastAPI 依赖：get_current_user（JWT 校验 → 注入当前账号 User 对象）
│   ├── models/                # 数据层：SQLAlchemy ORM（22 张表，见数据库设计文档）
│   │   ├── __init__.py        # 全量导出 Base 与模型
│   │   ├── enums.py           # 全部枚举类（与数据库设计文档第 5 章一一对应）
│   │   ├── user.py            # User / LlmProviderConfig（账号与 AI 供应商配置）
│   │   ├── application.py     # Application / JdAnalysisReport
│   │   ├── interview.py       # InterviewSession / InterviewQa
│   │   ├── drill.py           # DrillTopic / DrillAttempt（练习模式：我的题目与逐次练习记录）
│   │   ├── experience.py      # Experience / ExperienceItem
│   │   ├── question.py        # Question / PracticeRecord / WrongQuestion
│   │   ├── agent.py           # AgentConversation / AgentMessage
│   │   ├── system.py          # Reminder / CampusEvent / Config / UserProfile
│   │   ├── campus.py          # CrawlSource / JobPosting / Subscription（校招情报：源配置 / 岗位 / 订阅规则）
│   ├── schemas/               # 传输层：Pydantic DTO（每个字段带中文注释）
│   │   ├── common.py          # 统一响应体 / 分页模型
│   │   ├── auth.py            # 注册/登录/账号信息/改密/头像 DTO
│   │   ├── llm_provider.py    # 供应商配置/模型列表/连通性测试 DTO
│   │   ├── application.py     # 投递请求/响应 DTO（含导入结果模型 / JD 分析报告列表项与详情）
│   │   ├── overview.py        # 今日概览聚合响应 DTO（含状态计数与个人中心计数）
│   │   ├── interview.py       # 会话/问答 DTO
│   │   ├── practice.py        # 抽题/点评/错题 DTO
│   │   ├── drill.py           # 练习模式题目/练习记录 DTO
│   │   ├── experience.py      # 面经/条目 DTO
│   │   ├── agent.py           # Agent 消息/工具 DTO
│   │   ├── system.py          # 画像/设置/提醒/宣讲会 DTO
│   │   ├── campus.py          # 校招情报 DTO（岗位 / 信息源 / 订阅规则 / 日历事件）
│   │   └── stream.py          # 流式链路请求 DTO（协议自检 / JD 分析 / …，步骤 11 起）
│   ├── routers/               # API 层：路由 + 校验，禁止直连 ORM
│   │   ├── health.py          # GET /health（免鉴权）
│   │   ├── auth.py            # 注册/登录/当前账号/改资料/改密/头像（前三者免鉴权或自带校验）
│   │   ├── llm_providers.py   # AI 供应商配置（列表/保存/删除/激活/模型列表/连通性测试）
│   │   ├── overview.py        # GET /overview
│   │   ├── applications.py    # 投递 CRUD/导入/模板/状态流转
│   │   ├── jd_analysis.py     # JD 报告查询
│   │   ├── interview.py       # 面试会话 CRUD
│   │   ├── practice.py        # 抽题/陪练历史/题库
│   │   ├── drill.py           # 练习模式（我的题目/逐次练习记录/跨次进步）
│   │   ├── wrong_questions.py # 错题 CRUD/复习判定
│   │   ├── experiences.py     # 面经 CRUD/检索
│   │   ├── agent.py           # Agent 会话/消息/工具执行
│   │   ├── profile.py         # 画像
│   │   ├── settings.py        # 设置（账号级 + 系统级，LLM 字段已迁至 /llm-providers）
│   │   ├── voice.py           # 语音：转写 / 合成 / 音色列表（P2）
│   │   ├── reminders.py       # 提醒/手动触发
│   │   ├── campus.py          # 校招情报：宣讲会/双选会列表 + 岗位（含投喂预览与入库）+ 日历聚合
│   │   ├── crawl_sources.py   # 信息源管理：清单/新增/修改/删除/手动触发采集（步骤 21）
│   │   ├── subscriptions.py   # 订阅规则：列表/新增/修改/删除（步骤 22）
│   │   └── stream.py          # 全部 SSE 端点（demo 自检 / jd-analysis / interview-chat / …）
│   ├── services/              # 服务层：业务逻辑 + 事务边界，禁止返回 ORM 对象
│   │   ├── auth_service.py          # 注册/登录校验/Token 签发/改密/头像落盘/登录失败锁定
│   │   ├── llm_provider_service.py  # 供应商配置读写/Fernet 加解密/激活互斥/模型拉取落缓存
│   │   ├── settings_service.py      # 设置读写：账号级偏好 + 系统级抓取配置（两级 config）
│   │   ├── application_service.py   # 投递业务/状态机校验/批量导入解析
│   │   ├── overview_service.py      # 概览聚合：面试提醒/状态计数/跟进提醒/错题与面试计数
│   │   ├── interview_service.py     # 会话编排：开场/作答/点评/总结落库
│   │   ├── practice_service.py      # 抽题/点评/错题入本（评分<6 联动）
│   │   ├── drill_service.py         # 练习模式：题目 CRUD/逐次作答落库/跨次对比
│   │   ├── wrong_question_service.py# 复习档位推进规则（纯函数，可单测）
│   │   ├── experience_service.py    # 面经保存/条目落库/检索
│   │   ├── agent_service.py         # 意图路由+工具注册表+函数调用编排
│   │   ├── jd_service.py            # JD 分析：prompt 组装（画像摘要 + JD 原文）/ 报告落库 / 分数提取 / 列表与详情
│   │   ├── reminder_engine.py       # 定时任务：纯规则判定（无 LLM）+ 文案生成
│   │   ├── campus_service.py        # 校招情报：采集调度编排/投喂抽取与入库/匹配打分/订阅匹配写提醒/日历聚合
│   │   └── profile_service.py       # 画像读写/Agent 提取建议合并
│   ├── clients/               # 外部调用封装（唯一出网口）
│   │   ├── llm_client.py      # LLM 对话封装：配置解析（账号生效行解密 → .env 兜底 → 10012）/stream_chat/chat_json/超时重试/异常映射/get_llm_client 注入点（步骤 10 落地）
│   │   ├── llm_provider.py    # 供应商+模型两级注册表（12 家）+ list_models 动态拉取（/models 端点）+ test_connection 连通性探测（步骤 6 落地）；与 llm_client 均自建 httpx 客户端（代理由 LLM_TRUST_ENV 控制）
│   │   ├── asr_client.py      # ASR 抽象 + FunASR 本地流式（默认）+ 讯飞云端（备选）（P2）
│   │   ├── tts_client.py      # edge-tts 合成 + sha1(text+voice) 文件缓存 + 音色表（P2）
│   │   └── crawler/           # 校招信息采集（步骤 21 落地；抓取的唯一出网口）
│   │       ├── base.py        # 适配器协议：fetch(source) → RawItem 列表（统一字段结构）
│   │       ├── adapters/      # 一厂商一解析器：91job（免登录 JSON）/ 云就业（SSR）/ 才立方（SSR）
│   │       ├── processor.py   # 处理层纯函数（零 IO、可离线单测）：归一化/去重指纹/变更检测/过期归档/匹配打分
│   │       └── compliance.py  # 合规组件（NFR-016）：UA 标识/robots 检查/同域限频/只存元数据
│   ├── prompts.py             # 全部场景 prompt 模板（统一管理，见设计文档 5.3）
│   ├── exceptions.py          # 业务异常类 + code 映射（与接口文档错误码表一致）
│   └── utils/
│       ├── datetime_utils.py  # 时间格式化/日期比较工具
│       ├── security.py        # JWT 签发与校验 / bcrypt 密码哈希 / Fernet 对称加解密 / 头像压缩
│       ├── sse.py             # SSE 协议层（步骤 11 落地）：事件构造器 + 响应包装器 + 独立会话/断连/异常分派（步骤 12 补响应层显式关闭生成器）
│       ├── section_splitter.py # 流式段落切分（步骤 12 落地）：按标题行把增量文本切段并逐块标注 section（纯函数，可独立单测）
│       └── speech_metrics.py  # 表达力指标计算（纯函数，不触 LLM：语速/停顿/填充词/时长占比/趋势）
└── tests/                     # 测试（对应测试计划）
    ├── conftest.py            # fixture：测试库/TestClient/FakeLLM/FakeASR/FakeTTS
    ├── test_health.py
    ├── test_auth.py
    ├── test_llm_providers.py
    ├── test_applications.py
    ├── test_overview.py
    ├── test_interview.py
    ├── test_practice.py
    ├── test_wrong_questions.py
    ├── test_experiences.py
    ├── test_agent.py
    ├── test_jd_analysis.py
    ├── test_reminder_engine.py
    ├── test_profile_settings.py
    └── test_sse_streams.py
```

**后端启动**：在 `backend/` 目录下先用 `.\.venv\Scripts\Activate.ps1` 激活虚拟环境（PowerShell），再执行 `python -m uvicorn app.main:app --port 8000`（开发期 `--reload`）；未激活时把 `python` 换成 `.\.venv\Scripts\python.exe`。

首次启动前把 `.env.example` 复制为 `.env` 并填两项：`APP_SECRET_KEY`（JWT 签名 + Fernet 加密的主密钥，任意长随机串，缺失时启动即报错）与供应商 API Key（也可登录后在 AI 配置页填，存库优先）。

## 3. 前端（frontend/）

```
frontend/
├── package.json
├── vite.config.js             # 代理 /api → http://localhost:8000（mock 开启时不注册代理，二者互斥）
├── index.html
├── mock/                      # dev-only 接口 mock（Vite 插件中间件，不进生产构建；后端就绪后置 VITE_USE_MOCK=false 即停用）
│   ├── index.js               # 插件入口：接管 /api/v1/* 与 /uploads/avatars/*（与后端挂载点一致）
│   ├── db.js                  # 内存库：账号/画像/配置/头像字节（字段口径对齐数据库设计）
│   ├── auth.js                # /auth/* 7 个 handler（按真实业务规则：唯一性/失败锁定/改密作废旧 Token）
│   ├── misc.js                # 画像/设置/概览/业务空响应/头像静态文件
│   └── utils.js               # 统一响应体、请求体与 multipart 解析、mock token 签发校验
└── src/
    ├── main.js                # 挂载 + Element Plus（含主题定制）+ 设计 token + Pinia + Router
    ├── App.vue                # 布局骨架：侧栏 + 顶栏 + 内容区（见设计文档 4.4）；meta.layout='blank' 的账号页走全屏壳 + <AgentFloatBall/> + <GuideTour/>
    ├── api/
    │   ├── request.js         # axios 实例：统一注入 Authorization、拦截 code≠0 弹错、80001/80002 触发全局登出
    │   ├── auth.js            # 账号与鉴权接口（注册/登录/me/改资料/改密码/头像）
    │   ├── profile.js         # 画像接口（另含与 settings.js 重复的设置接口导出）
    │   ├── settings.js        # 设置接口（账号级偏好 + 系统级抓取配置，设置页出口）
    │   ├── overview.js        # 概览接口（本步先取统计供个人中心，步骤 9 扩充）
    │   ├── llmProviders.js    # AI 供应商配置接口（列表/保存/删除/激活/模型列表/连通性测试）
    │   ├── applications.js    # 投递接口
    │   ├── jdReports.js       # JD 分析报告接口：列表 / 详情（步骤 12）
    │   ├── interview.js       # 面试接口
    │   ├── practice.js        # 陪练/错题接口
    │   ├── drill.js           # 练习模式接口（题目列表/新建/详情/归档/单次详情/跨次对比，P2）
    │   ├── experience.js      # 面经接口
    │   ├── agent.js           # Agent 接口
    │   └── voice.js           # 语音接口（转写 / 合成 / 音色列表，P2）
    ├── constants/
    │   ├── application.js     # 投递状态常量：中文名/色值/合法流转（唯一出处）
    │   └── regions.js         # 全国省市选项（省→地级市两级，34 省级/340 城市），城市级联选择用
    ├── stores/
    │   ├── user.js            # 当前账号：Token（localStorage 持久化）/用户信息/登录登出/失效清理（见设计文档 4.2）
    │   ├── app.js             # 设置项 + 全局提示
    │   ├── agent.js           # 悬浮球开关/对话/确认卡片队列
    │   └── overview.js        # 概览数据（30s 轮询刷新）
    ├── router/
    │   └── index.js           # 路由表 + 路由守卫（未登录重定向 /welcome，见设计文档 4.1）
    ├── utils/
    │   ├── avatar.js          # 默认头像（用户名首字 + 哈希底色）与上传前 canvas 压缩至 256×256
    │   ├── datetime.js        # 时间显示格式化（ISO → MM-DD HH:mm；步骤 9 真联调时抽出）
    │   └── sse.js             # SSE 流式客户端：fetch + ReadableStream 解析、事件回调、中止（步骤 11）
    ├── styles/
    │   └── tokens.css         # 设计 token：色彩/间距/圆角的 CSS 变量单一出处（见设计文档 4.5）
    ├── components/
    │   ├── SideNav.vue        # 侧边导航：2 组菜单（求职流程/能力提升）+ 顶部品牌 + 底部今日待办（见设计文档 4.4）
    │   ├── AppHeader.vue      # 顶栏：折叠按钮 + 页面标题 + actions 具名插槽 + 右侧用户菜单
    │   ├── UserMenu.vue       # 顶栏右上角：头像 + 用户名 + 下拉（个人中心/设置/AI 配置/退出登录）
    │   ├── AuthShell.vue      # 账号页外壳：深紫渐变全屏 + 居中白卡片（登录/注册共用）
    │   ├── AgentFloatBall.vue # 全局悬浮球（浮窗+快捷指令 chips）
    │   ├── StreamText.vue     # 打字机文本（纯展示：rAF 平滑追赶传入文本；section 分段由调用方处理）
    │   ├── GuideTour.vue      # 新手指引：5 步高亮编排（driver.js，P2；步骤配置集中在本组件）
    │   ├── StatusKanban.vue   # 投递看板（列间拖拽流转）
    │   ├── ApplicationFormDialog.vue   # 投递新增/编辑表单对话框
    │   ├── ApplicationImportDialog.vue # 投递批量导入（模板下载/上传/错误行展示）
    │   ├── CloseReasonDialog.vue       # 标记已结束时的结束原因选择（未通过/主动放弃/无消息）
    │   ├── TrendChart.vue     # 投递趋势折线图（ECharts 按需引入）
    │   ├── ApplicationStats.vue # 投递统计条（总数/本周新增 + 状态占比堆叠条；受控组件，由页面传入 items 现算，不调接口）
    │   ├── ConfirmCard.vue    # Agent 工具确认卡片
    │   ├── ProviderFormDialog.vue # AI 供应商配置弹窗（「添加自定义模型」与行内「修改」共用；注册表退为预选项，含 Key/模型/端点与就地探测）
    │   └── VoiceInput.vue     # 语音作答（P2：连续录音 + 前端 VAD 静音断句 + 逐句转写回填）
    └── views/
        ├── Welcome.vue            # 入场动画（未登录入口页，兼作登录态预检，见设计文档 4.6）
        ├── Login.vue              # 登录（含「30 天免登录」勾选）
        ├── Register.vue           # 注册（密码强度提示不拦截提交）
        ├── Overview.vue           # 今日概览（FR-001）
        ├── Applications.vue       # 投递管理看板（FR-002~005）
        ├── JdAnalysis.vue         # JD 匹配分析（FR-006）
        ├── InterviewList.vue      # 面试会话列表
        ├── InterviewChat.vue      # 面试会话进行页（含语音作答，FR-007/014）
        ├── Practice.vue           # 八股陪练（FR-009）
        ├── DrillList.vue          # 我的题目（练习模式列表，FR-020，P2）
        ├── DrillDetail.vue        # 题目详情（历次记录时间线与跨次进步对比，FR-020，P2）
        ├── WrongQuestions.vue     # 错题本 + AI 抽问（FR-010）
        ├── Experiences.vue        # 面经整理与检索（FR-008）
        ├── Profile.vue            # 个人中心（账号信息/头像/改密/画像/数据概览/重看新手指引，FR-017/019）
        ├── AiConfig.vue           # AI 配置（「我的配置」单一入口：每行一家供应商、行首标记当前使用中；注册表退到弹窗里当预选项，FR-018）
        ├── Settings.vue           # 系统设置（账号级偏好 + 抓取开关；LLM 字段已迁出，FR-015）
        └── DevStream.vue          # 流式协议调试页 /dev/stream（靶子 POST /stream/demo，不进导航，步骤 11）
```

**前端启动**：在 `frontend/` 目录下先执行 `npm install`，再执行 `npm run dev`（5173 端口）。环境要求 **npm ≥ 11.12.1**、Node `^20.19.0 || >=22.12.0`（随 Vite 8）；`frontend/.npmrc` 已设 `engine-strict=true`，版本不符时 `npm install` 报 `EBADENGINE` 拒绝安装——用于防止 npm 版本差异导致 `package-lock.json` 反复互改（11.6.1 会给 optional 平台包写冗余 `dev`/`peer` 标记，11.12.1 及 12.x 不写，详见[问题记录](../07-工作日志/问题记录.md) IS-14）。**升 npm 前须先确认 Node 版本**：npm 12.x 自身要求 Node `^22.22.2 || ^24.15.0 || >=26.0.0`，Node 低于此时 `npm install -g npm@latest` 报 `EBADENGINE` 装不上（2026-09-24 实测 Node 24.11.0 被拒），须先升 Node 或改装 `npm@11.12.1`（两者产出的锁文件逐行一致）。

## 4. 命名与编码约定

| 项 | 约定 |
|---|---|
| Python | PEP 8；模块 snake_case、类 PascalCase；routers/services 内函数与接口语义同名（`create_application` 等） |
| Vue | 组件 PascalCase 文件名；组合式 API（`<script setup>`）；页面组件命名与路由一致 |
| DTO | Pydantic 模型每字段中文注释（含枚举取值），与接口文档字段表一一对应 |
| 枚举 | 单一定义在 `app/models/enums.py`，DB 存 VARCHAR 码，DTO 层做 str/enum 转换 |
| 错误码 | 只允许在 `app/exceptions.py` 定义，与接口文档错误码表一致，禁止散落魔法数字 |
| 鉴权 | 账号私有接口一律经 `Depends(get_current_user)` 取 `user_id`，**不接受请求体 / 查询参数传入的 user_id**；公共接口（health、注册、登录）显式豁免 |
| 提交规范 | 一次提交对应一个文档或一个功能步骤；message 用中文概述 |

## 5. 依赖清单

**backend/requirements.txt**（版本以安装时稳定版为准）：

```
fastapi
uvicorn[standard]
sqlalchemy>=2.0
pymysql               # MySQL 驱动（数据库为 MySQL 8.0，连接池参数 pool_pre_ping / pool_recycle）
pydantic>=2.0
pydantic-settings
openai                # openai 兼容 SDK（12 家供应商统一走该协议）
apscheduler
python-multipart      # 文件上传（含头像）
PyJWT                 # JWT 签发与校验（HS256）
bcrypt                # 密码哈希（直接调用，不引 passlib）
cryptography          # Fernet 对称加密（供应商 API Key 落库）
Pillow                # 头像压缩至 256×256
openpyxl              # Excel 批量导入
httpx                 # HTTP 客户端（供应商调用 + 校招信息采集）
lxml                  # HTML 解析（校招信息采集：三家就业网 SSR 页面，**步骤 21 才装**）
websockets            # 讯飞实时转写（备选供应商，P2）
funasr                # 本地流式 ASR（默认语音转写，P2；**依赖 torch，体积数 GB，步骤 24 才装**）
edge-tts              # 语音合成（默认 TTS，P2；纯 Python 包，无需注册）
pytest
pytest-asyncio
pytest-cov            # 覆盖率（测试计划 1.2）
```

**frontend 依赖**：vue、vue-router、pinia、element-plus、@element-plus/icons-vue、axios、vite、@vitejs/plugin-vue、**vuedraggable**（投递看板列间拖拽，须装 `@next` 标签的 4.x，npm `latest` 是 Vue 2 版）、**echarts**（趋势图，按需引入）、**driver.js**（新手指引高亮遮罩，约 5KB 零依赖，**P2、步骤 28 才装**）。

> **语音前端不引依赖**：VAD 静音断句用 Web Audio API 的 `AnalyserNode` 自行判定音量阈值（**按语速自适应**：语速快 1.6s / 正常 2.65s / 慢 3.6s 切句，见系统设计 §5.6），录音用浏览器原生 `MediaRecorder`，播报用原生 `<audio>` 播放后端返回的 mp3——三者均为平台能力，符合设计文档 1.2「最小依赖」原则。
