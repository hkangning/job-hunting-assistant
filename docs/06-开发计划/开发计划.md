# 个人求职助手 —— 开发计划

| 项 | 内容 |
|---|---|
| 文档版本 | v2.16 |
| 文档状态 | 定稿 |
| 维护人 | 胡康宁 |
| 最后更新 | 2026-09-26 |
| 上游依据 | 需求规格说明书 / 系统设计 / 数据库设计 / 接口文档 / 测试计划 / 项目介绍 |

## 变更记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| v1.0 | 2026-09-23 | 首版：11 个开发步骤（按进度不按日期）+ 团队分工 + 风险清单 + 验收演示预案 | 胡康宁 |
| v2.0 | 2026-09-24 | 开发周期预算 ≥2 个月：步骤 11→21 步细化，分 5 个阶段 + 5 个里程碑验收点，新增质量基线 | 胡康宁 |
| v2.1 | 2026-09-24 | ①步骤 2「主要验收用例」由文字描述改为 TC-51（测试计划 v1.4 新增该编号，下游同步）（卢世君）；②修正步骤 2 验收标准笔误：「questions 表」应为「question 表」（表名单数，依数据库设计文档 §1；见问题记录 IS-05）（胡康宁） | 卢世君 / 胡康宁 |
| v2.2 | 2026-09-24 | 补充**用户视角成果**维度：①第 4 节总览表新增「用户可见成果」列（21 步逐条）；②第 5 节各步骤详情在「产出」后新增同名段落，写明该步完成后系统对使用者呈现什么——纯后端步骤诚实标注"界面无变化"并给出可现场验证的方式；③第 1 节质量基线新增第 ⑤ 项「用户视角成果对照」，使该维度进入每步验收而非仅作说明 | 胡康宁 |
| v2.3 | 2026-09-25 | **多账号体系步骤**（对应 SRS v1.5 / 系统设计 v1.5 / 接口文档 v1.3 / 数据库设计 v1.3）：①**21 步 → 24 步**——在已完成的步骤 1~4 之后新增「**步骤 5 账号与鉴权（后端）**」「**步骤 6 AI 供应商配置（后端）**」「**步骤 7 账号体系（前端）**」「**步骤 8 AI 配置页 + 设置页瘦身（前端）**」四步；②原「步骤 7 用户画像 + 系统设置」**取消**，内容拆分并入新步骤 5~8（画像接口随账号 → 步骤 5；连通性测试与模型列表随 AI 配置 → 步骤 6；画像表单随个人中心 → 步骤 7；设置页改版 → 步骤 8）；③原步骤 5~21 **顺延为 9~24**；④阶段划分同步：阶段一 1~9、阶段二 10~17、阶段三 18~21、阶段四 22~23、阶段五 24；⑤**M1 口径更新**为「账号闭环 + 投递闭环 + 概览可用，pytest 全绿」；⑥新增四步的测试用例编号由测试侧分配（见 v2.3 待办） | 胡康宁 |
| v2.4 | 2026-09-25 | **步骤 22 语音方案重定**（对应 SRS v1.6 / 系统设计 v1.6 / 接口文档 v1.4 / 数据库设计 v1.4）：①步骤 22 由「讯飞语音听写」改为「**FunASR 本地流式 + edge-tts 合成**」——支持**长时长连续语音交互**（前端 VAD 约 1.2s 静音断句、逐句上传累积回填，不限轮次与总时长）与**鲜活音色播报**（晓晓 / 云希 / 云扬可选、可打断）；②总览表步骤 22 的交付物列同步；③风险清单「讯飞注册/审核周期长」两条替换为「本地模型下载/环境失败」（缓解：切讯飞云端备用）与「依赖体积大」（缓解：依赖仅在步骤 22 落地、模型不入仓库）；④演示脚本第 6 环节改「语音连续作答」；⑤「演示环境无外网」风险补注语音链路已脱网可演示。**动因**：语音需支持长时间交互且音色要鲜活，讯飞语音听写单次 ≤60 秒做不了长时长 | 胡康宁 |
| v2.5 | 2026-09-25 | **新增新用户指引步骤**（对应 SRS v1.7 FR-019 / 系统设计 v1.7 / 接口文档 v1.5 / 数据库设计 v1.5）：①**24 步 → 25 步**——新增「**步骤 24 新手指引（P2）**」，原步骤 24「整体验收 + 演示准备」**顺延为 25**；②该步**排期明确为"全部功能实现之后"**（指引高亮的目标入口须已定型，功能未齐就做会返工）；③阶段五由「24」改「24~25」、主题改「新手指引与交付」，M5 口径改 **AC-1~AC-12**；④总览表新增步骤 24 行、步骤 25 行验收用例改 AC-1~AC-12；⑤第 1 节总则步骤数 24 → 25。**动因**：账号首次注册需提供可跳过的新用户指引，排期放在全部功能实现之后 | 胡康宁 |
| v2.6 | 2026-09-25 | **新步骤的测试用例编号分配落地**（回应 v2.3 待办「新增四步的测试用例编号由测试侧分配」）：①总览表步骤 5 / 6 / 24 行的「新增用例（测试侧分配）」占位替换为实际编号——步骤 5 = **TC-52~54、TC-58~60**；步骤 6 = **TC-55、TC-56、TC-61**（另 TC-47~49 端点迁移改写）；步骤 24 = **TC-68**；②步骤 5~8、24 详情段落的测试行同步填入编号（步骤 7 = **TC-62~66**、步骤 8 = **TC-67**，原为集称描述）；③配套：测试计划已升 **v1.6**，新增 TC-52~68 共 17 条、改写 TC-31/41/47/48/49/51 共 6 条，**测试计划是用例编号与口径的权威出处** | 卢世君 |
| v2.7 | 2026-09-25 | **步骤 5 落地后的口径自查与对齐**（步骤 5 已完成实现）：①步骤 5 建表口径由「新增 `user` 表（表数 15 → 16）」改为「**新增 `user` + `llm_provider_config` 两张表（表数 15 → 17）**」——`llm_provider_config` 随本步建表（`GET /auth/me` 的 `llm_configured` 字段需读该表，否则该字段无法实现），其配置接口仍在步骤 6；②步骤 6 相应去掉「新增表（16 → 17）」表述，改为「表已在步骤 5 建好」；③步骤 5 验收「既有用例（**74 条**）改造后仍全绿」修正为 **81 条**（步骤 4 已新增 7 条，原数字未同步）；④第 4 节总览表步骤 5 / 6 两行产出列同步。**动因**：步骤 5 完成后按约定做「实现 ↔ 文档」逐条自查，口径以已落地的实现为准（数据库设计 v1.5 本就把两张表列为同一批新增） | 胡康宁 |
| v2.8 | 2026-09-25 | **新增表达力分析与练习模式两步**（对应 SRS v1.10 FR-020 / 系统设计 v1.11 / 数据库设计 v1.7 / 接口文档 v1.7）：①**25 步 → 27 步**——在步骤 22（语音交互）之后插入「**步骤 23 表达力分析（P2）**」（`speech_metrics` 纯函数 + 指标落库 + `done` 事件下发 + 前端指标展示）与「**步骤 24 练习模式（P2）**」（`drill` 两表 + 7 个接口 + 我的题目列表与题目详情页 + 跨次进步对比），原步骤 23~25 **顺延为 25~27**；②阶段四由「22~23」改「**22~25**」（主题补"表达力与练习模式"），阶段五由「24~25」改「**26~27**」；③M4 口径补"表达力指标可视、练习模式可用"，M5 口径由 AC-1~AC-12 改 **AC-1~AC-13**（AC-13 为练习模式验收）；④步骤 22 前端 VAD 口径由固定 1.2s 改**按语速自适应**（快 1.6s / 正常 2.65s / 慢 3.6s）；⑤演示脚本第 6 环节补表达指标展示、新增「练习模式：同一题练两遍看跨次对比」环节（原 7~9 顺延为 8~10）；⑥风险清单新增"表达力指标口径调整致历史数据不可比"一条；⑦顺带修正变更记录中 v2.6 / v2.7 两行的排列顺序。**动因**：语音作答此前只替代键盘、AI 点评的输入与文字作答无差别，"内容分与表达分同时评"缺位；且同一题反复练至流畅的个人练习场景无对应功能 | 胡康宁 |
| v2.9 | 2026-09-25 | **账号体系步骤（5~8）接口与界面清单补全**（起因：核查「个人中心、设置等界面与接口是否在计划中」）：①总览表与步骤详情**补显式接口清单**——步骤 5 补 `/profile` 2 接口（GET/PUT 画像转账号级），步骤 6 补 `/settings` 2 接口（GET/PUT 瘦身）；②**个人中心「数据概览卡片」数据来源定案**——`GET /overview` 响应体新增 `stats` 对象（`application_count` / `wrong_question_count` / `interview_count`），随**步骤 9** 落地；字段名与取值口径**按前端已有实现对齐**（步骤 7 个人中心已按此实现、缺字段时降级显示占位符），接口文档 **v1.9** 同步（**总数不变**）；③步骤 7 入场动画时长「约 2.5s」对齐 SRS v1.9 / 测试计划 v1.8 的「**约 1.8 秒（含末段 0.4s 淡出）**」（同一步骤内两处口径原先不一致）；④步骤 8 补后端接口归属说明（`/settings` 在步骤 6、`/llm-providers` 同步骤 6 落地），避免前端步骤误判为"纯前端"；⑤步骤 22 设置项表述精确化——`/settings` 新增 `asr_provider` / `tts_voice` 两个账号级键，讯飞凭据字段（`asr_app_id` / `asr_api_key`）同节；⑥步骤 23 / 24 总览表「新增用例（测试侧分配）」占位替换为实际编号（**TC-71、TC-72** / **TC-69、TC-70**，测试计划已分配） | 胡康宁 |
| v2.10 | 2026-09-26 | **步骤 5 私有表口径显式化**：原文「10 张账号私有表补 `user_id`」的 10 张**并非笔误**——数据库设计 v1.3 的列举为「8 张业务表 + `config` + `user_profile`」，该口径自洽（`llm_provider_config` 作为新增表单列、不重复计入）。本次仅把**构成写显式**并指向数据库设计（原文只给数字、未说明构成，易被误读为"业务表有 10 张"） | 卢世君 |
| v2.11 | 2026-09-26 | **步骤 6 落地后的口径自查**（步骤 6 已实现并实测）：①**步骤 10 的模块归属修正**——原写「llm_provider.py 全能力」把 `stream_chat` / `chat_json` 也算在该模块，实际按「是否产生对话内容」拆分：探测类（`test_connection` / `list_models` + 24h 缓存 + 内置表回退）随注册表落在 `llm_provider.py` 并**已在步骤 6 交付**，对话类（`stream_chat` / `chat_json` / 超时重试 / LLMError 映射）仍随 `llm_client.py` 留在本步；②本步产出、用户可见成果、验收三处表述同步（连通性冒烟已在步骤 6 交付，本步验收改为**对话链路冒烟**）；③跨文档同批核查：系统设计 v1.13（§5.4 职责拆分 + §8 mock 注入点）、项目结构 v1.22（§2 目录树）、接口文档 v1.10（§3.3 / §3.12 补 9 处实现口径） | 胡康宁 |
| v2.12 | 2026-09-26 | **团队分工表显式纳入测试计划文档**：第 2 节卢世君职责补「**测试计划文档维护**」——原表述为「测试用例编写与执行」，测试计划作为文档的维护职责未点名（项目结构 v1.23 同步） | 胡康宁 |
| v2.13 | 2026-09-26 | **校招情报模块落地为三个步骤**（对应 SRS v1.11 FR-021 / FR-022、系统设计 v1.14、数据库设计 v1.8、接口文档 v1.12）：①**27 步 → 29 步**——「步骤 21 就业网抓取 + 宣讲会」**改写为「多源采集层」**（三家就业网系统适配器 + 源配置接口 + 合规组件 + 处理层），其后新增「**步骤 22 校招情报（后端）**」（`job_posting` 表 / 岗位接口 / 投喂通道 / 匹配打分 / 订阅规则与推送联动 / 日历聚合接口）与「**步骤 23 校招情报（前端）**」（校招情报页 / 日历视图 / 订阅与源管理 / 概览区块改造 / 投递与 JD 分析打通），原步骤 22~27 **顺延为 24~29**；②阶段划分同步：阶段三 18~21 改 **18~23**（M3 口径补"多源校招情报聚合上线"）、阶段四 22~25 改 **24~27**、阶段五 26~27 改 **28~29**，M5 口径由 AC-1~AC-13 改 **AC-1~AC-15**（AC-14 校招情报 / AC-15 站内日历）；③风险清单「学校就业网改版/反爬」一条扩写为多源口径，新增「目标站点明确禁止抓取（robots / 持续 403）」与「岗位信息来源不足」两条；④系统级 `config` 的 `crawl_url` 废弃（改由信息源清单承载），**随步骤 21 落地移除**（步骤 6 详情中该键为历史描述，不回改）；⑤演示预案第 3 环节与数据准备补校招情报内容，收尾亮点补"多源校招信息聚合"；⑥新增用例编号自 **TC-73** 起，由测试侧在测试计划登记（本表暂标"测试侧待分配"）。**动因**：原设计只覆盖本校单站点、单类型、单通道，信息获取便利性不足；调研确认三家就业网系统（91job / 云就业 / 才立方）覆盖 1300+ 校且均为免登录 JSON 或服务端渲染页面，`httpx + lxml` 即可解析，可一站聚合宣讲会 / 双选会 / 岗位并提供变更提醒（宣讲会改期率约 25%） | 胡康宁 |
| v2.14 | 2026-09-26 | **数据库切换 MySQL + 题库体系升级**：①**数据库由 SQLite 改 MySQL 8.0**——风险清单「SQLite 并发写锁」一条替换为「数据库连接失效（MySQL `wait_timeout`）」（由 `pool_pre_ping` + `pool_recycle=3600` 兜住）；步骤 2 详情的查看/验收工具由 `sqlite3` 改 MySQL 客户端，并加注说明该步为历史记录（表数 15、题库 128 口径已随后续步骤变化）；②**步骤 13（八股陪练）详情扩写**——题库基建随步骤 2 就绪（585 题 / 5 个技术栈 / 18 个领域 / 152 道场景题）；新增 `GET /practice/meta`（下发栈、领域、题型清单与映射）、抽题接口按 `stacks` / `directions` / `qtypes` 筛选、点评按 `qtype` 分派（**场景题四维点评并下发 `dimensions`**）；用户可见成果与验收同步补场景题口径（四维分项 + 总分、展示参考框架而非标准答案）；前端任务补两步筛选与场景题差异化呈现。**动因**：题库扩至 585 题、新增场景题分型与多栈分类，陪练步骤的实现范围需相应展开 | 胡康宁 |
| v2.15 | 2026-09-26 | **步骤 9 落地后的口径修订**（步骤 9 后端已实现并实测）：步骤 9 详情、用户可见成果、验收三处原写「**错题**与宣讲会区块此步返回空占位」——实际**错题计数本步已真实落地**（顶层 `wrong_question_count` 查 `wrong_question` 表到期未掌握项、`stats.wrong_question_count` 查错题总数，均只读该表、不依赖步骤 13 的错题本功能），留空占位的仅**校招情报区块**（宣讲会 / 匹配岗位 / 订阅计数，待步骤 21~22）；三处表述同步改写，验收补「到期错题计入提醒数、已掌握的不计入」一条。**动因**：同节内原已写「个人中心的投递 / 错题 / 面试三张统计卡片同期显示真实计数」，与「错题区块留占位」自相矛盾；实现按前者落地。接口文档 v1.14 §3.4 第 ⑧ 点（区块判定口径）同批补充；**顺带对齐**：步骤 1 详情「测试」行原写的 `FakeLLMProvider` 占位改为中性表述——该替身与注入点实际于**步骤 10** 交付，命名落定 `get_llm_client`（测试计划 §1.3 的对应命名由测试侧对齐）；**新增指引**：步骤 23 前端任务行补「界面分离约定按系统设计 §4.8 执行」——问题记录 IS-29 / IS-30 的五条分离约定与空态口径已随系统设计 v1.17 落为 4.8 节，实现时以该节为准 | 胡康宁 |
| v2.16 | 2026-09-26 | **步骤 11 落地后的口径补充**（步骤 11 后端已实现并实测）：①步骤 11 后端任务行与总览表同步补**文件落点**（`app/utils/sse.py` 协议层 + `routers/stream.py` + `schemas/stream.py`）与实现口径（`SSE` 事件构造器 / `sse_response` 包装器 / 独立会话 / 异常分派）；②**演示端点由"临时"改为"保留"**——验收原写「写一个临时演示端点」，实际落为**常驻自检端点 `POST /stream/demo`**（前端打字机组件需联调靶子、测试侧协议单测需靶子、后续步骤可作回归标）；③产出与验收两处表述同步。**动因**：步骤 11 完成后按约定做「实现 ↔ 文档」逐条自查，接口文档 v1.17 / 系统设计 v1.18 / 项目结构 v1.33 同批联动 | 胡康宁 |

---

## 1. 总则

1. **步骤制，不设日期节点**：每步明确"做什么 → 产出 → 验收标准"，验收通过才进入下一步骤；开发周期预算 ≥2 个月，节奏自行把握，宁慢勿赶。
2. **阶段制 + 里程碑**：29 个步骤归入 5 个阶段，每阶段结束设里程碑验收点（见第 3 节），阶段产物固化后再进入下一阶段。
3. **文档先行**：动代码前确认对应文档（需求/设计/数据库/接口）已定稿；发现文档问题先改文档再改代码。
4. **测试随开发**：每步代码完成后跑对应测试用例（编号对应测试计划），全绿才验收。
5. **质量基线（每步验收时执行）**：①对应测试用例全绿；②接口文档状态标注同步更新；③手工验收有记录（页面截图/结果登记）；④代码走查一次（异常处理、边界条件、注释规范）；⑤**用户视角成果对照**——按该步「用户可见成果」段落确认：界面类成果须实际打开页面走一遍并留截图，纯后端步骤须给出可现场演示的验证方式（Swagger / Postman 调用）。
6. **前后端并行**：前后端以接口文档为协作契约并行开发；后端接口先行可用（前端可用 mock 数据联调）。
7. **范围控制**：不实现文档中未约定的功能；语音、表达力分析与练习模式为 P2 增强项（阶段四），不阻塞主线。

## 2. 团队分工

| 成员 | 职责 |
|---|---|
| 胡康宁 | 后端开发（含**账号体系与鉴权**）、数据库设计、AI 供应商接入 |
| 卢世君 | 前端开发（含**登录注册、个人中心、AI 配置页**）、测试用例编写与执行、**测试计划文档维护** |

**协作约定**：

- 接口文档是前后端唯一契约：接口变更必须先改接口文档再改代码，双方同步；
- 后端每完成一个模块，先自测通过（对应测试用例全绿）再通知前端联调；
- 前端手工验收清单（测试计划第 3 节）由卢世君按步骤执行并记录结果；
- git 协作：功能分支开发，合入主干前跑通全量测试；
- 每个里程碑验收点双方共同确认，通过后方可进入下一阶段。

## 3. 阶段划分与里程碑

| 阶段 | 步骤 | 主题 | 里程碑验收点 |
|---|---|---|---|
| 阶段一 | 1~9 | 工程基础与账号体系（无 AI 依赖） | **M1**：账号闭环 + 投递闭环 + 概览可用，pytest 全绿 |
| 阶段二 | 10~17 | AI 核心功能链路 | **M2**：JD 分析/陪练/错题/模拟面试/面经五条 AI 链路全通，真实供应商冒烟通过 |
| 阶段三 | 18~23 | 全局 Agent 与自动化、校招情报 | **M3**：悬浮球一句话操作 + 定时提醒 + **多源校招情报（宣讲会 / 双选会 / 岗位）聚合上线** |
| 阶段四 | 24~27 | 语音增强、表达力分析与练习模式、性能打磨 | **M4**：长时长语音交互可用、表达力指标可视、练习模式（含跨次对比）可用；NFR-001~003 延迟指标实测达标 |
| 阶段五 | 28~29 | 新手指引与交付 | **M5**：AC-1~AC-15 全过（含新手指引、练习模式与校招情报），代码+文档+演示预案可交付 |

## 4. 步骤总览

| 步骤 | 阶段 | 内容 | 用户可见成果 | 优先级 | 后端（胡康宁） | 前端（卢世君） | 主要验收用例 |
|---|---|---|---|---|---|---|---|
| 1 | 一 | 工程骨架 | 界面无变化（两端可启动） | P0 | 后端目录/统一响应体/异常处理 | Vite 工程/路由/布局 | pytest 骨架全绿 |
| 2 | 一 | 数据库落地 | 界面无变化（数据地基） | P0 | 15 表 ORM/枚举/建库 | — | TC-51 |
| 3 | 一 | 投递管理（后端） | 界面无变化（Swagger 可演示全流程） | P0 | CRUD/状态流转/批量导入/模板/趋势 | — | TC-01~04 |
| 4 | 一 | 投递管理（前端联调） | **投递看板可用**：录入→拖拽流转→导入→趋势图 | P0 | 联调支持 | 看板/表单/导入 | TC-26、TC-46 |
| 5 | 一 | **账号与鉴权（后端）** | 界面无变化（Swagger 可演示两账号互不可见） | P0 | `user` + `llm_provider_config` 表/`/auth` 7 接口/`/profile` 2 接口转账号级/鉴权依赖/存量接口挂鉴权/头像存储 | — | TC-52~54、TC-58~60 |
| 6 | 一 | **AI 供应商配置（后端）** | 界面无变化（Swagger 可演示多 Key 并存与切换） | P0 | `/llm-providers` 6 接口（表已在步骤 5 建好）/Fernet 加密/12 家注册表/`/settings` 2 接口瘦身 | — | TC-55、TC-56、TC-61（另 TC-47~49 端点迁移改写） |
| 7 | 一 | **账号体系（前端）** | **入场动画 → 登录/注册 → 右上角用户菜单 → 个人中心** | P0 | 联调支持（数据概览的 `stats` 见步骤 9） | 动画页/登录注册/个人中心/用户菜单/路由守卫 | 手工清单 |
| 8 | 一 | **AI 配置页 + 设置页瘦身（前端）** | **12 家供应商卡片：填 Key → 选模型 → 测连通 → 切换即生效** | P0 | 联调支持（后端接口见步骤 6） | AI 配置页/设置页改版 | 手工清单 |
| 9 | 一 | 今日概览 | **首页出统计卡片与逾期跟进提醒** | P0 | overview 聚合（含 `stats` 计数） | 概览页 | TC-27 |
| 10 | 二 | LLM 客户端基建 | 界面无变化（配 Key 可冒烟验证） | P0 | llm_client 全能力/两级注册表 | — | TC-50 |
| 11 | 二 | SSE 流式基建 | 界面无变化（AI"逐字输出"打底） | P0 | SSE 协议层（start/delta/tool_call/done/error）+ 自检端点 | sse.js/StreamText 组件 | SSE 协议单测 |
| 12 | 二 | JD 匹配分析 | **粘 JD → 报告逐字流式生成 → 历史可回看** | P0 | 五段 Prompt/流式端点/报告落库 | JD 分析页 | TC-20、TC-32、冒烟 5.1 |
| 13 | 二 | 八股陪练 | **抽题 → 作答 → AI 流式点评** | P0 | 抽题/点评/历史 | 陪练页 | TC-09、TC-10、TC-44、TC-34 |
| 14 | 二 | 错题本 | **错题自动入本，按 1/3/7/15 天安排复习** | P0 | 自动入本/档位推进/复习判定/知识点入本 | 错题本页 | TC-11~13、TC-45 |
| 15 | 二 | 模拟面试（会话与问答） | **聊天式模拟面试，逐题点评，刷新不丢** | P0 | 会话 CRUD/流式问答/跳过 | 面试列表+进行页 | TC-05~07、TC-33 |
| 16 | 二 | 模拟面试（总结与回看） | **面试总结报告 + 历史会话回看** | P0 | 总结流式端点/历史回看 | 总结页+详情页 | TC-08、TC-37 |
| 17 | 二 | 面经整理 | **粘面经 → 自动拆成结构化条目 → 检索** | P1 | 面经 CRUD/结构化提取/检索 | 面经页 | TC-14、TC-15 |
| 18 | 三 | 全局 Agent（后端） | 界面无变化（Agent 后端能力就绪） | P0 | 意图路由/工具注册表/Function Calling/画像注入 | — | TC-16~19 |
| 19 | 三 | 全局 Agent（前端） | **任意页面悬浮球一句话办事（确认卡片）** | P0 | 确认卡片执行接口 | 悬浮球/浮窗/chips/乐观更新 | TC-35、TC-36、TC-39 |
| 20 | 三 | 定时引擎 + 提醒 | **概览三类提醒（跟进/错题/面试）自动出现** | P0 | 每日任务/提醒列表/手动触发 | 概览接入提醒 | TC-21、TC-22、TC-30 |
| 21 | 三 | **多源采集层** | **多源宣讲会 / 双选会聚合上线，改期自动打「已变更」角标（失败保留上次）** | P1 | 适配器框架/三厂商解析器/源配置接口/合规组件/处理层（去重·变更·归档）/`campus_event` 扩展与 `/campus-events` 参数扩展 | 概览宣讲会区块改造 | TC-23（扩展）、测试侧待分配 |
| 22 | 三 | **校招情报（后端）** | 界面无变化（Swagger 可演示投喂→预览→入库、订阅命中写提醒） | P1 | `job_posting` 表/岗位接口/投喂通道/匹配打分/订阅规则与推送/`/calendar` 聚合接口 | — | 测试侧待分配 |
| 23 | 三 | **校招情报（前端）** | **校招情报页：三类信息 Tab + 月历视图 + 我的订阅 + 卡片一键加入投递** | P1 | 联调支持 | 校招情报页/日历视图/订阅与源管理/概览区块改造/投递与 JD 分析打通 | 手工清单 |
| 24 | 四 | 语音交互 | **模拟面试可长时长语音作答 + 鲜活音色播报** | P2 | FunASR 本地接入/edge-tts 合成/VAD 断句/三个语音接口 | VoiceInput/语音播报 | TC-31、TC-41、冒烟 5.2 |
| 25 | 四 | 表达力分析（P2） | **语音作答后即时看到语速 / 填充词 / 停顿等表达指标** | P2 | `speech_metrics` 纯函数/指标落库/`done` 事件下发 | 指标卡片展示 | TC-71、TC-72 |
| 26 | 四 | 练习模式（P2） | **同一道题反复练：逐次点评 + 跨次进步对比** | P2 | `drill` 两表/7 个接口/进步对比计算 | 我的题目列表 + 题目详情页 | TC-69、TC-70 |
| 27 | 四 | 性能验证与体验打磨 | **全站体验收口，延迟指标实测达标** | P0 | 延迟优化/兜底完善 | 交互细节打磨 | NFR-001~003 实测 |
| 28 | 五 | 新手指引（P2） | **新账号首进首页自动分步高亮 5 个功能入口，可跳过** | P2 | 复用 `/settings` 承载 `guide_done` | GuideTour（driver.js）/个人中心重看入口 | TC-68 |
| 29 | 五 | 整体验收 + 演示准备 | **完整系统可交付、演示脚本可预演** | P0 | 共同 | 共同 | AC-1~AC-15 |

## 5. 各步骤详情

### 步骤 1：工程骨架

- **后端（胡康宁）**：按项目结构文档建目录与依赖；config.py；main.py（lifespan 预留建库/导种子/定时器注册）；health 路由 + 统一响应体 + exceptions.py（全量错误码注册）
- **前端（卢世君）**：Vite 工程 + 路由表 + 侧边导航布局 + request.js（axios 拦截器）+ Element Plus 接入
- **测试（卢世君）**：conftest.py（独立测试库 + LLM 对话替身与依赖注入点，替身实际于**步骤 10** 交付）

**产出**：两端可启动，`/health` 通，统一响应体与异常处理就位。

**用户可见成果**：**界面无变化**（工程打底）。前端 9 个页面已可点击导航（均为占位内容），后端 `/health` 返回统一响应体——架子搭起来了，业务功能从下一步开始长。

**验收**：后端 uvicorn 启动无报错；前端 9 个路由可导航（页面占位）；pytest 骨架通过；统一响应体四态手测正常。

### 步骤 2：数据库落地

- **后端（胡康宁）**：15 张表 ORM 模型（口径=数据库设计文档第 5 章枚举表）+ enums.py；database.py（WAL+外键开启）；建库脚本；种子题库 questions.json（Java/MySQL/网络/OS 各 ≥30 题）
- **测试（卢世君）**：建表冒烟——启动后校验 15 表存在、字段与索引一致、枚举默认值正确

**产出**：空库一键初始化，种子题库入库。

**用户可见成果**：**界面无变化**（数据地基）。15 张表一次建好，128 道八股题（Java / MySQL / 网络 / OS 四方向）随首次启动自动入库，为后续陪练抽题、错题本备好题库；成果可用 MySQL 客户端（`mysql` 命令行 / Navicat / Workbench）直接查看。

**验收**：MySQL 客户端查 15 张表存在且字段符合设计文档；question 表种子数据 ≥120 题。

> 本步为历史记录，记录当时的表数（15）与题库量（128）口径——表数随后续步骤陆续增至 22 张，题库于 2026-09-26 升级至 **585 题**（见步骤 13）。

### 步骤 3：投递管理（后端）

- **后端（胡康宁）**：application 全部接口（CRUD/状态流转/批量导入/模板下载/趋势统计）；导入解析（xlsx/csv 行级错误不中断）
- **测试（卢世君）**：TC-01~04（服务层单元测试，FakeLLM 无关）

**产出**：投递数据接口层完整，可直接用 Postman 验证。

**用户可见成果**：**界面无变化**（纯后端）。打开 Swagger 页（`/docs`）即可现场演示完整投递管理：新增投递 → 状态流转（非法流转会被拒绝并提示）→ 下载模板→填写→批量导入（错误行逐行说明原因）→ 趋势统计；可视化的看板界面在步骤 4 呈现。

**验收**：TC-01~04 全绿；手工用接口文档逐接口核对请求/响应。

### 步骤 4：投递管理（前端联调）

- **前端（卢世君）**：投递看板（Kanban 拖拽流转）+ 新增/编辑表单 + 批量导入页（模板下载/错误行展示）+ 趋势图（ECharts）
- **后端（胡康宁）**：联调支持与接口修正（如需改接口先改文档）
- **测试（卢世君）**：TC-26（CRUD 全流程）、TC-46（趋势）

**产出**：投递数据闭环可用。

**用户可见成果**：**投递管理正式可用了**——进入投递页：新增 / 编辑投递记录，看板列间拖拽流转状态，下载 Excel 模板→填写→上传导入（错误行列表展示），趋势图查看投递节奏。**这是系统第一个能真正上手操作的页面**。

**验收**：TC-26、TC-46 全绿；手工：新增→看板流转→趋势图计数一致。

### 步骤 5：账号与鉴权（后端）

- **后端（胡康宁）**：**新增 `user` + `llm_provider_config` 两张表（表数 15 → 17）**、账号私有表补 `user_id`（**8 张业务表 + `config` + `user_profile`，共 10 张**；清单与约束变更见[数据库设计](../03-数据库/数据库设计文档.md) §3 / §7，子表经父表间接隔离、公共表不加）、`user_profile` 改每账号一条；**`/auth` 7 接口**（POST 注册 / POST 登录 / GET 当前账号 / PUT 改资料 / PUT 改密码 / POST 头像上传 / DELETE 恢复默认头像）；**`/profile` 2 接口（GET / PUT 求职画像）由全局单份改为按账号隔离**——请求体不接受 `user_id`，归属由登录态决定；鉴权依赖 `get_current_user`（JWT 签发与校验、白名单三端点）；**存量接口全部挂鉴权 + 服务层强制 `user_id` 过滤**；头像文件存储（类型体积校验、旧文件清理）；登录失败锁定。**注**：`llm_provider_config` 表随本步一并建好（`GET /auth/me` 的 `llm_configured` 字段需读该表），其配置接口在步骤 6 落地
- **测试（卢世君）**：TC-52~54（注册 / 登录与失败锁定 / 免登录有效期与改密作废旧 Token）、TC-58~60（鉴权拦截与白名单豁免 / 跨账号越权 404 / 头像上传）
- **本步为存量改造**：步骤 1~4 已落地的接口全部加鉴权与账号过滤；开发库业务数据为空，直接重建库

**产出**：多账号地基完成——注册登录可用、全部接口受保护、账号间数据互不可见。

**用户可见成果**：**界面无变化**（纯后端）。Swagger 上可现场演示：注册 A、B 两个账号 → 各自登录拿 Token → A 创建的投递，B 用自己的 Token 去查返回"资源不存在"（与不存在完全同款响应，不暴露存在性）；不带 Token 访问任意接口返回 401。

**验收**：新增用例全绿；既有用例（81 条）改造后仍全绿；手工：两账号交叉访问 + 越权返回 404 核对。

### 步骤 6：AI 供应商配置（后端）

- **后端（胡康宁）**：`llm_provider_config` 表**已在步骤 5 建好**（每账号 × 每供应商一行、Key 以 Fernet 加密、`is_active` 激活位互斥），本步落地其读写；**`/llm-providers` 6 接口**（GET 列表 / PUT 保存 / DELETE 删除 / POST 设为当前使用 / GET 模型列表 / POST 连通性测试）；**12 家供应商注册表**（国内 5 + 国外 4 + 聚合 + 本地 + 自定义）；**`/settings` 2 接口（GET / PUT）瘦身**——保留账号级偏好（`tts_enabled` / `voice_enabled` / `default_question_count` / `asr_provider` / `tts_voice` / `guide_done`）与系统级抓取配置（`crawl_enabled` / `crawl_url`），**LLM 字段全部迁出**（`llm_provider` / `llm_model` / `llm_base_url` / `llm_key_set`）；原 `POST /settings/llm-test`、`GET /settings/llm-models` 迁移至本组（`POST /llm-providers/test`、`GET /llm-providers/{provider}/models`）
- **测试（卢世君）**：TC-55、TC-56、TC-61（配置增删改与 Key 密文落库 / 激活互斥 / 多账号配置隔离）；原 TC-47~49（连通性测试 / 两级切换 / 模型列表缓存与回退）随端点迁至本组后改写

**产出**：AI 配置接口层完整，多供应商 Key 并存与一键切换可用。

**用户可见成果**：**界面无变化**（纯后端）。Swagger 上可演示：给 DeepSeek 与智谱各存一份 Key → 切换当前使用 → 拉取模型列表（24h 缓存，断网回退内置表）→ 连通性测试当场返回结果；数据库里 `api_key` 是密文，接口只回 `key_set: true`。

**验收**：新增用例全绿；手工：两账号各自配置不同供应商互不干扰；断网验证内置模型表回退。

### 步骤 7：账号体系（前端）

- **前端（卢世君）**：入场动画页 `/welcome`（约 1.8 秒，含末段 0.4s 淡出、可点击跳过、会话内只播一次）；登录页（含"30 天免登录"勾选）；注册页（密码强度提示不拦截提交）；个人中心 `/profile`（账号信息（`GET /auth/me`） / 头像上传（`POST`·`DELETE /auth/avatar`） / 修改密码（`PUT /auth/password`） / 画像表单（`GET/PUT /profile`） / **数据概览卡片**（投递数 / 错题数 / 面试场次，取 `GET /overview` 的 `stats` 字段——该字段随**步骤 9** 落地，未返回时显示占位符））；顶栏用户菜单（头像 + 用户名 → 个人中心 / 设置 / AI 配置 / 退出登录）；`useUserStore`（Token 持久化 + 账号切换 reset）；路由守卫 + axios 拦截器（401 → 80001/80002 全局登出）
- **后端（胡康宁）**：联调支持与接口修正（如需改接口先改文档）；**本步不新增接口**——个人中心数据概览卡片所依赖的 `stats` 字段由步骤 9 的 `GET /overview` 提供
- **测试（卢世君）**：手工验收 TC-62~66（入场动画与登录态预检 / 登录注册与免登录 / 未登录拦截与全局登出 / 个人中心 / 账号切换无数据残留）

**产出**：登录注册与个人中心可用，全站进入"必须先登录"形态。

**用户可见成果**：**系统外观与入口变了**——打开先播约 1.8 秒品牌入场动画（可点击跳过），随后进登录页；注册即自动登录进首页；右上角出现"头像 + 用户名"，点开是个人中心 / 设置 / AI 配置 / 退出登录；个人中心可上传头像（自动压成 256×256）、改昵称邮箱、改密码、维护求职画像。**这是本轮改造后用户第一眼看到的变化**。

**验收**：手工清单全过——含"勾选 30 天免登录 → 关浏览器重开仍在线"、"改密后旧登录态立即失效"、"未登录访问业务页被重定向到登录页"。

### 步骤 8：AI 配置页 + 设置页瘦身（前端）

- **前端（卢世君）**：AI 配置页 `/ai-config`（12 张供应商卡片：状态标签 / Key 输入 / base_url 高级设置 / 模型下拉 / 保存·测试·设为当前使用·删除，对接步骤 6 的 `/llm-providers` 6 接口）；设置页 `/settings` 改版（账号级偏好 + 系统级抓取开关，**LLM 相关字段全部移除**，对接步骤 6 的 `/settings` 2 接口）
- **后端（胡康宁）**：联调支持（本步**无新增接口**——两页对接的接口均已在步骤 6 落地）
- **测试（卢世君）**：手工验收 TC-67（配置保存与切换即生效 / 删除当前激活项的二次确认 / 模型列表刷新）

**产出**：AI 配置页可用，设置页与账号体系对齐。

**用户可见成果**：**"配好就能用 AI"的入口独立成页了**——AI 配置页列出 12 家供应商卡片，每家可分别填 Key、选模型、测连通、设为当前使用；保存即生效，切换供应商不用重填 Key；删除配置有二次确认，删除当前生效项会提示"AI 功能将不可用"。设置页只剩账号级偏好与系统级抓取开关。

**验收**：手工清单全过（含两家供应商来回切换、删除当前激活项的提示）；设置页不再出现任何 LLM 字段。

### 步骤 9：今日概览

- **后端（胡康宁）**：**`GET /overview` 聚合接口**（纯 SQL 规则；校招情报区块——宣讲会 / 匹配岗位 / 订阅计数——此步返回空占位，**错题计数按 `wrong_question` 表实查返回**）——响应含 **`stats` 对象**（`application_count` / `wrong_question_count` / `interview_count`），供**个人中心数据概览卡片**（步骤 7 前端已按此口径实现、此前显示占位符）
- **前端（卢世君）**：概览页（统计卡片/跟进列表/置顶排序）；个人中心数据概览卡片接上真实计数
- **测试（卢世君）**：TC-27

**产出**：今日概览页展示统计与跟进提醒（AI 相关区块留占位）；个人中心数据概览卡片数据到位。

**用户可见成果**：**打开系统第一眼就有用**——首页展示投递统计卡片与逾期跟进列表（超 3 天无进展的自动置顶），每天扫一眼就知道该跟进哪一家；校招情报区块（宣讲会 / 匹配岗位 / 订阅计数）先留占位，待步骤 21~22 填充；个人中心的投递 / 错题 / 面试三张统计卡片同期显示真实计数。

**验收**：TC-27 全绿；手工：构造 >3 天无进展数据，概览正确置顶展示；到期错题计入提醒数、已掌握的不计入；个人中心数据概览卡片显示真实计数（不再为占位符）。

**🎯 里程碑 M1**：账号闭环 + 投递闭环 + 概览可用，pytest 全绿。

### 步骤 10：LLM 客户端基建

- **后端（胡康宁）**：`llm_client.py` 对话能力——`stream_chat`（流式）/ `chat_json`（非流式 JSON）；超时（连接 5s / 首 token 8s）与重试（网络错 1 次）；LLMError 映射；**复用步骤 6 已落地的 `llm_provider.py`**（两级注册表、`test_connection`、`list_models` 动态拉取 + 24h 缓存 + 内置表回退均已交付）
- **测试（卢世君）**：TC-50（FakeLLM 单元测试：各方法行为/缓存与回退/超时重试/异常映射）

**产出**：AI 调用地基完成，后续所有 AI 功能复用 `llm_client` + `llm_provider` 两个模块。

**用户可见成果**：**界面无变化**（AI 调用地基）。配置真实 Key 后可跑通一次对话冒烟（连通性测试已在步骤 6 交付）；后续所有 AI 功能的流式输出、超时重试、多供应商与模型切换，全部复用这一层，不重复造轮子。

**验收**：TC-50 全绿；`.env` 配真实 Key 后对话链路冒烟成功（连通性测试已在步骤 6 验收）。

### 步骤 11：SSE 流式基建

- **后端（胡康宁）**：SSE 协议层（`app/utils/sse.py`——事件 start/delta/tool_call/done/error 统一封装为 `SSE` 事件构造器 + `sse_response` 响应包装器；start 事件进入生成器后立即推送、先于任何 AI 调用；每个流式请求独立数据库会话；客户端断连安全终止）；统一流式异常处理（`BizException` → 其 code，其余 → 10000，中途失败发 error，已渲染内容保留）；协议自检端点 `POST /stream/demo` + `schemas/stream.py`
- **前端（卢世君）**：sse.js 封装（fetch+ReadableStream 解析）+ StreamText 打字机组件
- **测试（卢世君）**：SSE 协议单测（事件顺序/断连/异常路径，FakeLLM delay 模拟流式节奏）

**产出**：流式链路公共基建完成，后续四条 SSE 功能链路全部复用；自检端点同时作为前端联调靶子与测试协议单测靶子。

**用户可见成果**：**界面无变化**（流式基建）。它决定的是"AI 回答逐字蹦出来"而不是转圈干等——这份手感要到步骤 12 才看得见，但此后四条 AI 链路（JD / 陪练 / 面试 / 面经）全都复用这一层。

**验收**：协议单测全绿；手工：以自检端点 `POST /stream/demo` 为靶子，前端打字机渲染流畅。

### 步骤 12：JD 匹配分析

- **后端（胡康宁）**：prompts.py 首批模板（JD 五段结构）；/stream/jd-analysis 端点；报告落库与列表/详情
- **前端（卢世君）**：JD 分析页（粘贴 JD→流式报告→历史列表/详情）
- **测试（卢世君）**：TC-20、TC-32

**产出**：第一条完整 SSE 功能链路（JD 五段报告）。

**用户可见成果**：**第一条完整 AI 链路跑通**——粘贴一份真实 JD，报告以"逐字打字"的方式流式生成（首字 2 秒内出现），分段给出匹配分析；分析完自动落库，历史报告随时回看。

**验收**：用例全绿；真实供应商冒烟（首字 <2s）；打字机渲染流畅。

### 步骤 13：八股陪练

- **后端（胡康宁）**：**题库基建已就绪**（步骤 2 建表时已含 `stack` / `rubric` 两列，种子 **585 题**覆盖 5 个技术栈 / 18 个领域 / 含 152 道场景题）；本步实现——**`GET /practice/meta`**（下发技术栈、领域、题型清单与 `stack → direction` 映射，供前端两步筛选）、**抽题接口**（按 `stacks` / `directions` / `qtypes` 筛选、不含答案、数量上限）、**`/stream/practice-review` 点评**（按 `qtype` 分派：`CHOICE` 规则判定不调 LLM、`SUBJECTIVE` 普通点评、**`SCENARIO` 四维点评并下发 `dimensions`**）；评分 <6 自动入错题本（含重复入本边界：更新档位不重复插入）；陪练历史
- **前端（卢世君）**：陪练页——**两步筛选（先选技术栈 → 再选领域）+ 题型入口**；题目卡片 / 作答 / 点评卡片；**场景题差异化呈现**（不显示「标准答案」而显示「参考框架」，点评按四维度分栏 + 总分）
- **测试（卢世君）**：TC-09、TC-10、TC-44、TC-34（另需补场景题四维评分与多栈筛选用例）

**产出**：陪练闭环（抽题 → 作答 → 点评），题库覆盖 5 个技术栈、题型含场景题。

**用户可见成果**：**笔试备战可用了**——先选岗位栈再选领域抽题（题目不含答案）→ 作答提交 → AI 流式点评并打分；**场景题按解题框架、量化估算、取舍权衡、兜底降级四个维度分别点评**，与"标准答案"区分呈现；**评分低于 6 分自动进错题本**，为下一步的错题复习埋好钩子。

**验收**：用例全绿；手工：分别抽一道主观题与一道场景题作答 → 流式点评 → 主观题出「评分 + 参考答案差距点」、**场景题出四维分项 + 总分**，评分正确落库。

### 步骤 14：错题本

- **后端（胡康宁）**：错题列表/删除；复习判定（客观题标准答案比对不调 LLM）；档位推进（1/3/7/15 天，答错回档）；面试知识点入本（两种形态，后者自动建题）
- **前端（卢世君）**：错题本页（列表/到期标记/AI 抽问卡片）
- **测试（卢世君）**：TC-11~13、TC-45

**产出**：笔试备战闭环（陪练→点评→入本→抽问→掌握）。

**用户可见成果**：**错题本自动积累起来了**——列表展示错题并标注哪些已到期，可让 AI 现场抽问；系统按 1 / 3 / 7 / 15 天的节奏推进复习档位（答错回档重来），客观题直接比对标准答案、不消耗 AI 调用。

**验收**：用例全绿；手工：答错一题→错题本出现→到期提醒数变化。

### 步骤 15：模拟面试（会话与问答）

- **后端（胡康宁）**：会话 CRUD（带 application_id 自动带入公司岗位）；/stream/interview-chat（开场/作答/点评/下一题/跳过）；中途刷新可恢复
- **前端（卢世君）**：面试列表页 + 进行页（聊天式界面+逐题点评卡片）
- **测试（卢世君）**：TC-05~07、TC-33

**产出**：面试问答闭环，会话可恢复。

**用户可见成果**：**可以像模像样地面一场了**——从投递记录一键发起面试（自动带入公司岗位），聊天式逐题作答、每题即时点评；**中途刷新页面进度不丢**，回来接着答。

**验收**：用例全绿；手工：完整 8 题面试中途刷新可恢复。

### 步骤 16：模拟面试（总结与回看）

- **后端（胡康宁）**：/stream/interview-summary（会话置 FINISHED/重复调用不重复生成）；面试知识点入错题本联动（复用步骤 14 服务）
- **前端（卢世君）**：总结页 + 会话详情回看页
- **测试（卢世君）**：TC-08、TC-37

**产出**：完整面试闭环（开场→问答→总结→回看）。

**用户可见成果**：**面试闭环完整了**——一场面试结束生成总结报告（亮点与待改进点），历史会话可随时回看；面试中暴露的知识盲区自动进错题本，形成"面试 → 发现问题 → 复习 → 再面"的循环。

**验收**：用例全绿；手工：面试结束→总结报告→错题本出现面试知识点。

### 步骤 17：面经整理（P1）

- **后端（胡康宁）**：面经保存/详情/删除；/stream/experience-extract 结构化提取（提取失败原文保留）；条目关键词检索
- **前端（卢世君）**：面经页（列表/详情/检索）
- **测试（卢世君）**：TC-14、TC-15

**产出**：面经库可用（原文+结构化+检索）。

**用户可见成果**：**面经不再是躺在备忘录里的乱文本**——粘贴一篇面经原文，AI 自动拆成结构化条目（公司 / 岗位 / 轮次 / 具体问题等）存进面经库，关键词一搜就命中；提取失败时原文照常保留，不丢内容。

**验收**：用例全绿；手工：粘贴一篇面经→提取条目→检索命中。

**🎯 里程碑 M2**：五条 AI 链路全通（JD/陪练/错题/面试/面经），真实供应商冒烟全部通过。

### 步骤 18：全局 Agent（后端）

- **后端（胡康宁）**：意图路由（四类意图）+ 工具注册表 + Function Calling（stream=True + tool_calls 增量）+ 画像注入 + 历史消息上下文
- **测试（卢世君）**：TC-16~19（意图样例路由/工具调用/执行/失败映射）

**产出**：Agent 后端能力完整，可通过接口直接对话。

**用户可见成果**：**界面无变化**（Agent 后端能力就绪）。用 Postman 发一句"记一笔投递"即可看到完整的意图识别 → 工具调用 → 结果返回事件序列；悬浮球界面在步骤 19 呈现。

**验收**：用例全绿；手工：用 Postman 发"记一笔投递"验证 tool_call 事件序列。

### 步骤 19：全局 Agent（前端）

- **后端（胡康宁）**：/agent/tools/{tool}/execute 确认卡片执行接口
- **前端（卢世君）**：AgentFloatBall 悬浮球（浮窗+chips 快捷指令+确认卡片渲染+乐观更新+失败红色重试）
- **测试（卢世君）**：TC-35、TC-36、TC-39

**产出**：任意页面可用 AI 对话并执行真实动作。

**用户可见成果**：**任意页面都能"说一句话办事"**——右下角悬浮球随时唤起，说"记一笔投递"→ 弹出确认卡片（AI 不直接写库）→ 确认后看板真的多出一条；浮窗内置快捷指令 chips，写操作失败标红可重试。**这是整个项目最有辨识度的功能**。

**验收**：用例全绿；手工："记一笔投递"→卡片→确认→看板出现。

### 步骤 20：定时引擎 + 提醒

- **后端（胡康宁）**：reminder_engine 每日任务（纯规则判定：3 天无进展/错题到期/7 日面试）；提醒列表/已读；/reminders/run 手动触发；文案 LLM 失败回退模板
- **前端（卢世君）**：概览页接入错题提醒与面试提醒区块
- **测试（卢世君）**：TC-21、TC-22、TC-30

**产出**：概览信息完整（投递/面试/错题提醒全区块）。

**用户可见成果**：**概览页信息补齐了**——跟进提醒（超 3 天无进展）/ 错题到期 / 近 7 日面试三类提醒到点自动出现，不用自己记；AI 文案生成失败时回退固定模板，提醒照常送达。

**验收**：用例全绿；手工：造 3 天前投递数据→手动触发→概览出现跟进提醒。

### 步骤 21：多源采集层（P1）

- **后端（胡康宁）**：`app/clients/crawler/` 落地——**适配器框架**（`fetch(source) -> list[RawItem]` 协议，"一厂商一解析器 + 一校一配置"：91job 免登录 JSON / 云就业 SSR / 才立方 SSR）+ **源配置**（`crawl_source` 表 + `/crawl-sources` 5 接口，新增学校 = 加一行配置不写代码）+ **合规组件**（UA 标识 / robots 检查 / 同域 ≥2 秒限频 / 每源每日 1 次 / 遇 403 停止不重试 / 只存元数据，NFR-016）+ **处理层纯函数**（归一化 / 去重指纹 / `content_hash` 变更检测 / 过期归档，源 `last_status != OK` 时不归档）+ `campus_event` 扩 9 字段、约束改 `UNIQUE(dedup_key)`、`/campus-events` 参数扩展；每日任务「校招信息采集」全站一次、单源隔离；系统级 `config` **移除 `crawl_url`**（改由源清单承载）
- **前端（卢世君）**：概览宣讲会区块改造（补「已变更」角标与来源站点标签；源管理界面在步骤 23）
- **测试（卢世君）**：TC-23（多源口径扩展）；**本地 HTML/JSON 快照 fixture 离线解析断言**（不联网）、处理层纯函数单测（去重 / 变更 / 归档 / 打分）、合规断言（UA / 限频 / robots 跳过 / 不存正文）——新增用例编号由测试侧在测试计划登记

**产出**：多源采集层与处理层就绪——宣讲会 / 双选会跨校汇聚、跨源去重、变更可测、单源失败隔离。

**用户可见成果**：**宣讲会不再只有本校**——每日自动同步多所高校就业网公开的宣讲会与双选会，同一场活动跨源只显示一条；活动改期后卡片打「已变更」角标；某个源失效只标记该源，其余源数据照常，抓取失败时保留上次数据不报错、不空窗。

**验收**：TC-23 全绿；手工：填入不可访问的域名触发抓取 → 仅该源标记失败，其余源数据与提醒不受影响；同一活动两个来源同时出现 → 只展示一条。

### 步骤 22：校招情报（后端）（P1）

- **后端（胡康宁）**：`job_posting` 表落地（**混合表**：公共自动抓取 `user_id=0` / 账号投喂私有，查询限 `user_id IN (0, 当前账号)`）；岗位接口（列表 / 确认入库 / 删除，自动抓取项不可删）；**投喂通道**——`POST /job-postings/ingest` 文本走 `chat_json` 抽取 → 返回字段预览（**不入库**）→ 确认后入库；链接走合规组件抓公开正文，遇登录墙 / 403 / 验证码 → `70003` 降级为手动填表；**匹配打分**纯函数（城市 +30 / 方向 +40 / 专业 +30，字段缺失不加不减、不做百分比折算）；**订阅规则**（`subscription` 表 + 4 接口，维度间 AND、维度内 OR，匹配范围限"公共 + 本账号投喂"）+ **订阅推送联动**（新入库 / 变更 → 逐账号过启用规则 → 写 `reminder`：`reminder_type=INFO_MATCH` + `ref_type`，模板文案不调 LLM，去重靠现有唯一约束）；`GET /calendar` 四类事件区间聚合（跨月 / 空区间返回空数组）；`GET /overview` 补 `top_job_postings` 与 `match_reminder_count`
- **前端（卢世君）**：—
- **测试（卢世君）**：投喂三条路径（成功 / 抽取失败 / 链接被拒）LLM 全 mock；匹配打分纯函数（命中 / 缺失 / 全空）；订阅命中与否、多规则并集、同信息同日不重复提醒；**跨账号隔离**（投喂岗位换账号不可见、A 的订阅不得命中 B 的投喂记录）；日历区间边界——新增用例编号由测试侧在测试计划登记

**产出**：岗位信息与订阅推送链路就绪，站内日历数据一次取全。

**用户可见成果**：界面无变化（Swagger 可演示：粘贴一段 JD → 字段预览不入库 → 确认后入库；建订阅规则后新信息入库即写提醒）。

**验收**：用例全绿；手工：投喂一条 JD 走完预览→入库，换账号查不到该记录；同一信息重复触发不产生重复提醒。

### 步骤 23：校招情报（前端）（P1）

- **后端（胡康宁）**：联调支持（接口已在步骤 21 / 22 交付；`crawl_url` 的移除随步骤 21 完成）
- **前端（卢世君）**：侧边导航新增「**校招情报**」（路由 `/campus`）——三类信息 Tab（宣讲会 / 双选会 / 岗位）+ 列表 / 日历视图切换；筛选（城市 / 公司 / 类型 / 来源站点 / 时间区间含"只看未过期"）与排序（时间 / 匹配度）；信息卡片（来源站点标签、「已变更」角标、匹配命中项）与操作（查看原文 / 加入投递 / 分析匹配度）；**站内日历**月视图（四类事件 + 点日期展开当日 + 点事件跳详情）；**我的订阅**管理；**设置页源管理区块**（源清单增删改 + 手动触发抓取 + 逐源结果提示）；概览区块改造（近期宣讲会 / 双选会 + 匹配度最高岗位 + 命中订阅计数）；**界面分离约定按系统设计 §4.8 执行**（招聘信息的家只在校招情报页、与投递管理视觉语言分离、已投递岗位沉底或弱化、概览页只放入口不展开；空态判定以「是否真的什么都看不到」为准）
- **测试（卢世君）**：手工清单（AC-14 / AC-15 界面项逐条走查）

**产出**：校招情报页 + 站内日历 + 订阅与源管理界面可用，业务打通（加入投递 / 转 JD 分析）。

**用户可见成果**：**多校宣讲会、双选会、岗位一页看全**——按城市 / 公司 / 类型筛选，切「匹配度」排序把画像对口的排前面，改期条目一眼可见；月历里宣讲会 / 双选会与自己的笔试 / 面试同屏展示；看到合适的岗位一点「加入投递」，表单已预填公司 / 岗位 / 渠道 / 链接，或直接跳 JD 匹配分析。

**验收**：AC-14 / AC-15 界面项全过；手工：投喂一条 JD → 确认入库 → 岗位 Tab 可见并一键加入投递（表单预填正确）。

**🎯 里程碑 M3**：悬浮球一句话操作 + 定时提醒 + 多源校招情报（宣讲会 / 双选会 / 岗位）聚合上线。

### 步骤 24：语音交互（P2，不阻塞主线）

- **后端（胡康宁）**：`clients/asr_client.py`（ASR 供应方抽象 + **FunASR 本地流式**默认实现 + 讯飞备选）+ `clients/tts_client.py`（edge-tts 合成 + 按 `sha1(text+voice)` 文件缓存）；三个接口——`POST /asr/transcribe`（单片段转写）、`POST /tts/synthesize`（音频流）、`GET /tts/voices`（音色列表）；**`/settings` 新增 `asr_provider` / `tts_voice` 两个账号级键**（讯飞备选凭据 `asr_app_id` / `asr_api_key` 同节维护、传值以 Fernet 加密，见接口文档 §3.12）
- **前端（卢世君）**：VoiceInput 组件（MediaRecorder 录音 + **VAD 静音自动切句，阈值按语速自适应：快 1.6s / 正常 2.65s / 慢 3.6s** + 逐句上传累积回填 + 可编辑后提交 + **记录每句起止毫秒供提交时上传 `segments`**）+ 播报（`<audio>` 播放后端合成音频、可随时打断，默认关）
- **测试（卢世君）**：TC-31、TC-41（asr / tts 客户端**全 mock**，测试不加载本地模型、不调 edge-tts，离线可跑）

**产出**：模拟面试支持长时长语音作答与鲜活音色播报。

**用户可见成果**：**模拟面试可以"说"着答**——按下开始后连续说话，停顿 1 秒多自动断句转写（目标 1.5 秒内出字）并累积到作答框，可改字后提交；整场不限轮次与总时长。AI 点评用 edge-tts 中文音色播报（晓晓 / 云希 / 云扬可选，默认关闭）。属 P2 增强项，不阻塞主线。

**验收**：用例全绿；**真实语音冒烟**（连续说 3 分钟以上、断句转写准确、切换音色即时生效）；**断网状态可完整走一轮语音作答**（本地模型 + TTS 缓存）。

### 步骤 25：表达力分析（P2，不阻塞主线）

- **后端（胡康宁）**：`utils/speech_metrics.py` **纯函数模块**（输入分句时间轴 `segments` → 输出五项指标 + 基础量，零外部依赖、不触发 LLM）；`interview_qa.voice_metrics` 落库；`POST /stream/interview-chat` 接收 `segments`、`done` 事件下发 `voice_metrics`；`quality` 标记（`TOO_SHORT` / `TEXT_ONLY` 不产出表达维度）
- **前端（卢世君）**：提交作答时随作答上传 `segments`（步骤 24 已记录每句起止毫秒）；作答区**指标卡片**（语速 / 填充词 / 停顿点 / 有效时长占比 / 流畅度趋势），指标先于点评展示
- **测试（卢世君）**：`speech_metrics` 纯函数单测（正常样例 + 边界：空数组 / 单句 / 全静音 / 作答不足 10 秒）；接口侧 `segments` 解析用例（**TC-71 / TC-72**）

**产出**：语音作答在转写文本之外，额外产出可量化、可回看的表达维度数据。

**用户可见成果**：**说完立刻看到"说得怎么样"**——语音作答提交后点评还在流式生成，指标卡片已经出来：语速 246 字/分（参考区间 180~240）、填充词 12 处（"然后" 6 次 / "就是" 4 次）、最长停顿 3.2 秒（在"缓存雪崩"前）。内容点评评"答得对不对"，指标卡片评"说得顺不顺"，两者分列展示。属 P2 增强项，不阻塞主线。

**验收**：纯函数单测全绿（含边界）；真实语音作答一轮，指标与实际听感一致；文字作答不产出表达维度（`quality=TEXT_ONLY`）；作答不足 10 秒标 `TOO_SHORT`。

### 步骤 26：练习模式（P2，不阻塞主线）

- **后端（胡康宁）**：`drill_topic` / `drill_attempt` 两表落地（数据库设计 §3.18 / §3.19）；7 个接口（接口文档 §3.15：题目列表 / 新建 / 详情 / 编辑归档 / 单次详情 / 跨次进步对比 + `POST /stream/drill-review`）；建题支持从错题本 / 面经 / 投递记录 / 简历导入（**只记来源不复制内容**）；进步对比按 `voice_metrics.version` 过滤不可比记录
- **前端（卢世君）**：`/drill` 我的题目列表 + `/drill/:topicId` 题目详情（历次记录时间线与跨次对比 + 继续练）；建题入口（手动写题 / 从既有数据导入）；作答区复用 VoiceInput
- **测试（卢世君）**：题目 CRUD 与归属校验（跨账号 → 404）；归档后不可作答（40003）；同一题连练 3 遍逐次留存；练满 2 遍出跨次对比；口径版本不一致的记录不并列（**TC-69 / TC-70**）

**产出**：同一道题反复练、逐次点评、跨次看进步的练习闭环。

**用户可见成果**：**"把自我介绍练到不卡"在系统里闭环了**——建一道「自我介绍」，练一遍给一次点评；再练一遍，题目详情页把两次并排展示（填充词 12→4、时长 95 秒→68 秒、停顿 3 处→1 处），看得见哪里变好了。语音作答的表达指标直接进对比。与模拟面试的分工：模拟面试是"一场 N 题"练临场应变，练习模式是"一道题 N 遍"练到流畅。属 P2 增强项，不阻塞主线。

**验收**：AC-13 通过；练习记录逐次留存可回看；跨次对比三项以上指标正确；语音作答的指标进对比；归档题目不出现在默认列表、历史仍可查。

### 步骤 27：性能验证与体验打磨

- **后端（胡康宁）**：延迟实测三指标（NFR-001~003：AI 首字 <2s/语音 <1.5s/普通 API <200ms）；不达标项优化（Prompt 精简/缓存/预加载）；兜底路径复查（断网/错 Key/限流全场景走查）
- **前端（卢世君）**：交互细节打磨（加载态/空态/错误态/文案）；pytest-cov 覆盖率补齐至 ≥80%
- **测试（卢世君）**：全量回归 + 覆盖率报告

**产出**：性能达标报告 + 全量测试绿 + 覆盖率 ≥80%。

**用户可见成果**：**全站体验收口**——加载态 / 空态 / 错误态与提示文案补齐，断网、错 Key、限流等异常场景都有得体反馈；三项延迟指标（AI 首字 <2 秒、语音转写 <1.5 秒、普通接口 <200ms）实测达标。

**验收**：三指标实测达标（记录实测值）；全量 pytest 绿；覆盖率报告 ≥80%。

**🎯 里程碑 M4**：语音可用 + 表达力指标可视 + 练习模式可用 + 性能达标，**P0 功能开发全部结束**（P2 余新手指引一步，见步骤 28）。

### 步骤 28：新手指引（P2，功能开发收尾）

> 排期说明：**放在全部功能实现之后做**（2026-09-25 讨论确定）——指引高亮的目标入口必须是已定型的界面，功能未齐就做会反复返工。

- **后端（胡康宁）**：**无新增接口**——复用 `GET/PUT /settings` 承载账号级标记 `guide_done`（注册预置 `false`，响应体补该字段；沿用现有入参校验，传非布尔 → 10001）
- **前端（卢世君）**：`GuideTour.vue`（引入 `driver.js`，5 步高亮配置集中为一个数组）+ 首页挂载判定（`guide_done !== true` 时延迟 ~300ms 启动）+ 个人中心「重看新手指引」按钮（重播不回写标记）+ 元素缺失自动跳过
- **测试（卢世君）**：TC-68（`guide_done` 默认值与读写 / 走完与跳过均置 `true` / 「重看」不回写标记 / 两账号标记互不影响 / 引导不阻塞页面）

**产出**：新账号首进首页自动分步高亮引导；走完或跳过写入 `guide_done`；个人中心可重看。

**用户可见成果**：**新账号打开就知道从哪下手**——注册进首页自动高亮「投递管理 → JD 分析 → 模拟面试 → 陪练/错题 → AI 配置」5 个入口，每步一句话讲清用途、随时可跳过；老账号可在个人中心「重看新手指引」随时重播。属 P2，不阻塞主线。

**验收**：AC-12 通过；`guide_done` 读写正确（走完 / 跳过均置 `true`，重看不回写）；引导期间页面其余功能可用、不锁屏；两账号标记互不影响。

### 步骤 29：整体验收 + 演示准备（双方共同）

按 SRS 第 8 章 AC-1~AC-15 逐条过；测试计划第 3 节手工清单全过；执行验收演示预案（第 7 章）完整预演；文档一致性终检（代码 vs 接口文档 vs 数据库设计）；数据准备（预置演示数据）。

**产出**：可交付状态：代码 + 全套文档 + 演示预案 + 演示数据。

**用户可见成果**：**一套能完整演示、可交付的系统**——验收项逐条通过，演示脚本可一次跑完不断档，预置演示数据让每个页面打开即有内容。

**验收**：AC-1~AC-15 全部通过；pytest 全绿（覆盖率 ≥80%）；演示预演无断点。

**🎯 里程碑 M5**：项目交付。

## 6. 风险清单

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| LLM 供应商网络不稳/限流/欠费 | AI 功能全挂，演示受影响 | 重试+兜底提示；演示前检查余额/配额；**演示机热点兜底**；预留"预生成演示数据"方案 |
| 免费供应商限速严重 | 使用免费方案时 AI 响应慢/频繁失败 | 统一兜底（重试+提示）；演示环境优先配置有额度的供应商；设置页一键切换 |
| 长周期节奏松弛（≥2 个月跨度） | 中后期动力下降、临近截止赶工 | 5 个里程碑验收点强制阶段固化；每步验收三件套+质量基线；阶段完成做一次小复盘 |
| 前后端进度错位（一方慢导致联调等待） | 步骤阻塞 | 后端接口先行+mock 联调；每步验收前先对齐双方进度 |
| 本地语音模型首次下载失败 / 推理环境装不上 | 步骤 24 延迟 | P2 不阻塞主线；先用文字版验收；设置页切 `asr_provider=xunfei` 云端备用 |
| 本地模型依赖体积大（funasr 依赖 torch，venv 膨胀数 GB） | 环境搭建慢、磁盘占用高 | 依赖**仅在步骤 24 落地**（前 23 步不装）；模型首次联网下载（数百 MB）后本地缓存，**不入仓库** |
| 表达力指标算法口径调整 | 历史记录与新记录不可比，跨次进步曲线失真 | 指标带 `version` 标识，**跨版本记录不参与对比**（宁可少展示一次对比）；口径变更时同步修订 SRS 与系统设计 |
| 就业网站点改版导致解析失效 | 单个源数据断流 | **一厂商一解析器 + 单源隔离**：失败只标记该源状态（`last_status`），其余源与处理层不受影响；本地快照 fixture 可离线复现解析失败；界面提示"该源疑似失效" |
| 目标站点明确禁止抓取（robots 禁止 / 持续 403） | 该源不可用 | 合规组件按 robots 与状态码跳过并标记 `BLOCKED`，不重试；该源覆盖的学校改由**投喂通道**兜底（粘贴 JD / 活动信息经 AI 抽取入库） |
| 岗位信息来源不足（部分源岗位模块不开放） | 岗位库稀疏、匹配排序无数据可排 | 投喂通道兜底（本就是设计的一部分）；匹配打分对字段缺失不扣分也不隐藏 |
| 校招情报模块体量偏大（拆为 3 步） | 阶段三周期拉长 | 按步骤 21 → 22 → 23 顺序推进：步骤 21 先行交付多源宣讲会能力，页面与订阅功能分批联调 |
| 前后端协作冲突（git） | 联调返工 | 接口文档先行定稿；后端先出可 mock 的接口；每步骤结束合并一次 |
| 数据库连接空闲断开（MySQL `wait_timeout`） | 偶发 500 / 连接丢失 | 引擎 `pool_pre_ping`（取连接前探活）+ `pool_recycle=3600`（早于 MySQL 默认 `wait_timeout`）主动回收；LLM 调用不在事务内，长耗时请求不占连接 |
| LLM 输出格式漂移（不按五段/JSON 出） | 解析失败 | 格式校验+降级路径（设计第 7 章）；判定类 JSON 重试 1 次 |
| 演示环境无外网 | AI 功能瘫痪 | 手机热点；演示前完整预演一次；**语音链路已脱网**（本地 ASR + TTS 缓存），断网仍可演示语音作答 |

## 7. 验收演示预案

**演示脚本结构**（正式验收演示前按此完整预演）：

1. **开场**（30s）：一句话定位——"覆盖投递到复盘全流程的个人求职助手，AI 嵌入每个操作而不是套壳聊天"；
2. **入场与账号**（40s）：打开系统播出场动画 → 登录（勾选 30 天免登录）→ 进概览；顺带展示右上角用户菜单与个人中心（头像/画像/改密入口）；**现场注册一个新账号**触发新手指引（走 2 步后点「跳过」）；
3. **今日概览 + 校招情报**：展示四个提醒区块（提前造好数据：明日面试、3 天前投递、到期错题）+ **校招情报区块**（临近宣讲会含 1 条「已变更」、匹配度最高岗位、命中订阅计数）；点进「校招情报」页展示三类信息 Tab（含跨源去重后的一条）与**月历视图**（宣讲会 / 双选会 / 笔试 / 面试四类同屏）；
4. **投递三方式**：表单录入 → 悬浮球一句话录入（确认卡片）→ 批量导入（演示模板+一行非法行报错）；
5. **JD 分析**：粘贴真实 JD → 流式五段报告（边讲：SSE 流式，首字 2 秒内）；
6. **模拟面试**：发起会话 → **语音连续作答**（说完停顿自动断句转写、累积成段）→ **提交后指标卡片即时展示**（语速 / 填充词 / 停顿）→ 逐题点评（含一句音色播报）→ 总结报告；
7. **练习模式**：把刚才答得最差的一题存为「我的题目」→ 连练 2 遍 → 题目详情页展示**跨次进步对比**（填充词 / 时长 / 停顿三项变化）；
8. **陪练与错题**：答 1 题（故意答差）→ 自动入错题本 → AI 抽问；
9. **兜底演示（加分项）**：展示一次断网/错 Key 下的错误提示与重试——体现工程健壮性；
10. **收尾**（30s）：技术亮点一句话——手写 Agent、SSE 全链路、**多账号数据隔离**、多供应商+模型级可切换、**多源校招信息聚合（合规采集：UA 标识 / robots 检查 / 限频）**、LLM 全 mock 测试、文档体系完整。

**数据准备**：验收演示前在演示环境预置**演示账号**（含已配置的 AI 供应商 Key）与一套数据（投递 10+ 条覆盖各状态、面经 3 篇、错题 5 条、模拟面试 2 场、**练习题目 3 条其中 1 条已练满 2 遍带跨次对比**、**校招信息：宣讲会 4 条其中 1 条已变更 1 条跨两源重复、岗位 6 条含 1 条投喂 1 条已投递标记、订阅规则 2 条、信息源 3 个**），确保登录后每个区块打开即有内容。

**应急预案**：外网故障 → 热点；LLM 限流 → 顺势演示"兜底提示+重试"环节；数据库损坏 → 备份恢复（演示前用 `mysqldump` 导出 `job_hunter` 库留档，异常时导回）。
