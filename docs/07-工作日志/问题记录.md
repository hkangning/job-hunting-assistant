# 个人求职助手 —— 问题记录

| 项 | 内容 |
|---|---|
| 文档类型 | 工作日志（过程记录，**非开发依据文档**） |
| 维护人 | 胡康宁 / 卢世君 |
| 覆盖范围 | 项目开发全过程，按发现顺序追加 |
| 最后更新 | 2026-09-24 |

## 维护约定

1. 每个问题一条记录，分配**唯一编号 `IS-XX`**，按发现顺序递增，编号**不复用、不跳号**；
2. **不改写历史条目**——问题状态变化时只更新「处理」「结论」「状态」三栏，原始描述保持原样；
3. 状态取值仅三种：

   | 状态 | 含义 |
   |---|---|
   | ✅ 已解决 | 已处理完毕并经验证 |
   | ⏳ 待处理 | 尚未处理，或部分完成、剩余部分依赖后续步骤 |
   | 🚫 不处理 | **必须写明理由**——分两类：经双方商讨后忽略，或属设计内的已知行为 |

4. 「不处理」最需要写明理由（谁定的、依据哪份文档的哪一条）。本表存在的意义就是让**当时的判断可追溯**，避免日后重复讨论同一个问题；
5. 范围不限于前端/测试：发现的文档问题、后端实现问题、协作流程问题一律记入；
6. 本文件位于 `docs/07-工作日志/`，属**公共文件**，改后需与 `backend` 分支互相拉取同步。

---

## 问题一览

| 编号 | 日期 | 来源步骤 | 问题简述 | 状态 |
|---|---|---|---|---|
| [IS-01](#is-01--测试库未隔离测试会污染开发库) | 2026-09-24 | 步骤 2 测试 | 测试库未隔离，测试会污染开发库 | ✅ 已解决 |
| [IS-02](#is-02--conftest-完整版在步骤-1-无法交付) | 2026-09-24 | 步骤 1 测试 | conftest 完整版在步骤 1 无法交付 | ⏳ 待处理 |
| [IS-03](#is-03--默认值无法通过查表结构验证) | 2026-09-24 | 步骤 2 测试 | 默认值无法通过查表结构验证 | ✅ 已解决 |
| [IS-04](#is-04--测试计划缺少建表冒烟用例编号) | 2026-09-24 | 步骤 2 测试 | 测试计划缺少「建表冒烟」用例编号 | ✅ 已解决 |
| [IS-05](#is-05--开发计划中种子表名写作-questions实际为-question) | 2026-09-24 | 步骤 2 测试 | 开发计划中种子表名写作 `questions`，实际为 `question` | ✅ 已解决 |
| [IS-06](#is-06--侧栏今日待办在步骤-5-前无数据) | 2026-09-24 | 视觉改版 | 侧栏「今日待办」在步骤 5 前无数据 | 🚫 不处理 |
| [IS-07](#is-07--docsreadmemd-文档清单版本号滞后) | 2026-09-24 | 写工作日志 | `docs/README.md` 文档清单版本号滞后 | ✅ 已解决 |
| [IS-08](#is-08--任务收尾遗漏清理-backendappdb) | 2026-09-24 | 步骤 2 收尾 | 任务收尾遗漏清理 `backend/app.db` | ✅ 已解决 |
| [IS-09](#is-09--conftest-两套隔离方案被手工合并测试全线不可用) | 2026-09-24 | 步骤 3 合并 | conftest 两套隔离方案被手工合并，测试全线不可用 | ✅ 已解决 |
| [IS-10](#is-10--删除投递被外键拦下会返回-500) | 2026-09-24 | 步骤 3 走查 | 删除投递被外键拦下会返回 500 | ✅ 已解决 |
| [IS-11](#is-11--运行产物-appdb-shm--appdb-wal-被提交入库) | 2026-09-24 | 步骤 3 提交 | 运行产物 `app.db-shm` / `app.db-wal` 被提交入库 | ✅ 已解决 |
| [IS-12](#is-12--命令行长参数中的中文被按-gbk-传递请求体解析失败) | 2026-09-24 | 步骤 3 验收 | 命令行长参数中的中文被按 GBK 传递，请求体解析失败 | ✅ 已解决 |
| [IS-13](#is-13--跨职责文件-conftestpy-存在合并冲突风险) | 2026-09-24 | 步骤 3 合并修复 | 跨职责文件 `conftest.py` 存在合并冲突风险 | ✅ 已解决 |
| [IS-14](#is-14--_cell_text-的-date-分支在-xlsx-路径下不可达) | 2026-09-24 | 步骤 3 测试补强 | `_cell_text` 的 `date` 分支在 xlsx 路径下不可达 | 🚫 不处理 |

---

## IS-01 · 测试库未隔离，测试会污染开发库

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 2 测试，准备编写建表冒烟时） |
| 类别 | 测试基础设施 |
| **问题** | `tests/conftest.py` 的 `client` fixture 直接使用生产 engine（`sqlite:///./app.db`）。任何触发应用 `lifespan` 的测试都会在**开发库**上建表、导入 120+ 道种子题、插入初始行 |
| **影响** | 违反测试计划 §1.1「测试数据库不碰开发库」；本步的建表冒烟需反复执行初始化，不修复则无法安全编写 |
| **处理** | 改为在 `conftest.py` **模块级**（早于任何 `app.*` 导入）把 `DATABASE_URL` 指向临时目录下的独立 SQLite 文件——`app/config.py` 的 `Settings` 在模块导入时即读取环境变量，晚一行就固定成开发库；会话结束先 `engine.dispose()` 再删目录 |
| **结论** | 修复后开发库全程零污染（5 个任务期间 `app.db` 始终未被创建），临时目录无 `jobpilot-test-*` 残留 |
| **状态** | ✅ 已解决 |

> 详见[测试日志 · 步骤 2](测试日志.md)。

---

## IS-02 · conftest 完整版在步骤 1 无法交付

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 1 测试） |
| 类别 | 计划 / 依赖 |
| **问题** | 开发计划步骤 1 要求 conftest 交付「独立测试库 + `FakeLLMProvider` 占位 + 依赖注入点」三项，但三项依赖的模块均未就位：独立测试库依赖步骤 2 的 `database.py`、`FakeLLMProvider` 依赖步骤 6 的 `llm_provider.py`、注入点依赖步骤 6 的 `get_llm_provider` |
| **影响** | 步骤 1 只能交付基础 `client` fixture，无法满足该条要求 |
| **处理** | 拆到依赖就位的步骤补齐，并在测试日志中登记欠账。**独立测试库部分已于步骤 2 完成**（见 IS-01）；`FakeLLMProvider` 与注入点待步骤 6 |
| **结论** | 隔离层已就位，步骤 6 届时只需新增 fixture，无需再动隔离逻辑 |
| **状态** | ⏳ 待处理（剩余部分依赖步骤 6） |

---

## IS-03 · 默认值无法通过查表结构验证

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 2 测试设计阶段） |
| 类别 | 技术陷阱 |
| **问题** | SQLAlchemy 的 `default=` 是 **Python 侧默认值**，在 DDL 中**不产生 `DEFAULT` 子句**。若按常规思路查 `PRAGMA table_info` 的 `dflt_value` 来验证默认值，会得到全 `NULL` |
| **影响** | 会导致误判「枚举默认值未设置」，或写出永远通过（永远为空、断言不到东西）的假测试 |
| **处理** | 改为**行为验证**：插入一行**不指定**该列，再读回断言（如 `application.status` 读回应为 `"APPLIED"`） |
| **结论** | 用例 9 / 10 依此设计，11 处枚举默认值 + 整型 / 文本默认值全部验证通过 |
| **状态** | ✅ 已解决 |

---

## IS-04 · 测试计划缺少「建表冒烟」用例编号

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 2 测试） |
| 类别 | 文档缺口 |
| **问题** | 测试计划 §2 的三类用例清单（服务层单测 / API 集成 / SSE 专项）中**没有**对应「建表冒烟」的编号；步骤 2 的验收依据只写在开发计划正文里 |
| **影响** | 测试计划与实际测试项目录对不上，验收时无法按编号检索对应实现 |
| **处理** | 经双方确认「文档双方都可动、改后及时同步」后，在测试计划新增 §2.4「数据库层冒烟」并分配编号 **TC-51**（编号全局递增分配，不按小节顺序连续，不影响既有编号）；同时按下游方向把开发计划步骤 2 的「主要验收用例」列由文字改为 TC-51 |
| **结论** | 测试计划 v1.3 → v1.4，开发计划 v2.0 → v2.1 |
| **状态** | ✅ 已解决 |

---

## IS-05 · 开发计划中种子表名写作 `questions`，实际为 `question`

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 2 测试） |
| 类别 | 文档笔误 |
| **问题** | 开发计划步骤 2 验收标准写「`questions` 表种子数据 ≥120 题」，而数据库设计文档 §1 约定**表名单数**、§3.7 定义的表名为 `question` |
| **影响** | 轻微——按实际表名测试即可，但两处口径不一致，易在后续引用时产生混淆 |
| **处理** | 测试文件中以 `question` 为准。2026-09-24 后端侧核实：第 96 行的 `questions.json` 是**文件名**（无误），确系笔误的只有第 101 行「questions 表种子数据 ≥120 题」，应作 `question` 表。**2026-09-24 经用户授权修正完毕**：该行已改为 `question` 表，变更记录并入开发计划当天版本行（保持 v2.1，不新增版本号） |
| **结论** | 已修正；开发计划属公共文件，该改动需与 `frontend` 侧同步（并入既有同步清单） |
| **状态** | ✅ 已解决 |

---

## IS-06 · 侧栏「今日待办」在步骤 5 前无数据

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（视觉改版） |
| 类别 | 设计内的已知行为 |
| **问题** | 侧栏「今日待办」的数据源 `GET /overview` 属开发计划**步骤 5**，后端尚未实现，真实环境中该请求会失败 |
| **影响** | 该区块在步骤 5 完成前保持隐藏 |
| **处理** | **不处理**。系统设计文档 4.4 明确要求「加载失败时整块隐藏，不在侧栏常驻显示错误」；`request.js` 已新增 `silent` 选项配合，`SideNav.vue` 捕获异常后清空列表 |
| **结论** | 步骤 5 完成后自动生效，**非缺陷** |
| **状态** | 🚫 不处理（设计内行为，依据：系统设计文档 4.4） |

---

## IS-07 · `docs/README.md` 文档清单版本号滞后

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（编写工作日志时） |
| 类别 | 文档一致性 |
| **问题** | 文档清单中：项目结构记 v1.4（实际 v1.5）、测试计划记 v1.3（实际 v1.4）、开发计划记 v2.0（实际 v2.1）、用例数记 50（实际 51） |
| **影响** | 文档总目录与实际版本不符，查阅时误导（README 是文档体系的入口） |
| **处理** | 本次一并对齐四处，并新增 07-工作日志两行；同时明确 07 目录为过程记录、不参与文档依赖链 |
| **结论** | 已对齐 |
| **状态** | ✅ 已解决 |

---

## IS-08 · 任务收尾遗漏清理 `backend/app.db`

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 2 收尾，经用户指出） |
| 类别 | 流程 / 工作习惯 |
| **问题** | 建表冒烟验证中按计划创建的开发库 `backend/app.db`，被当作「验收证据」保留，未按「任务收尾清理临时产物」的既有约定删除 |
| **影响** | 仓库工作目录多出运行产物；虽然已被 `.gitignore` 覆盖不入库，但与「收尾时目录干净」的约定不符 |
| **处理** | 删除该文件；明确判断口径——**以「任务开始时它存在吗」为准**，收尾时目录不该比开工时多出任何文件；能自动重建的运行产物（如 `app.db`，后端启动时 `init_db()` 会重建）一律清掉，不以「留作验收证据」为由保留（证据是测试输出与验收记录，不是文件本身） |
| **结论** | 已修正，并记入个人工作约定 |
| **状态** | ✅ 已解决 |

---

## IS-09 · conftest 两套隔离方案被手工合并，测试全线不可用

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 3 提交后、合入 `origin/frontend` 时） |
| 类别 | 流程 / 合并冲突解决 |
| **问题** | `tests/conftest.py` 被拼进**两套测试库隔离方案**：测试侧步骤 2 交付的**模块级 `DATABASE_URL` 环境变量**方案，与步骤 3 改造的**逐用例替换全局 engine** 方案；拼接时 `create_engine` / `sessionmaker` / `Session` / `Generator` 等 import 一并丢失 |
| **影响** | `pytest` 直接收集失败（`NameError: name 'Engine' is not defined`），43 条用例一条也跑不了。**更隐蔽的风险**：若只补 import 了事，`tests/test_database_init.py` 用的是模块级 `from app.database import engine`——替换方案对它无效（只改模块属性，改不掉用例里已绑定的名字），那 15 条建表冒烟会**真实打在开发库上** |
| **处理** | 以**模块级 `DATABASE_URL` 为唯一隔离底座**（对全部用例一视同仁，且是 test_database_init 的依赖），三个 fixture 改为基于全局 `engine` / `SessionLocal`；另加每例 `_reset_database()`（`drop_all` + `create_all`）保证用例间互不干扰；临时目录清理加三次重试（Windows 句柄释放有延迟，一次 `rmtree` 会出现「文件删掉、空目录留下」） |
| **结论** | 全量 **43 passed**（6 骨架 + 15 建表冒烟 + 22 投递）；临时目录无残留；开发库时间戳未变 |
| **状态** | ✅ 已解决 |

> 详见[后端工作日志 · 合并事故修复](后端工作日志.md)。**经验**：共享基础设施（conftest / 公共 fixture）合并时不能"两边都留"——要么 import 缺失直接报错，要么两套机制互相架空、让某批用例偷偷跑在开发库上；一律以一份为准，另一份的增量改造到这一份上。

---

## IS-10 · 删除投递被外键拦下会返回 500

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 3 走查） |
| 类别 | 后端实现 |
| **问题** | `application` 被 `jd_analysis_report`、`interview_session` 两表以 `application_id` 外键引用；投递已有关联数据时，`DELETE /applications/{id}` 触发 `IntegrityError: FOREIGN KEY constraint failed`，被全局兜底处理器转为 10000（HTTP 500） |
| **影响** | 删除接口在「已分析过 JD」「已做过模拟面试」的投递上不可用——而这恰是使用一段时间后的常态 |
| **处理** | 删除前先把两表指向该投递的 `application_id` **置空**（两列本就可空），保留子数据、仅解除关联，再物理删除；已加用例 `test_delete_application_detaches_related_records` 覆盖。`reminder.ref_id` 无外键约束，不构成拦截源 |
| **结论** | 删除成功且子数据保留；**步骤 17 生成提醒时需按 `ref_id` 有效性过滤**（该残留项已记入后端工作日志遗留） |
| **状态** | ✅ 已解决 |

---

## IS-11 · 运行产物 `app.db-shm` / `app.db-wal` 被提交入库

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（核查 `fd3509a` 提交内容时） |
| 类别 | 流程 / 工作习惯（`.gitignore` 覆盖不全） |
| **问题** | 步骤 3 的提交中包含 `backend/app.db-shm`、`backend/app.db-wal`——SQLite WAL 模式的伴生文件；`.gitignore` 只写了 `*.db`，未覆盖 `-shm` / `-wal` 后缀 |
| **影响** | 运行产物入库；且这两个文件随服务运行持续变化（服务一启动就存在），会造成无意义 diff，并可能再次被误提交 |
| **处理** | 2026-09-24 经用户确认后执行：①`git rm --cached backend/app.db-shm backend/app.db-wal`（本地文件保留，服务运行不受影响）②`.gitignore` 在 `*.db` 后补 `*.db-shm`、`*.db-wal`。**注意 `.gitignore` 属公共文件**，改动需与 `frontend` 侧同步 |
| **结论** | 两个文件已移出索引（`git ls-files` 中不再有 `.db` 类文件）、规则已补齐；下次提交后彻底生效 |
| **状态** | ✅ 已解决 |

---

## IS-12 · 命令行长参数中的中文被按 GBK 传递，请求体解析失败

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 3 接口验收） |
| 类别 | 技术陷阱 / 验收方法 |
| **问题** | Windows（Git Bash）下用 `curl -d '{"company":"浩鲸科技"}'` 直传中文 JSON，服务端返回 `{"code":10000,"message":"There was an error parsing the body"}`——命令行参数按 GBK 编码传递，而 HTTP 请求体按 UTF-8 解析 |
| **影响** | 易误判为接口缺陷；实际请求体在进入业务代码前就解析失败了（纯 ASCII 内容不受影响，故极易漏判） |
| **处理** | 中文请求体写成 UTF-8 文件后 `curl --data-binary @file`（文件上传用 `-F "file=@..."`）；调试接口优先用 Swagger UI（`/docs`）或 Postman |
| **结论** | 17 组验收请求全部通过，**非接口缺陷**；口径已记入验收方法约定 |
| **状态** | ✅ 已解决 |

---

## IS-13 · 跨职责文件 `conftest.py` 存在合并冲突风险

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 3 合并事故修复后，经用户问询核查） |
| 类别 | 协作流程 / 分支约定灰区 |
| **问题** | `backend/tests/` 位于 `backend/` 目录下（按分支约定归后端改动），但文件内容实际由**测试侧**交付与维护。经核查，步骤 2 交付的 `test_database_init.py` / `test_health.py` / `expected_schema.py` 三文件在两条分支上**版本完全一致**，唯独 `conftest.py` 因本次修复在两侧分叉（`backend` 侧已改，`frontend` 侧仍为原版） |
| **影响** | ①**当前合并无冲突**——`97033e8` 已把 `origin/frontend` 合入，该文件的合并基线即测试侧现版本，后端侧改动属单边修改，git 可自动合并；②**风险在下一轮**：若测试侧在自己分支上继续改此文件（如步骤 6 补 `FakeLLMProvider`，见 IS-02 欠账），双方修改同一批行，合并必冲突；③**解决方向错误的后果**：若冲突时保留测试侧原版，后端侧新增的 `db_session` fixture 会丢失，`test_applications.py` 的 22 条用例将直接报错（**非静默失败**，会立刻暴露） |
| **处理** | ①本次修复**保留测试侧原始隔离方案不动**（`_TEST_TMP_DIR`、模块级 `DATABASE_URL`、连接收尾逻辑均原样），改动属**纯增量**（新增 `db_session` fixture / 每例 `_reset_database()` / 清理重试）；②**约定补漏已落地**——项目结构 v1.5 第 1 节新增「跨职责文件区」（测试侧改动前先 `git fetch` 取后端侧版本再叠加、后端侧改动先知会测试侧、冲突以后的版本为准）；③**剩与测试侧知会**（用户口头告知即可） |
| **结论** | 已核实不影响既有测试：全量 **43 passed**（含测试侧 15 条建表冒烟）；另将测试侧原版 `conftest.py` 单独切回验证为 **21 passed**，确认**其代码本身无误、问题出在合并产物**。约定层面已补漏。**2026-09-24 补充**：测试侧已于步骤 3 测试补强时读取本记录并确认知悉跨职责约定；本步改动 `backend/tests/test_applications.py` 前已按约定 `git fetch origin` 并核实与 `origin/backend` 无差异（`git diff --stat` 为空），改动为**纯追加**（唯一变更行是首行 import 补 `datetime`），知会目的已达成 |
| **状态** | ✅ 已解决 |

---

## IS-14 · `_cell_text` 的 `date` 分支在 xlsx 路径下不可达

| 项 | 内容 |
|---|---|
| 发现日期 | 2026-09-24（步骤 3 测试补强，设计阶段实测） |
| 类别 | 后端实现 / 死代码观察（**非缺陷**） |
| **问题** | `app/services/application_service.py:311-312` 的 `if isinstance(value, date): return value.strftime(DATE_FORMAT)` 在 xlsx 导入路径下**不可达**——第 309 行的 `isinstance(value, datetime)` 恒先命中 |
| **影响** | 无功能影响（两个分支行为一致，都返回 `YYYY-MM-DD`）。但步骤 20 做覆盖率统计时该行会显示未覆盖，若无记录会被反复讨论 |
| **处理** | **不处理**。属防御性代码——若某版本 openpyxl 返回 `date` 而非 `datetime`，该分支即生效；且 `date` 是 `datetime` 的父类，先查 `datetime` 再查 `date` 的顺序本身是正确的写法。删除无收益，且改 `app/` 代码超出测试侧职责（开发计划 §2 分工） |
| **结论** | 实测依据：`load_workbook(..., read_only=True, data_only=True)` 读回日期单元格得到 `datetime.datetime(2026, 9, 1, 0, 0)`，类型为 `datetime`。实际生效的 `datetime` 分支已由 `test_import_xlsx_date_cell` 覆盖 |
| **状态** | 🚫 不处理（防御性代码，登记备查） |

> 详见[测试日志 · 步骤 3](测试日志.md)。

---

## 状态统计

| 状态 | 数量 | 编号 |
|---|---|---|
| ✅ 已解决 | 11 | IS-01、IS-03、IS-04、IS-05、IS-07、IS-08、IS-09、IS-10、IS-11、IS-12、IS-13 |
| ⏳ 待处理 | 1 | IS-02（待步骤 6） |
| 🚫 不处理 | 2 | IS-06（设计内行为）、IS-14（后端防御性代码） |
