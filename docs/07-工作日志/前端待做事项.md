# 个人求职助手 —— 前端待做事项

| 项 | 内容 |
|---|---|
| 文档类型 | 协作台账（**非开发依据文档**） |
| 登记维护 | 胡康宁（后端）——新增事项追加到表尾 |
| 完成回标 | 卢世君（前端 + 测试）——完成后在「状态」列标注 |
| 覆盖范围 | 后端侧提出、需前端 / 测试侧配合处理的事项（自 2026-09-25 起累积） |
| 最后更新 | 2026-09-26 |
| 上游依据 | [后端工作日志](后端工作日志.md)（各事项的背景与后端先行改动见对应步骤节） |

## 维护约定

1. **后端侧登记**：后端工作中产生、需要前端 / 测试侧配合的事项（改前端代码、补测试用例、动对方维护的文档章节等），**一律追加到本表表尾**；事项若涉及后端先行改动，已在[后端工作日志](后端工作日志.md) 对应节记录。
2. **前端侧回标**：事项完成后，由前端 / 测试侧把「状态」列改为 **`已完成（日期，执行人：卢世君）`**，并写明依据（文件 / 版本号 / 命令结果），同时更新上方「当前待处理」行，**不删行**。
3. **后端侧定期核对**：后端侧定期 `git fetch origin` 后对照 `origin/frontend` 的代码与文档，核对事项实际落地情况并回标，**不等对方通知、不等提醒**；两种回标方式并存，以最新一次为准。
4. 本表**只增不删**：完成的条目保留在表内作为协作记录。
5. 本文件位于 `docs/07-工作日志/`，属**公共文件**，改后需与 `frontend` 分支互相拉取同步（冲突以后端最新版为准）；**问题记录、测试计划的维护归属不变**（见[项目结构](../02-设计文档/项目结构.md) 第 1 节「跨职责文件区」）。
6. **与问题记录的分工（2026-09-26 起）**：问题记录中的条目按「**谁处理谁标注**」由处理方自行结案（见[问题记录](问题记录.md) 维护约定 8），**不登记为本表事项**；本表只登记需要前端 / 测试侧**实际动手**的事项。

## 事项表

**当前待处理**（截至 2026-09-26）：**#11、#27、#32、#33、#37、#38、#39、#40、#41**。（**#17、#34、#35、#36 已于 09-26 完成并验证通过**，见下表对应行。）

- **#11 / #17 / #27**：**前置依赖尚未满足**，非遗漏——分别待步骤 26（练习模式表与枚举落地）、步骤 9（`/overview` 的 `stats` 落地）、步骤 23（校招情报前端实现落地后登记目录树）；
- **#32 / #33**：**排期已定**——2026-09-26 经讨论确认**不提前做**（分属步骤 13 / 15，其中陪练页的点评依赖步骤 11 的 SSE 基建），届时按接口文档 v1.14 落地即可；
- **#36 / #37**：**后端已就绪**，等测试侧执行（分别对应 `GET /overview` 用例与导入模板中文表头的断言同步）。
- **#41**：**纯表述清理**，不涉功能与结论，择机改注释即可（两处文件头注释里的本地过程目录路径）。

| # | 事项 | 落点 | 登记日期 | 状态 |
|---|---|---|---|---|
| 1 | 头像 URL 拼接改用库中相对路径（`'/' + avatar`），不再取文件名重拼 `/static/avatars/`（IS-22 口径定案 = `/uploads`） | `frontend/src/utils/avatar.js:13` | 09-25 | **已完成（09-25，卢世君：`avatarUrl` 已改 `'/' + avatar`）** |
| 2 | 开发代理前缀 `/static` → `/uploads`（IS-22） | `frontend/vite.config.js:29` | 09-25 | **已完成（09-25，卢世君：proxy 已改 `/uploads`）** |
| 3 | mock 层静态资源路径 `/static/avatars/*` → `/uploads/*`（IS-22） | `frontend/mock/index.js:42-43`、`frontend/mock/misc.js:66` | 09-25 | **已完成（09-25，卢世君：两处已改 `/uploads/avatars/`）** |
| 4 | mock 层各错误码的 HTTP 状态码按接口文档 §1.3 新表校正，不再自行推断（IS-18）——重点 `80005` 账号锁定 = **403**（原推断 423） | `frontend/mock/**` | 09-25 | **已完成（09-25，卢世君：`mock/auth.js` 已改 80005 → 403）** |
| 5 | 项目结构 §3 前端目录树 mock 说明行路径改 `/uploads/*` | 项目结构 207 行 | 09-25 | **已完成（09-25，卢世君：随项目结构 v1.21）** |
| 6 | 测试日志头像路径口径改 `/uploads`（2 处）；TC-62~66 真后端复核不再被 IS-22 阻塞 | 测试日志 432 / 452 行 | 09-25 | **已完成（09-25，卢世君：TC-65 行已改 `/uploads/avatars/<file>`，阻塞说明已解除）** |
| 7 | IS-16 / IS-22 处理后可在问题记录标已解决结案（后端侧均已闭环） | 问题记录 | 09-25 | **已完成（09-26，卢世君：IS-22 已于 09-25 结案；IS-16 已由测试侧逐值复核通过并于 09-26 结案）** |
| 8 | 测试计划补练习模式与表达力用例——题目 CRUD 与归属校验 / 归档 40003 / 连练逐次留存 / 跨次对比 / `version` 过滤 / `speech_metrics` 边界 / `segments` 解析 | 测试计划（编号由测试侧分配） | 09-25 | **已完成（09-25，卢世君：新增 TC-69~TC-72）** |
| 9 | TC-51 表数 **17 → 19**；§6 通过标准 **AC-1~AC-12 → AC-1~AC-13** | 测试计划 138 / 191 行 | 09-25 | **已完成（09-25，卢世君：测试计划 v1.10）** |
| 10 | 「步骤 25 / AC-1~AC-12 整体验收」→ 整体验收现为**步骤 27**、验收标准 **AC-1~AC-13** | 测试日志 592 行 | 09-25 | **已完成（09-25，卢世君：测试日志索引已按 27 步版重排，整体验收 = 步骤 27）** |
| 11 | 枚举计数用例随新增枚举调整——`drill_topic.source`（六值）、`voice_metrics.quality`（三值）、`wrong_question.source_type` 新增 `DRILL` | 测试计划 TC-51 / `expected_schema.py` | 09-25 | 待处理（**前置依赖：步骤 24 练习模式落地**——枚举尚未进模型，现改用例必失败；测试侧已注明「待后端实现步骤 24 后执行」） |
| 12 | 项目结构 §3 前端目录树补 `/drill`、`/drill/:topicId` 两页与 `api/drill.js` | 项目结构（前端章节） | 09-25 | **已完成（09-25，卢世君：随项目结构 v1.21）** |
| 13 | 语音用例 TC-31 / TC-41 按「VAD 断句 + 逐句转写 + 自适应阈值」重写；§5.2 标题「真实讯飞冒烟」需改 | 测试计划 | 09-25 | **已完成（09-25，卢世君：测试计划 v1.10 改写 TC-31 / TC-41，§5.1 标题已改「真实供应商冒烟」）** |
| 14 | 测试计划补账号体系与 AI 配置用例 | 测试计划 | 09-25 | **已完成（09-25，卢世君：TC-52~TC-68）** |
| 15 | 项目结构 §3 前端目录树登记账号体系新页面（Welcome / Login / Register / Profile / AiConfig）、组件 `UserMenu`、store `user.js`、路由守卫 | 项目结构（前端章节） | 09-25 | **已完成（09-25，卢世君：随项目结构 v1.18 / v1.21 登记）** |
| 16 | （旧账）TC-47 端点迁 `/llm-providers/{provider}/test`；测试日志 82 / 91 行「9 个枚举类」需改；IS-13 conftest 约定知会；npm 门槛 `>=11.12.1` 须升级（11.6.1 会被 `EBADENGINE` 拒装） | 测试计划 / 测试日志 / 仓库检出环境 | 09-25 | **已完成（09-26）**：①TC-47 已迁 **`POST /llm-providers/test`**（`provider` 在请求体）——**本条原描述的 `/{provider}/test` 系笔误**，测试计划 v1.12 已勘误并说明该错误写法从未出现在接口文档中（`git log -S` 零命中），经核对与实现一致；②测试日志「9 个枚举类」两处已改 10；③IS-13 知会已完成（问题记录 IS-13 状态栏已记「知会动作已完成」）；④npm 门槛已升级（问题记录 IS-14：前端侧 Node 24.21.0 / npm 12.1.0，锁文件一字未改） |
| 17 | 测试计划 TC-65（个人中心）可补「数据概览卡片显示真实计数（`GET /overview` 的 `stats`）」验证点——口径见接口文档 v1.14 §3.4 | 测试计划 TC-65 | 09-25 | **已完成（09-26，执行人：卢世君）**：测试计划 **v1.19** 的 TC-65 已补验证点「**数据概览卡片显示真实计数**（取 `GET /overview` 的 `stats` 三项：投递数 / 错题数 / 面试场次，未返回时显示占位符）」。**并经真联调实测**——个人中心三张卡片显示真实计数（投递 6 / 错题 0 / 面试 0），与接口返回一致 |
| 18 | 补 TC-55 / TC-56 / TC-61（AI 配置增删改与 Key 密文落库 / 激活互斥 / 多账号配置隔离）并改写 TC-47~49（端点已迁 `/llm-providers`）——接口口径见接口文档 v1.10 §3.3/§3.12；**用例需带鉴权头**（8 个新端点全部挂 `get_current_user`），mock 层可对接 | 测试计划 / `backend/tests/` | 09-26 | **已完成（09-26，卢世君：测试计划 v1.13 完成 TC-47~49 改写；新增 `tests/test_llm_providers.py` 38 条，全量 152 passed）** |
| 19 | 测试计划两处口径随步骤 6 落地调整（均属测试侧文档，后端未动）：①§5.2 mock 方案段「`app/clients/llm_provider.py` 定义抽象 `LLMProvider`，生产实现走 DeepSeek」与实现不符——该模块现为 12 家注册表 + 探测函数，**无抽象基类**，抽象与对话实现属步骤 10 的 `llm_client`；②TC-50 描述中的 `test_connection` / `list_models`（含缓存与回退）已在步骤 6 交付，可拆为「步骤 6 已验收 + 本步对话类」或改写措辞 | 测试计划 50 / 96 行 | 09-26 | **已完成（09-26，卢世君：测试计划 v1.12——§1.3 模块归属订正为「步骤 10 的 `llm_client`」、TC-50 收窄为对话类能力）** |
| 20 | 问题记录 **IS-02** 的依赖项顺延：原记「`FakeLLMProvider` 依赖步骤 6 的 `llm_provider.py`、注入点依赖步骤 6 的 `get_llm_provider`」——步骤 6 已交付的 `llm_provider.py` 是 12 家注册表 + 探测函数（**无抽象基类**，`get_provider` 只返回元数据、非可替换的 Provider 实例），`get_llm_provider` 注入点与测试替身实际属**步骤 10** 的 `llm_client.py`；请测试侧将 IS-02 的依赖步骤由 6 更正为 10 并更新状态说明（与 #19 ①同源；问题记录由测试侧维护，后端侧只读） | 问题记录 IS-02 | 09-26 | **已完成（09-26，卢世君：IS-02 已补编号说明，明确「以测试日志文末索引的步骤 10 为准」）** |
| 21 | 问题记录 **IS-21 可结案**：测试计划 TC-58 的 Token 错误码口径**已修正**——测试计划 v1.9（提交 `a35768b`，2026-09-25）改为「Token 无效 80001 / 已过期 80002」并补判定说明，与 `deps.get_current_user` 实现、接口文档 §1.3 三方一致（已逐条复核测试计划其余 8xxxx 引用：TC-52/53/54/58/64 全部正确）；请测试侧将 IS-21 状态改为已解决结案（问题记录由测试侧维护，后端侧只读） | 问题记录 IS-21 | 09-26 | **已完成（09-26，卢世君：IS-21 状态已改 ✅ 已解决）** |
| 22 | 测试计划文档头部「维护人」字段更正：现为「胡康宁」，按 2026-09-26 明确后的职责边界（项目结构 v1.23 / 开发计划 v2.12：测试计划文档归测试侧维护）应改为「**卢世君（测试负责人）**」，与问题记录头部口径一致；**该文档内容归测试侧，后端侧未动**，请测试侧更正 | 测试计划头部第 7 行 | 09-26 | **已完成（09-26，卢世君：测试计划 v1.14 头部维护人已改「卢世君」）** |
| 23 | 问题记录 **IS-23** 的用例编号分配——`/settings` 契约变更（步骤 6 交付的字段闭包与部分更新语义）需补 TC 编号：归属**经讨论确定补进步骤 6 用例组**（接口在本步交付、回归防护用例已写入 `test_llm_providers.py`），请测试侧分配编号并回标；后续步骤新增的设置键（`asr_provider` / `tts_voice` 属步骤 22、`guide_done` 属步骤 26）由各步用例另行覆盖 | 测试计划 / 问题记录 IS-23 | 09-26 | **已完成（09-26，执行人：卢世君）**：编号分配为 **TC-86**，已登记入测试计划 **v1.15** §2.2（字段闭包不得回流 LLM 字段 / 部分更新语义 / 布尔严格校验 / 题量越界 / `crawl_url` 废弃口径）；问题记录 IS-23 同步结案 ✅ |
| 24 | 问题记录 **IS-25 已在后端侧闭环**：`GET /llm-providers` 单条 DTO 已补 `needs_key`（接口文档 **v1.11** §3.3 + 代码 2 文件，实测字段闭包 8 个、`ollama=false` 其余 11 家 `true`）→ 请测试侧将 IS-25 结案（若要补断言，可在既有 AI 配置用例组内加 `needs_key` 校验）；**前端侧可选优化**：`ProviderCard.vue` 的 `KEYLESS_PROVIDERS` 硬编码（`provider === 'ollama'`）可改读响应 `needs_key` 字段——不改也功能正常，改则注册表扩展时无需同步常量 | 问题记录 IS-25 / `frontend/src/components/ProviderCard.vue` | 09-26 | **已结案（09-26，胡康宁：IS-25 按「谁处理谁标注」由后端侧自行标注 ✅ 已解决）**；余「前端择机改读 `needs_key`」为可选优化，不做亦不影响功能 |
| 25 | 问题记录 **IS-26 已在后端侧闭环**：项目结构 **v1.25** §3 已补登 `api/settings.js`、`components/ProviderCard.vue`，并一并补登核查中发现的同类漏登 `components/ApplicationStats.vue`（步骤 3 产物）；§5 依赖清单已核对**无需联动** → 请测试侧将 IS-26 结案 | 问题记录 IS-26 / 项目结构 | 09-26 | **已结案（09-26，胡康宁：IS-26 按「谁处理谁标注」由后端侧自行标注 ✅ 已解决）** |
| 26 | `frontend/src/api/profile.js` 仍导出 `getSettingsApi` / `updateSettingsApi`，与步骤 8 新增的 `settings.js` **重复定义**（现 `Settings.vue` 走 `settings.js`）——是否清理（或让 `profile.js` 改从 `settings.js` 转出）由前端侧判断；**属前端代码，后端未动**，仅在项目结构 v1.25 的 `profile.js` 说明行如实加注 | `frontend/src/api/profile.js` / `settings.js` | 09-26 | **已完成（09-26，执行人：卢世君）**：核查后确认两个方法**已无任何引用**（`Settings.vue` 走 `settings.js`，`Profile.vue` 只用画像两方法），按死代码**直接删除**；文件头注释同步改为「画像接口封装（接口文档 §3.12），设置接口见 settings.js」。未采用「转出」方案——增加一层转出只为兼容无人使用的导出，不如删净 |
| 27 | 项目结构 **§3 前端目录树**补登「校招情报」相关文件（步骤 23 交付，页面 / 组件命名由前端侧定）：`/campus` 页 + 站内日历月视图 + 我的订阅管理 + 设置页源管理区块 + 概览区块改造所涉组件，以及 `api/campus.js`（对应接口见接口文档 v1.12 §3.16，14 个端点）；**属前端章节，后端未动** | 项目结构（前端章节） | 09-26 | 待处理（前置依赖：步骤 23 前端实现落地后登记） |
| 28 | 测试计划随「校招情报」模块同步三项：①补 **AC-14 / AC-15** 用例（采集层多源去重 / 变更检测 / 单源隔离、投喂三条路径、匹配打分、订阅命中与跨账号隔离、日历区间边界；**编号自 TC-73 起**由测试侧分配，LLM 全 mock、抓取走本地快照 fixture 不联网）；②**TC-51 表数 19 → 22**；③§6 通过标准 **AC-1~AC-13 → AC-1~AC-15**。另：**测试日志**内的步骤索引需按 **29 步版**重排（整体验收 = **步骤 29**，语音 = 24、表达力 = 25、练习 = 26、新手指引 = 28） | 测试计划 / 测试日志 | 09-26 | **已完成（09-26，执行人：卢世君）**：测试计划 **v1.15**——①**新增 14 条**（TC-73~79 服务层 / TC-80~82 API / TC-83~85 前端手工 / TC-86 `/settings`，编号自 TC-73 起按序分配）；②TC-51 表数 **19 → 22** 并补四处列增量（`campus_event` 扩 9 字段 + `UNIQUE(dedup_key)`、`reminder.ref_type`、`job_posting` 混合归属）；③§6 **AC-1~AC-15**；④**TC-23 改写**为「多源采集与单源隔离」（原口径无单源隔离，而它是 FR-021 的核心失效语义）；⑤测试日志步骤索引已按 **29 步版**重排。**用例总数 72 → 86** |
| 29 | 前端 **设置页移除 `crawl_url` 表单项**（`Settings.vue:28/46/70/173`）及 `mock/misc.js:37` 对应字段：该配置键已废弃（数据库设计 v1.5 §3.14 / 接口文档 v1.12 §3.12），抓取目标改由源清单承载——**后端侧已于 2026-09-26 随「MySQL 切换 + 题库升级」一并移除**（`database.py` / `schemas/system.py` / `services/settings_service.py` 三处），`GET /settings` 不再返回该字段 | `frontend/src/views/Settings.vue`、`frontend/mock/misc.js` | 09-26 | **前端已完成（09-26，执行人：卢世君）**；**后端侧同步已完成（09-26，胡康宁：三处代码已删，`GET /settings` 字段闭包不再含 `crawl_url`）**：`Settings.vue` 四处（`DIFF_KEYS` / `form` / `applySettings` / 模板「目标地址」项）与 `mock/misc.js` 的 `crawl_url` 已移除（前端已不读不提交，后端接口暂仍返回该字段，无害）；该组标题由「就业网抓取」改为「**信息采集**」（对齐 SRS v1.11 §3.12 措辞），并补一句源清单位置说明 |
| 30 | 项目结构 **§5 第 355 行**注释「前端 VAD 静音断句……（连续静音约 1.2s 切句）」为旧口径，系统设计 v1.14 §5.6 已改为**自适应静音阈值**（语速快 1.6s / 正常 2.65s / 慢 3.6s）——**该行描述前端实现，属前端章节，后端未动** | 项目结构 355 行（前端章节） | 09-26 | **已完成（09-26，执行人：卢世君）**：项目结构 **v1.29** §5 已改为「**按语速自适应**：语速快 1.6s / 正常 2.65s / 慢 3.6s 切句」并补指向系统设计 §5.6；`docs/README.md` 版本清单联动（v1.28 → v1.29） |
| 31 | **前端放开「未保存也能拉」**（IS-27 后端已落地，接口文档 **v1.13**）：`GET /llm-providers/{provider}/models` 新增可选 query 参数 **`api_key`** / **`base_url`**，**传任一即临时探测**（不读缓存、不写缓存、不落库）；不传则行为不变。前端可：①`listModelsApi` 支持传该两参数；②`ProviderCard.vue` 在 Key 输入框有值时带上它、放开「需凭 Key 查询」的前置拦截（`loadModels` 的 `key_set` 判断）。**三点注意**：a. 已保存的配置**不必传参**（传了会绕过 24h 缓存、每次实时拉取）；b. `custom` 未保存时**只传 Key 不够**，还需一并传 `base_url`（否则仍 10001）；c. 传空串等同未传。**另请测试侧**在 AI 配置用例组内补该参数的断言（临时探测可拉取 / 错误 Key → 10013 / 探后不落缓存），编号由测试侧分配 | `frontend/src/api/llmProviders.js`、`frontend/src/components/ProviderCard.vue`、测试计划 | 09-26 | **已完成（09-26，执行人：卢世君）**：①`listModelsApi` 改为对象入参，支持 `apiKey` / `baseUrl` 两个临时探测参数；②`ProviderCard.vue` 的 `loadModels` 在**尚未保存配置**时携带这两个参数、放开 `key_set` 前置拦截，Key 输入框加**失焦即拉**（粘贴 Key 后点别处，列表自动出来）；③**已存配置不传参**（传了会绕过 24h 缓存）——保存后的自动拉取沿用 `force` 路径，同样不传（实测请求 URL 无 `api_key`）；④**测试侧**：测试计划 **v1.17** 新增 **TC-87**（编号由测试侧分配）。**验证 12/12 通过**（CDP 真后端 + 本地 mock 供应商）：失焦后请求带 `api_key` 与 `base_url`、**未保存状态下已能选到模型**、此时状态标签仍为「未配置」、保存后自动拉取不带临时参数、控制台 0 JS 错误 |
| 32 | **陪练页筛选与场景题呈现随题库升级同步**（接口文档 v1.14 §3.8 / SRS v1.12 FR-009）：①筛选面板改为**两步**——先选岗位 / 技术栈、再选领域，选项数据取自新增的 **`GET /practice/meta`**（避免枚举在前端硬编码）；②新增**题型筛选**（问答型 / 选择题 / 场景题）；③**场景题差异化呈现**——卡片标识题型、点评流按新增的 **`dimensions`** section 渲染**四维度分项**（解题框架 / 量化估算 / 取舍权衡 / 兜底降级，各 0~10）+ **总分**（`score`），措辞用「参考框架」而非「标准答案」。抽题请求体新增可选 `stacks` / `qtypes`，`directions` 改为**可选**（不传即不限），三者与关系 | frontend 陪练页（筛选面板 / 场景题卡片 / 点评渲染）、`api/practice.js` | 09-26 | 待处理（**排期已定**：2026-09-26 经讨论确认**不提前做**——该页属**步骤 13**，且点评是 SSE 流式、依赖步骤 11 的基建尚未就绪；届时按接口文档 v1.14 的新契约直接实现即可，无需回头核对此条） |
| 33 | **模拟面试方向选择扩容**：`Direction` 值域由 5 扩为 **19**（18 个领域 + `GENERAL`），前端方向选择项需同步（建议按技术栈分组展示，数据可复用 `GET /practice/meta` 的 `stacks` / `domains`）；接口文档 v1.14 §3.7 已补明 `direction` 可取任一领域值做**专项面试**（不传为 `GENERAL`） | frontend 新建面试页方向选择 | 09-26 | 待处理（**排期已定**：2026-09-26 经讨论确认**不提前做**——面试页属**步骤 15**；届时方向选项按接口文档 v1.14 §3.7 取值，数据源复用 `GET /practice/meta` 的 `stacks` / `domains`，不在前端硬编码 19 个方向） |
| 34 | **测试库由 SQLite 切换至 MySQL**：新建 `job_hunter_test` 库（SQL 见数据库设计 v1.9 §7.1，把库名换掉即可），`backend/tests/conftest.py` 的模块级 `DATABASE_URL` 隔离与逐例 `_reset_database()` 适配 MySQL（SQLite 专有的 WAL / 外键 PRAGMA 分支后端已保留，仅方言为 sqlite 时生效，可平滑过渡）；口径见系统设计 v1.15 第 8 章。**注意**：`backend/tests/` 内容归测试侧，后端未改动。**前置条件**：测试侧本机需已安装并启动 **MySQL 8.0**（此前测试全程走 SQLite 临时库，本机未必装过；连接串密码即执行 §7.1 建库命令时自设的那个；测试不需要 `backend/.env`，`conftest.py` 在模块级自行覆盖 `DATABASE_URL`） | 本机 MySQL 8.0 环境 / 测试库 / `backend/tests/conftest.py` | 09-26 | **已完成（09-26，执行人：卢世君）**：`conftest.py` 已适配——连接串从 `backend/.env` 的 `DATABASE_URL` **派生**（只换库名、凭据沿用，不写死密码；`.env` 缺该键时直接报错并给指引，不静默回退 SQLite）、逐例清库改 **TRUNCATE**（MySQL 的 DDL 是重量级操作，17 表 × 150+ 用例不再逐例 DROP+CREATE）、**题库种子会话内只导一次**（585 题重复 upsert 是最大开销；`question` 表不再被清，断言题库内容的用例读库中已有种子）、收尾由删临时目录改为 `engine.dispose()`。**验证**：`pytest -q` → **153 passed / 0 failed**（连跑两次均绿，125~127s）；**隔离实测**——跑完后开发库 `job_hunter` 仍 **0 张表**（测试完全没碰它）、测试库 `job_hunter_test` 为 17 张表 + 585 题 |
| 35 | **测试镜像与用例随「MySQL 切换 + 题库体系升级」同步**（本轮后端已落地；`pytest` 现状 **7 failed / 145 passed**，失败项全部可归因于测试侧期望镜像未同步、**无实现缺陷**）：①`tests/expected_schema.py`——`question` 表补 `stack`（非空、默认 `COMMON`）/ `rubric`（可空）/ `idx_question_stack` 索引、`qtype` 补 `SCENARIO`；枚举补 `Stack`（5 值）、`Direction` 5 → **19**、`QuestionType` 补 `SCENARIO`；`SYSTEM_CONFIG` **移除 `crawl_url`**（后端三处已删：`database.py` / `schemas/system.py` / `settings_service.py`）；种子口径改 **585 题 / 18 领域**；②测试计划补用例——场景题四维点评与 `dimensions` 解析、抽题 `stacks` / `qtypes` 筛选、`GET /practice/meta`、错题复习的 `SCENARIO` 判定、种子 upsert 幂等与「不删题」断言；③**TC-51** 的「SQLite WAL 模式与外键开关开启」改为 MySQL 口径，表结构断言同步上述列增量 | `backend/tests/expected_schema.py`、测试计划 | 09-26 | **已完成（09-26，执行人：卢世君）**：①`expected_schema.py`——`question` 补 `stack`（非空）/ `rubric`（可空）列与 `idx_question_stack` 索引、`Direction` 5 → **19** 值、新增 `Stack`（5 值）、`QuestionType` 补 `SCENARIO`、`SYSTEM_CONFIG` 移除 `crawl_url`、种子口径改 **585 题 / 5 栈 / 18 领域**；②`test_database_init.py`——SQLite PRAGMA 用例改 **MySQL 口径**（InnoDB + utf8mb4）、种子断言加栈维度、**新增场景题 `rubric` 齐备性断言**、**索引比对改为「只比 `idx_` 前缀」**（MySQL 下唯一约束与外键会自动产生 8 个索引，属实现细节而非设计要求）；③测试计划 **v1.18** 新增 **TC-88~92**（`practice/meta` / 多条件筛选 / `dimensions` 四维分派 / `SCENARIO` 判定 / 种子 upsert 幂等与不删题）、TC-51 三处口径更新，**用例总数 87 → 92**；④**顺带修 `test_llm_providers.py` 的 `/settings` 字段闭包期望**——去掉已废弃的 `crawl_url`（未在台账列出，但同属本轮遗留的失败项）。**验证**：`pytest -q` → **153 passed / 0 failed**，原 7 条失败**全部转绿** |
| 36 | **步骤 9（今日概览·后端）已落地**（2026-09-26）：`GET /overview` 聚合接口已交付——待面试/笔试、投递状态计数、3 天无进展跟进、错题到期数、个人中心 `stats` 三项计数；校招情报四项（`campus_events` / `last_crawl_at` / `top_job_postings` / `match_reminder_count`）的数据源属步骤 21~22，按契约固定返空值；**错题计数非占位**——顶层 `wrong_question_count`（到期未复习数）与 `stats.wrong_question_count`（错题总数）本步均为**真实查询**，二者**语义不同、勿混用**（开发计划已升 v2.15 同步该口径，原文「错题区块留占位」已修正）。**请测试侧**：①执行 **TC-27**（`GET /overview` 用例），并覆盖接口文档 v1.14 §3.4 新增的**区块判定口径**四条（7 日窗口自当前时间起算且已过时点不展示 / 跟进需满 3 天且未安排未来的笔试面试 / 顶层错题数排除已掌握 / 计数含已结束）；②`stats` 三项真实计数可并入 TC-65 验证点（见 #17） | 测试计划 / `backend/tests/` | 09-26 | **已完成（09-26，执行人：卢世君）**：①**TC-27 已执行**——新增 `backend/tests/test_overview.py`（**8 条**），覆盖「区块判定口径」四条**全部**：7 日窗口与升序（排除已结束 / 已过时点 / 超窗口）、跟进满 3 天且**未安排未来笔试面试**、**顶层错题数排除已掌握**、**计数含已结束**；另有鉴权、9 字段恒返回、无记录补 0、`stats` 跨账号隔离、校招四项固定空值。**全量 `pytest` 161 passed / 0 failed**；②`stats` 三项并入 **TC-65** 验证点（见 #17，测试计划 v1.19）；③**真联调亦已完成**——概览页与侧栏待办在真后端下 **12/12 通过**，个人中心显示真实计数。**联调中发现并修复一处前端缺陷**：概览页与侧栏直接输出后端原始时间（换 MySQL 后为 ISO 格式 `…T14:00:00`），已抽公共格式化工具统一为 `MM-DD HH:mm`（详见[前端工作日志](前端工作日志.md)同条目） |
| 37 | **测试用例随「导入模板中文表头」同步**（问题记录 **IS-28** 后端已落地，接口文档 **v1.15**）：`pytest` 现状 **3 failed / 150 passed**——失败项**全部是文案断言、无逻辑回归**：①`test_import_csv_partial_rows` 第 6 行非法状态的期望文案（原 `状态值非法（可选：APPLIED/WRITTEN/INTERVIEW/OFFER/CLOSED）` → 现 `状态值非法（可选：已投递、待笔试、面试中、已获 offer、已结束，或对应英文枚举）`）；②`test_import_missing_required_header` 断言 `"position" in message` → 报错改中文列名「岗位」；③`test_import_missing_multiple_required_headers` 同理（`company` → 「公司」）。**另建议补用例**（编号由测试侧分配）：中文表头导入成功、**示例行自动跳过**（上传未改动的模板 → 0 条 0 错误）、状态列填**中文标签**可导入、`GET /applications/template` 表头为中文且含示例行；**英文旧表头兼容已被既有用例覆盖**，无需另补 | 测试计划 / `backend/tests/test_applications.py` | 09-26 | 待处理（**后端已就绪**，`backend/tests/` 内容归测试侧，后端未改动） |
| 38 | **步骤 10（LLM 客户端基建·后端）已落地**（系统设计 **v1.16** §5.4）：请测试侧按实际实现写入 **TC-50**，并**订正测试计划 §1.3 的三处命名**——生产实现落名为 **`LLMClient`**（抽象基类）/ **`OpenAICompatibleClient`**（生产实现）/ **`get_llm_client`**（注入点），测试替身建议命名 **`FakeLLMClient`**（均不叫 `LLMProvider` / `get_llm_provider` / `FakeLLMProvider`），理由：原命名与**探测模块 `llm_provider.py`**（12 家注册表 + `test_connection` / `list_models`）撞名，两模块职责不同（对话 vs 探测），命名须能区分。**可直接据以写用例的实现事实**：①注入点 = `app.dependency_overrides[get_llm_client]`（模块级单例，替身实现 `stream_chat` / `chat_json` 两个方法即可）；②签名 = `stream_chat(config, messages, tools=None) -> Iterator[str]` 逐块 yield 文本增量、`chat_json(config, messages) -> dict`（容忍 ```json 围栏与前后解释文字，解析失败抛 10011）；③`config` 为 frozen dataclass `LLMConfig`（provider / base_url / api_key / model）；④`resolve_config(db, user_id)` 可独立单测——账号生效行（解密 Key）→ `.env` 兜底 → 都无抛 **10012**，**生效行存在但不完整时不走 .env 兜底**；⑤异常统一抛 **`LLMError`**（继承 `BizException`，`code` 属性即 `ErrorCode`，可直接断言）；⑥重试口径 = 连接错误 / 超时 / 服务端 5xx **重试 1 次**，429 与鉴权错**不重试**，流式**只重试建流阶段** | 测试计划 §1.3 / TC-50 | 09-26 | 待处理（**后端已就绪**：自测 26/26 通过；`backend/tests/` 内容归测试侧，后端未改动） |
| 39 | **问题记录 IS-02 的剩余依赖已满足**（步骤 10 已交付，2026-09-26）：`llm_client.py` 提供抽象基类 **`LLMClient`** 与依赖注入点 **`get_llm_client`**（模块级单例，测试用 `app.dependency_overrides[get_llm_client]` 替换）。IS-02 正文与「处理」栏写「剩余部分依赖步骤 10」，该前置现已就绪——请测试侧在 `backend/tests/conftest.py` 补 **LLM 对话替身 fixture**（建议命名 `FakeLLMClient`，实现 `stream_chat` / `chat_json` 两个方法即可，签名与断言要点见 #38）并更新 IS-02 的状态栏。**注**：`backend/tests/` 内容归测试侧，后端未改动；后端侧已在问题记录状态统计注明「依赖步骤 10 已交付」 | 问题记录 IS-02 / `backend/tests/conftest.py` | 09-26 | 待处理（**后端已就绪**） |
| 40 | **校招情报界面分离约定已写为设计文档**（问题记录 **IS-29 / IS-30** 后端侧已闭环）：系统设计 **v1.17** 新增 **§4.8 校招情报界面设计**——五条分离约定（招聘信息的家只在校招情报页、投递页仅保留「从岗位预填」入口 / 与投递管理视觉语言分离 / 岗位信息状态不复用投递状态色 / 已投递岗位沉底或弱化 / 概览页只放入口不展开）+ 空态口径（以「是否真的什么都看不到」为准，文案分「全空」与「仅无投递」两场景）+ 模块色 `--m-campus: #93AE55`；**§4.4 菜单分组表与 §4.5.1 色彩表两处「待登记」已回写完毕**（黄绿 / `#93AE55`）。前端实现时以该节为准，开发计划步骤 23 任务行已补指引 | 系统设计 §4.8 / 开发计划步骤 23 | 09-26 | 待处理（实现随**步骤 23**） |
| 41 | **两处测试文件的文件头注释含本地过程目录路径**（属 AI 工具痕迹，非功能问题）：`backend/tests/test_database_init.py:7` 的「设计依据：docs/superpowers/specs/2026-09-24-步骤2建表冒烟测试-design.md」与 `test_applications.py:400` 的「（设计依据：docs/superpowers/specs/2026-09-24-步骤3投递服务层测试-design.md）」。该目录**不入库**（`.gitignore` 已忽略），路径对读者无意义，建议改为中性表述（如「设计依据见设计稿（过程文档，不入库）」）或直接删除路径。**属 `backend/tests/` 内容，后端未改动。** 另：本轮对测试 / 前端侧维护的文档（前端工作日志 / 测试日志 / 测试计划 / 问题记录）做了元叙述表述清理（「用户提出 / 用户原话 / 用户裁决」等改为「试用中提出 / 经讨论确定」一类中性写法），**只改表述、未动任何内容与结论**，已事先取得同意，一并知悉 | `backend/tests/test_database_init.py`、`test_applications.py` | 09-26 | 待处理（表述清理，不涉功能） |
