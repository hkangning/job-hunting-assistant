# 个人求职助手 —— 前端工作日志

| 项 | 内容 |
|---|---|
| 文档类型 | 工作日志（过程记录，**非开发依据文档**） |
| 维护人 | 卢世君 |
| 覆盖范围 | 开发计划步骤 1 起，按步骤追加 |
| 最后更新 | 2026-09-26 |
| 上游依据 | 开发计划 / 系统设计文档 / 项目结构 / 测试计划 |

## 维护约定

1. **一份记录对应一个开发步骤**，该步骤完成后追加，**不改写历史条目**；
2. 每节固定五栏：日期 / 工作内容 / 产出 / 验证 / 决策与遗留；
3. **验证栏只写实际执行过的命令与结果**，不写"应该没问题"；手工验收需留存截图或 DOM 断言输出（开发计划质量基线 ③）；
4. 本文件位于 `docs/07-工作日志/`，属**公共文件**，改后需与 `backend` 分支互相拉取同步（`git fetch origin && git checkout origin/backend -- docs README.md .gitignore`，冲突以后端最新版为准）。

---

## 步骤 1：工程骨架（前端）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-24 |
| 对应分支 | `frontend` |
| 对应提交 | `c427479 初始骨架搭建，更改页面布局样式设计` |
| 上游依据 | 开发计划步骤 1 / 项目结构文档第 3 节 / 系统设计文档 4.1~4.3 |

### 工作内容

按开发计划步骤 1 分给前端的清单执行：

- 初始化 Vite + Vue 3 工程（**JavaScript 而非 TypeScript**，依据项目结构文档第 3 节全部标注 `.js`）；
- `vite.config.js`：端口 5173（与后端 CORS 白名单一致），`/api` 代理至 `http://localhost:8000`，**不做 rewrite**（后端路径本身即 `/api/v1/*`）；
- 路由表 9 条：8 个菜单页 + 1 个隐藏页（`/interview/:sessionId`，不进菜单）；
- 布局三件套：`App.vue`（容器骨架 + 折叠状态）、`components/SideNav.vue`（菜单渲染 + 路由高亮）、`components/AppHeader.vue`（折叠按钮 + 页面标题 + `actions` 插槽）；
- `src/api/request.js`：axios 实例（`baseURL = /api/v1`）+ 统一响应体拦截——成功分支解包 `response.data.data`（调用方直接拿业务数据），失败分支按「超时 / 有响应体 / 无响应」三类提示并透传业务错误码；
- 9 个占位页 `src/views/*.vue`，统一 `el-empty` 占位并标注对应开发步骤；
- `main.js` 挂载顺序：Pinia → Router → Element Plus（全量引入 + `zh-cn` locale）。

### 产出

| 项 | 位置 |
|---|---|
| 工程配置 | `frontend/package.json`、`package-lock.json`、`vite.config.js`、`index.html` |
| 布局与导航 | `src/App.vue`、`src/components/SideNav.vue`、`src/components/AppHeader.vue` |
| 请求层 | `src/api/request.js` |
| 路由表 | `src/router/index.js` |
| 页面占位 | `src/views/` × 9 |

### 验证

| # | 验证项 | 方式 | 结果 |
|---|---|---|---|
| 1 | 依赖安装 | `npm install` | 通过，生成 `package-lock.json`（提交入库以锁定依赖） |
| 2 | 构建正确性 | `npm run build` | 通过，无 error |
| 3 | 前端启动 | `npm run dev` | 5173 端口启动无报错 |
| 4 | 9 路由可导航 | 手工逐条访问浏览器 | 全部渲染正常、无控制台报错 |
| 5 | 菜单高亮 | 访问 `/interview/123` | 「模拟面试」保持高亮（`meta.activeMenu` 生效） |
| 6 | 折叠交互 | 手工点击 ☰ | 210px ⇄ 64px 正常切换，图标与文案正确 |
| 7 | 请求全链路 | 概览页「后端连接状态」卡片 | 显示「已连接」；`/health` 响应体 `{code:0, message:"ok", data:{...}}` 正确解包 |
| 8 | 错误提示链路 | 停掉后端刷新概览页 | 出现「网络异常」提示，页面不白屏 |

> 验证项 4、5 已留存截图（对应开发计划质量基线 ③「手工验收有记录」）。

### 决策与遗留

| 决策 | 理由 |
|---|---|
| 不建 `src/stores/` 目录 | 本步无任何真实 store 需求（设置项→步骤 7、概览轮询→步骤 5、悬浮球→步骤 16）；`main.js` 照常挂载 Pinia 为后续铺路 |
| 不建业务接口文件（`api/applications.js` 等） | 随各业务步骤交付，避免创建空文件 |
| 引入 `@element-plus/icons-vue` | 项目结构文档依赖清单未列此包，但**折叠侧边栏**是本步既定形态——折叠后菜单只剩图标位，无图标则侧边栏成为空白窄条。按需引入，不做全局注册 |
| 不建 `public/` 目录 | `index.html` 的 favicon 用内联 SVG data URI，避免新增文档外目录 |
| 概览页保留后端连接状态卡片 | 步骤 1 验收要求「`/health` 通」，这是验证 `request.js` 全链路（axios → baseURL → Vite 代理 → 拦截器 → 后端响应体）的唯一手段；步骤 5 实现真正概览页时在此基础上扩展，非废弃代码 |

**遗留**：`tests/conftest.py` 的完整版（独立测试库 + `FakeLLMProvider`）依赖步骤 2 / 6 的模块，本步无法完成——该欠账在[测试日志](测试日志.md)中跟踪。**部分闭环**：独立测试库部分已于步骤 2 完成；`FakeLLMProvider` 与依赖注入点按重排后的计划转入**步骤 10**（见测试日志文末索引），**仍未开始**。

---

## 视觉改版（系统设计文档 4.4 / 4.5 对齐）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-24 |
| 性质 | **非开发计划步骤**，属系统设计文档 v1.2 新增章节（4.4 全局布局 / 4.5 视觉设计系统）的代码同步 |
| 对应分支 | `frontend` |

### 工作内容

- **新增 `src/styles/tokens.css`**：色彩 / 间距 / 圆角 / 布局尺寸全部收敛为 CSS 变量，并在其上覆盖 Element Plus 主题变量（**只改视觉变量，不改组件行为**）。派生色按 Element Plus 的 `mix(white, 主色, n/10)` 算法计算，保证与其内部组件配色自洽；
- **品牌与模块色定稿**：主色品牌紫 `#6B4E9E`；8 个模块各有模块色；投递 5 个状态色对齐数据库设计文档第 5 章枚举；
- **路由表 `meta` 扩展**：新增 `group`（菜单分组，侧栏按此分组）与 `color`（模块色），移除 8 个 Element Plus 图标的 import（菜单改用模块色点）；
- **`request.js` 增加 `silent` 选项**：辅助性请求失败时不自动弹提示，由调用方降级；
- **`SideNav.vue` 重写**：品牌区 + 三组菜单（求职流程 / 能力提升 / 系统）+ 模块色点 + 底部「今日待办」；折叠态下组标题收为细分隔线；
- **`AppHeader.vue`**：折叠按钮 + 当前页标题 + `actions` 具名插槽 + 提醒入口 + 「＋ 新增投递」高频操作；
- **`App.vue`**：侧栏宽度 210px → **230px**（设计文档 4.4 新规格），内容区 padding 统一走 token；
- **9 个页面去掉与顶栏重复的 `<h2>`**（顶栏已渲染 `meta.title`，页内再渲染一次即同屏两个相同标题）。

### 验证

| # | 验证项 | 方式 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 每个任务后各跑一次，均通过 |
| 2 | 页面无重复标题 | 无头浏览器逐 9 条路由断言 `<h2>` 计数 | 每条均为 **0** |
| 3 | 主题色生效 | 无头浏览器 DOM 断言 + 截图 | 主色为品牌紫 `#6B4E9E`，非 Element Plus 默认蓝 `#409EFF` |
| 4 | 折叠交互 | CDP 驱动真实点击断言计算样式 | 230px ⇄ 64px；折叠态 `.el-menu` 含 `el-menu--collapse`；组标题高度 `1px`；待办区块不在 DOM（`v-if` 生效） |
| 5 | 截图留存 | 无头浏览器 1440×900 | 已留存，对照步骤 1 旧截图确认四项变化 |

> 前端无单元测试框架（步骤 1 已确立，本步不新增），验证以 `npm run build` + 无头浏览器 DOM 断言与截图为准。

### 决策与遗留

- **侧栏「今日待办」在 `GET /overview` 就绪前保持隐藏**。该接口属开发计划**步骤 9**（2026-09-25 计划重排前的旧编号为「步骤 5」），后端尚未实现；此处「加载失败整块隐藏」是设计文档 4.4 明确要求的行为，**不是缺陷**，已按设计内行为记入[问题记录](问题记录.md) **IS-06**（🚫 不处理），步骤 9 完成后自动生效；
- ~~「＋ 新增投递」暂跳转 `/applications`，真正的录入弹窗由步骤 4 提供；~~ **已完成**——步骤 4 交付 `ApplicationFormDialog.vue`，顶栏按钮改为跳 `/applications?action=new` 并即时清 query（见下方步骤 4 条目）；
- 同步更新**项目结构文档 v1.5**，登记 `src/styles/tokens.css`、`components/SideNav.vue`、`components/AppHeader.vue` 三项，使目录树与实际一致。

---

## 依赖版本核查与上游文档口径同步

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-24 |
| 性质 | **非开发计划步骤**，属文档口径修正（SRS 2.4 / 系统设计 2.2 / 项目结构第 5 节） |
| 对应分支 | `frontend` |

### 工作内容

核查项目组件版本是否满足文档要求。**结论：全部达标，无需升级**；核查中查出两处文档缺陷，本次一并修正。

核查结果：

| 项 | 文档要求（出处） | 本机实际 | 结论 |
|---|---|---|---|
| Node | `^20.19.0 \|\| >=22.12.0`（项目结构第 3 节） | v24.11.0 | ✅ |
| npm | ≥ 11（同上） | 11.6.1 | ✅ |
| Python | 3.11+（SRS 2.4） | 3.13.13 | ✅ |

- **前端 8 个包**：`package.json` 声明版本与 `node_modules` 实际安装逐条一致；源码引用的外部包（`vue` / `vue-router` / `pinia` / `element-plus` / `element-plus/es/locale/lang/zh-cn` / `@element-plus/icons-vue` / `axios`）全部已安装，无缺件；`npm ls --all` 报的 UNMET 全是 `OPTIONAL DEPENDENCY`（sass / less / typescript 等可选增强），非必需 peer 缺失；
- **后端 13 条**：`.venv` 实际版本与 `requirements.txt` 固定版本逐条吻合；
- **`npm outdated`**：仅 `vite` 有补丁可升（8.3.0 → 8.3.1），其余包当前版本即 registry 最新版。

修正的两处文档缺陷：

1. **上游 Node 版本口径滞后**：SRS 2.4 与系统设计 2.2 均写 `Node 18+`，与实际工具链矛盾——Vite 8 要求 `^20.19.0 || >=22.12.0`，按 `Node 18+` 的说法**根本装不上本工程**。改为与项目结构文档第 3 节同款口径；
2. **前端依赖清单漏项**：项目结构文档第 5 节未登记 `@element-plus/icons-vue`，而 `src/components/AppHeader.vue` 实际在用（Fold / Expand / Bell）——步骤 1 日志决策栏已说明是"按需引入、不全局注册"，本次补登入册。

**依赖文件一字未改**：`package.json` / `package-lock.json` / `requirements.txt` 版本既已全达标，无谓改动只会给协作者制造锁文件合并冲突。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| SRS | `docs/01-需求文档/需求规格说明书.md` | v1.1 → **v1.2**（2.4 表 Node 口径） |
| 系统设计文档 | `docs/02-设计文档/系统设计文档.md` | v1.2 → **v1.3**（2.2 表 Node 约束） |
| 项目结构文档 | `docs/02-设计文档/项目结构.md` | v1.5 → **v1.6**（第 5 节补 `@element-plus/icons-vue`） |

### 验证

| # | 验证项 | 方式 | 结果 |
|---|---|---|---|
| 1 | 版本满足性 | `node -v` / `npm -v` / `python -V` 对照文档逐条比对 | 三项全部满足 |
| 2 | 前端依赖完整性 | `npm ls --depth=0`、`npm ls --all` 筛查 invalid / missing | 8 包齐全，无 invalid，无必需 peer 缺失 |
| 3 | 前端依赖新旧度 | `npm outdated` | 仅 `vite` 一个补丁位 |
| 4 | 后端依赖一致性 | `.venv/Scripts/python.exe -m pip list` 对照 `requirements.txt` | 13 条逐条吻合 |
| 5 | 源码引用闭包 | 提取 `src/` 全部 `from '...'` 外部包名，比对 `node_modules` | 无未安装引用 |
| 6 | 改前基线 | `git diff --stat HEAD origin/backend -- <三份文档>` | 三份在两条分支上 diff 为空，不存在基于旧版改写 |

### 决策与遗留

- **`vite` 8.3.0 → 8.3.1 补丁本次不升**：本次只做文档口径同步，锁文件不动；补丁留待下次功能性改动时顺带升级；
- **`npm ≥ 11` 未写入 SRS / 系统设计**：那两层写技术栈，npm 版本属工具链细节，项目结构文档第 3 节已覆盖；
- **历史设计稿 / 计划（过程文档，不入库）不追改**：属已完成步骤的过程存档，其中一份引用的「Node 18+」保留原样；
- ~~**公共文件同步欠账**：本次改动的三份文档均为公共文件，按约定需与 `backend` 分支互相拉取同步；且三份**维护人均为胡康宁，改动需知会对方**。~~ **已消解**（2026-09-26 核实：三份文档均已推进到后端续写过的版本——SRS v1.10、系统设计 v1.12、项目结构 v1.21，两分支版本合流）；

---

## npm 工具链升级与 IS-14 收尾

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-24 |
| 性质 | **非开发计划步骤**，属工具链升级 + 问题记录收尾（含一起合并事故修复） |
| 对应分支 | `frontend` |
| 对应提交 | 待提交（改动在工作区） |
| 上游依据 | 后端合并提交 `5ded396` 携带的 IS-14 遗留动作 / 问题记录 IS-13、IS-14 |

### 工作内容

**一、npm 升级（执行 IS-14 的遗留动作）**

后端侧 IS-14 处置留下的动作是：前端/测试侧将 npm 升至 ≥ 11.12.1。本次实际升至 **12.1.0**（当前 latest）。

执行中被 Node 版本拦下一次——**npm 12.1.0 自身要求 Node `^22.22.2 || ^24.15.0 || >=26.0.0`，本机 Node v24.11.0 不满足**（24.11 < 24.15），`npm install -g npm@12.1.0` 直接报 `EBADENGINE`。故拆成两步：

1. **先升 Node**：取 24.x 最新 `24.21.0`，官方 MSI 经 SHA256 校验后安装；用 `INSTALLDIR="D:\Program Files\nodejs\"` 显式沿用原安装目录（不指定会装到 `C:\Program Files\nodejs`，与现有 PATH 失配）；
2. **再升 npm**：`npm install -g npm@12.1.0` 通过。

**二、合并事故修复（`问题记录.md`）**

核查中发现该文件在 `5ded396` 合并后处于自相矛盾状态：

| 症状 | 成因 |
|---|---|
| IS-14 索引行指向 `_cell_text`、条目段却是 npm | 两分支**独立占用了同一编号 14**（前端：`_cell_text` 死代码；后端：npm 锁文件）；合并在索引表保留了前端行、在条目段与统计表采用了后端版 |
| 前端 `_cell_text` 条目段**整段丢失**、索引行锚点悬空 | 同上，其条目段被后端 IS-14 覆盖 |
| IS-13 索引行写 ✅、条目段与统计表写 ⏳ | 索引行取自前端（测试侧知悉后已改 ✅），条目段与统计表取自后端（改动前的 ⏳） |

处置：按维护约定「冲突时一律以后端最新版为准」（项目结构文档第 1 节），`IS-14` 归 npm 条，`_cell_text` **重编为 IS-15 并恢复条目段**（原文逐字取自合并前的 `79694b1`，未改写）；IS-13 状态按测试日志记为 ✅ 已解决；状态统计由 10/3/1 更新为 **12/1/2**。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 问题记录 | `docs/07-工作日志/问题记录.md` | IS-14 状态 ⏳ → ✅ 并补收尾实测；恢复 IS-15 条目段；IS-13 ⏳ → ✅；统计表 10/3/1 → 12/1/2 |
| 测试日志 | `docs/07-工作日志/测试日志.md` | 步骤 3 两处 `IS-14` 引用改指 `IS-15` 并标注重编 |
| README | `README.md` | 「前端」节新增「升 npm 前先确认 Node 版本」一条——原文 `npm install -g npm@latest` 在本机 Node 下会失败 |
| 项目结构文档 | `docs/02-设计文档/项目结构.md` | v1.7 → **v1.8**（第 3 节补 npm 12.x 的 Node 前提） |
| 文档清单 | `docs/README.md` | 项目结构版本号 v1.7 → **v1.8**（与上一条同步，属 IS-07 同类滞后） |

**未改**：`frontend/package.json`（`engines.npm` 仍为 `>=11.12.1`）、`frontend/.npmrc`、`frontend/package-lock.json`。

### 验证

| # | 验证项 | 命令 / 方式 | 结果 |
|---|---|---|---|
| 1 | 安装包完整性 | `sha256sum` 对比 nodejs.org `SHASUMS256.txt` | `bb0eaee1…2cac` 逐字符一致 |
| 2 | Node 升级 | `node -v` | **v24.21.0**（原 v24.11.0） |
| 3 | npm 升级 | `npm -v` | **12.1.0**（原 11.6.1） |
| 4 | 全局包未丢 | `npm ls -g --depth=0` | `@anthropic-ai/claude-code@2.1.153`、`docx@9.6.1` 均在 |
| 5 | 引擎门槛放行 | `frontend/` 下 `npm install` | `up to date, audited 120 packages`，**无 `EBADENGINE`** |
| 6 | **锁文件零改动**（IS-14 核心验收） | `git status --short` + `git diff --stat HEAD -- frontend/package-lock.json` | 无差异；标记数 1 `dev` / 0 `peer`，与升级前一致 |
| 7 | 前端工具链可用 | `frontend/` 下 `npm run build` | `✓ built in 837ms` |
| 8 | 后端回归未受影响 | `backend/` 下 `python -m pytest -q` | **66 passed** in 4.07s；`app.db` 时间戳 `20:52` 未被触碰 |

### 决策与遗留

- **`engines.npm` 维持 `>=11.12.1`，不上调至 12.1.0**：门槛的目的是防锁文件互改，而 11.12.1 与 12.1.0 实测产出一致，取保守下界可让后端侧（11.12.1）无需任何升级即通过；若上调，等于变相要求后端侧把 Node 也升至 ≥ 22.22.2，属跨侧约定变更，须先知会；
- **编号重编需知会后端侧**：IS-15 的重编与恢复、IS-13 状态改 ✅、统计表 12/1/2，三处均需后端拉取同步——`问题记录.md` 维护人为「胡康宁 / 卢世君」，属公共文件；
- ~~**本次 5 份改动均为公共文件**（README / 项目结构 / docs README 清单 / 问题记录 / 测试日志）：按约定需与 `backend` 分支互相拉取同步，冲突以后端最新版为准；~~ **已消解**（2026-09-26 核实：两分支已互相合并，`frontend` 提交已全部推送）；
- **本机 Node 被覆盖安装**：`D:\Program Files\nodejs` 由 24.11.0 → 24.21.0（同主版本内小版本）；项目 `engines.node` 为 `^20.19.0 || >=22.12.0`，仍满足；
- **安装包与构建产物已清理**：`%TEMP%\node-upgrade\`（MSI 31.7 MB + 安装日志 9.4 MB）、`frontend/dist/` 已删。

---

## 步骤 4：投递管理（前端联调）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-24 |
| 对应分支 | `frontend` |
| 上游依据 | 开发计划 v2.2 步骤 4 / 接口文档 v1.1 §3.3 / 系统设计文档 v1.3 §4.1~4.5 / 项目结构文档 v1.9 |
| 设计依据 | 设计稿（过程文档，不入库） |

### 工作内容

**一、依赖引入（含一处实测排雷）**

新增 `vuedraggable` 与 `echarts` 两项依赖。两个坑：

- **装 vuedraggable 必须带 `@next`**：npm 的 `latest` 标签指向 2.24.3（Vue 2 版），直接 `npm i vuedraggable` 在本工程里装了个用不了的包；
- **vuedraggable 4.1.0 的入口是 UMD**：查证到其 `main` 与 `module` 字段**都**指向 `dist/vuedraggable.umd.js`，无 ESM 产物，社区有在 Vite 下报 `does not provide an export named 'default'` 的先例。**设计阶段做了三项实测排雷**（安装 → `npm run build` → Edge 无头渲染），确认本工程（Vite 8 + Vue 3.5.43）可用，故按试用选型继续。用法上必须写 `item-key` 与 `#item` 具名插槽。

**二、状态常量收敛为单一出处**

新增 `src/constants/application.js`：5 个状态的中文名、色值、合法流转表（含 `canTransit`）。同一份状态数据在看板列头与拖拽校验、表单状态展示、卡片状态点三处要用，散着写必然漂移。合法流转表与后端 `application_service.LEGAL_TRANSITIONS` 逐条对齐（SRS FR-004）。

**三、请求层补一处必要透传**

`request.js` 错误分支原先只透传 `code` / `httpStatus`，而导入「全行非法」时后端返回 **HTTP 400 + code=20002 + `data.errors` 行级清单**，前端拿不到清单就无法展示「哪一行错、为什么错」——而错误行展示正是本步的交付内容。补 `bizError.data = error.response.data.data` 一行。

**四、四个组件 + 页面容器**

页面 [Applications.vue](../../frontend/src/views/Applications.vue) 是唯一的数据持有者，四个子组件都是受控展示层（不直接调接口）：

| 组件 | 职责 | 关键设计 |
|---|---|---|
| `StatusKanban.vue` | 5 列看板 + 列间拖拽 | 见下方拖拽设计 |
| `ApplicationFormDialog.vue` | 新增/编辑表单 | 编辑时**状态只读**——接口文档 §3.3 明确 PUT 不改 status，放开会诱导用户改了不生效 |
| `ApplicationImportDialog.vue` | 模板下载/上传/结果展示 | 前端预校验（扩展名/≤2MB/非空）与后端限制一致，不合格不发请求 |
| `TrendChart.vue` | ECharts 折线趋势图 | 受控组件，数据由页面请求后传入；展开时才加载（懒加载） |

**五、拖拽合法性做双层校验**

- **前端预判**：`group.put` 读目标列与拖拽卡片各自的 `data-status` 查 `canTransit`，**非法落点直接拖不动**（sortablejs 拒绝），而不是拖过去再弹回——后者有视觉跳跃且说不清原因；
- **后端兜底**：被接受的拖拽走 `PATCH /applications/{id}/status`，失败则重新拉取列表以服务端数据为准；
- **终态保护**：`OFFER` / `CLOSED` 两列 `pull: false`，卡片出不去；`sort: false` 禁止列内排序（列内顺序由接口的 `applied_at DESC` 决定，允许拖动会导致刷新后顺序跳回）。

**六、顶栏与页面双向连通**

- **页面 → 顶栏**：`AppHeader` 的 actions 区加 Teleport 目标容器，投递页用 `<Teleport>` 注入「批量导入」按钮（设计文档 §4.4「页面级操作走 AppHeader 插槽」的落地方式，不引入状态库）；
- **顶栏 → 页面**：「＋ 新增投递」改为跳 `/applications?action=new`，页面 `watch` 该 query 打开表单后**立即 `router.replace` 清掉 query**——不清的话再次点击因导航目标相同而不触发，这是 query 方案的必要收尾。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 依赖 | `frontend/package.json` / `package-lock.json` | 新增 `vuedraggable@^4.1.0`、`echarts@^6.1.0` |
| 常量层 | `src/constants/application.js` | 新增 |
| 接口层 | `src/api/applications.js` | 新增（9 个方法） |
| 请求层 | `src/api/request.js` | 错误分支补透传 `data`（1 行） |
| 组件 | `src/components/StatusKanban.vue`、`ApplicationFormDialog.vue`、`ApplicationImportDialog.vue`、`TrendChart.vue` | 4 个新增 |
| 顶栏 | `src/components/AppHeader.vue` | 加 Teleport 容器 + 新增按钮改跳 query |
| 页面 | `src/views/Applications.vue` | 占位 → 完整实现 |
| 文档 | `docs/02-设计文档/项目结构.md` | v1.8 → **v1.9**（§5 依赖补 2 项、§3 目录树登记 4 个新文件；**顺带修正头部版本号滞后**：v1.8 条目加入时漏改头部，头部停在 v1.7） |
| 文档 | `docs/README.md` | 项目结构版本号 v1.8 → v1.9 |

> `TrendChart.vue` 内的图表色值写成字面值（`#3B7BB5` 等）而非 CSS 变量：ECharts 在 canvas 上绘制，**读不到 CSS 变量**。这是设计文档「色值收敛在 tokens.css」的唯一例外，已在代码注释中逐个标注出处（设计文档 §4.5.1）。

### 验证

| # | 验证项 | 命令 / 方式 | 结果 |
|---|---|---|---|
| 1 | 状态机纯函数 | 临时 `verify-constants.mjs` 跑 14 条断言（6 条合法边 + 8 条非法边，含终态封闭性） | **ALL 14 CASES PASS** |
| 2 | 组件编译 | `vue/compiler-sfc` 编译 4 个新增组件（未被路由引用时 build 不会处理它们） | **4 个组件全部编译通过** |
| 3 | 构建 | `npm run build` | **✓ built in 823ms**；`Applications` chunk 688.66 kB（gzip 235.03 kB，含 echarts + vuedraggable） |
| 4 | 端到端（主流程） | Edge 无头 + CDP 驱动真实点击/输入：看板渲染 / 顶栏注入 / 新增表单提交 / 趋势图 canvas / 流转持久化 / 控制台无报错 | **14/14 PASS** |
| 5 | 端到端（对账与导入） | 同上：趋势图今日计数与看板卡片数对账；真实文件上传（CDP `DOM.setFileInputFiles`）验证成功数 + 错误行清单 | **7/7 PASS**（对账：趋势 1 = 看板 1；错误行 = 第 3 行「岗位名称不能为空」） |
| 6 | 端到端（编辑与删除） | 同上：编辑对话框字段回填 + 状态只读；删除二次确认（取消保留 / 确认删除） | **7/7 PASS** |
| 7 | 后端回归 | `cd backend && .venv/Scripts/python.exe -m pytest` | **66 passed** in 4.18s |

共 **28 项**浏览器端断言全部通过，截图 8 张（看板 / 新增表单 / 趋势图 / 流转后 / 趋势对账 / 导入结果 / 编辑对话框 / 删除确认），留存于系统临时目录 `apps-verify/artifacts`（临时目录，验收后清理）。

> **未覆盖面（如实记录）**：**拖拽手势本身未自动化**。sortablejs 用原生 HTML5 拖拽，CDP 需 `Input.setInterceptDrags` + 实验性 `dispatchDragEvent` 才能驱动，稳定性未验证，故未纳入脚本。自动化覆盖到的是：合法性判定（14 条纯函数断言）、流转落库与刷新持久化（第 4 组）、后端状态机（TC-02）。**请在浏览器里实际拖一次卡片确认手势**（合法落点成功、非法落点拖不动）。

### 决策与遗留

- **一处文档口径偏差（待步骤 21 终检）**：SRS FR-003 写批量导入为「解析后**预览 → 确认导入**」，但接口文档 §3.3 的 `POST /applications/import` 是**直接入库**、无预览端点。本次按接口文档执行（后端是唯一契约），前端以「选择文件 + 显式点『开始导入』」作为确认动作。两者口径不一致，终检时需统一（改 SRS 或增接口）；
- ~~**看板一次最多显示 50 条**：接口 `page_size` 上限 50（接口文档 §1.1），超出时看板顶部提示「共 N 条，看板显示最近 50 条」，未做「加载更多」（本步无此约定）；~~ **已改进**——第一轮试用迭代改为**循环翻页拉全**（封顶 500 条并提示），见下方「步骤 4 迭代：试用反馈修订」二；
- **拖拽后列内不重排**：流转成功后仅更新该卡片状态，列内顺序仍是上次拉取的顺序，刷新后按 `applied_at DESC` 恢复；
- **验证脚本已清理**：临时脚本与 Edge profile 在验收后删除；`frontend/dist/` 构建产物亦已清理；
- ~~**公共文件同步欠账**：本文件、[测试日志](测试日志.md)、[项目结构文档](../02-设计文档/项目结构.md)、`docs/README.md` 均为公共文件，需与 `origin/backend` 互相拉取同步（冲突以后端最新版为准）；其中项目结构文档维护人为胡康宁，改动需知会。~~ **已消解**（2026-09-26 核实：项目结构 v1.9 之后已有后端续写的 v1.18/v1.19，两分支版本合流）；

---

## 步骤 4 迭代：试用反馈修订

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-24 |
| 性质 | 步骤 4 交付后的**试用迭代**（非新步骤），其中一轮属**需求变更**并已回改文档 |
| 对应分支 | `frontend` |
| 上游依据 | 试用反馈 / SRS **v1.4** / 数据库设计 **v1.2** / 接口文档 **v1.2** / 测试计划 **v1.5** |

### 工作内容

七轮修订，前五轮为体验迭代，第六轮为需求变更，第七轮为过程中修掉的两个真 bug。

**一、拖拽失效（功能缺陷）**

根因：`canDropTo` 读 `to.el.dataset.status`，而 `to.el` 是 sortablejs 挂载的**列表元素** `.kanban__list`，`data-status` 却绑在父级列容器 `.kanban__col` 上 → 取到 `undefined` → `canTransit(状态, undefined)` **恒为 false** → 所有落点被静默拒绝。修复：属性改绑 draggable 根元素，并以 `closest('[data-status]')` 兜底。

**二、页面改版（布局与信息密度）**

试用反馈"不能一眼看全""一半多页面是空白"。实测取证：看板 `scrollWidth 1336 > clientWidth 1136`（第 5 列被截断）、看板底部距内容区底 **362px 全空**、列宽固定 256px × 5 排不下。改版：

- 列宽等分自适应（`flex: 1 1 0`），**消除横向滚动**；看板 `flex: 1` 撑满剩余高度，列内滚动；
- 新增**统计条**：总数 / 本周新增 + 状态占比堆叠条 + 计数图例（指标全部由已拉取数据现算，不新增接口）；
- 卡片改高密度排版，删除图标改 hover 显示，空列改矮虚线框；
- 数据量策略：**循环翻页拉全**（接口 `page_size` 上限 50，封顶 500 条并提示），列内滚动。

**三、配色调整（全站）**

试用反馈"不优美、太严肃"。定位：原配色（品牌紫 `#6B4E9E` + 深蓝 / 暗琥珀 / 墨绿）是"作战面板"调性，而**求职本身是压力源，界面不该加重紧张感**。改为提亮降饱和的清爽蓝紫：品牌 `#7A6BC4`、已投递 `#5B8DD9`、面试中 `#E0A030`、offer `#4CB782`、页面底 `#F7F8FC`。**不改色相**——蓝=起点、紫=笔试、琥珀=进行中、绿=成功、灰=结束这套语义本身是对的。

**四、卡片精简（消除截断）**

试用反馈"卡片信息显示不完整"。根因：**三个字段挤在一行**（"Java 后端开发 · 南京 · BO⋯"）——既没显示全、又占地方，是最糟的组合。改为一行一个信息：公司 / 岗位 / 投递天数 + 排期；城市 · 渠道 · 薪资 · 备注移到详情（点卡片）。卡片左侧色条改按**该列状态色**着色。

**五、拖拽体验：反馈与防误触**

| 反馈 | 处理 |
|---|---|
| 拖错了还没确认就转移了 | 加**流转确认框**（列出公司/岗位与源→目标状态），取消则本地还原、不发请求 |
| 拖动失败没有反馈 | 拖拽中**放不下的列整体变灰 + 边框转虚线**（`@start` 记录源状态 → 逐列判 `canTransit`） |
| 卡片那串日期看不懂 | `09-26 14:00` → 按状态补上下文：`笔试 09-19 19:00` / `面试 09-23 15:30`；终态不显示；颜色从最弱一级提到 `--c-text-2` |

**六、状态机放开 + 结束原因（需求变更）**

- 试用实测"已获 offer 拖不到已结束"→ 根因是 SRS FR-004 把 `OFFER` 定为终态，业务上拒 offer / offer 撤回 / 谈崩都无法标记。经讨论**放开**（SRS v1.3）；
- 试用中进一步指出"已结束"太笼统（笔试面试被淘汰 / 主动放弃 / 石沉大海混为一谈，复盘价值丢失）→ 新增 `close_reason`：
  - 三类取值 `FAILED` 未通过 / `DECLINED` 主动放弃 / `EXPIRED` 无消息，**仅 CLOSED 态有值**；
  - 拖到「已结束」时弹**专门的原因选择框**（不走轻量确认——结束原因是终态记录的唯一分类信息，选错这条就废了）；
  - 卡片带原因标记；编辑对话框可更正选错的原因。

**七、过程中修掉的两个真 bug**

1. **文字竖排**（试用中发现"有些卡片竖着写"）：`.kanban-card__days` 缺 `white-space: nowrap`，作为 flex 子项默认可收缩，在"有排期 + 列宽偏窄"时被压到一字宽、中文逐字换行。多视口复现：1024 视口下被压成 20px、900 视口下 14px。修复：加 `nowrap + flex:none`，并给列设 `min-width: 168px`（实测出的下限），宽度不足时改为横向滚动而非压扁内容；
2. **确认结束的卡片被误还原**：`CloseReasonDialog` 的 `el-dialog` 关闭时一律触发 `@close`，我在那里无条件 `emit('cancel')`，导致**确认路径也被当成取消**、卡片被还原到原列。修复：加 `confirmed` 标志区分"确认后关闭"与"用户取消"。

**八、城市筛选改造：级联选择（试用中提出并迭代一次）**

原为纯文本输入框，讨论中提出"输入或自主寻找，按省份然后城市"，进一步明确为"**先出省份、点省份再出城市**"的级联交互。最终用 `el-cascader`（`show-all-levels=false` + `filterable`）实现，**未新增运行时依赖**。

配套新增 `src/constants/regions.js`（34 省级 / **747 城市** / 37.7 KB）。数据自 `element-china-area-data@6.1.0` 的 `regionData` **构建期提取一次**后内嵌，**运行时不含该依赖**（`npm install --no-save` 装、用完即删）。三条口径：

- **value 去掉尾部「市」字**——库内既有数据存的是「南京」而非「南京市」，不去后缀会让选择结果与历史数据分裂成两个筛选条件（显示仍为「南京市」，只 value 去后缀）；
- **第二级 = 地级市 + 该省下辖的县级市**（昆山、常熟、江阴等）——否则「投递到县级市」就筛不到，只能用公司关键字捞；实测 747 个可选城市；
- **直辖市不设第二级**（其下辖是「区」，市一级就是它自己），列表中直接选中；数据包只含大陆 31 个省级行政区，港澳台手工补齐。

**筛选与表单共用同一份数据**（表单的城市字段一并换成同一选择器）：否则录入时手写「昆山市」、筛选选「昆山」，两种写法在精确匹配下会分裂成两批。表单对**列表外的历史自由文本**做了兜底——若记录的城市不在省市列表中，则临时补一个「（原值，不在省市列表中）」的一级选项，保证能回显、不被静默清空。

筛选语义未扩展（仍是按城市精确匹配，后端未改）。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 页面 | `src/views/Applications.vue` | 三段布局、统计条接入、循环翻页拉全、确认框与原因对话框编排、Teleport 注入 |
| 看板 | `src/components/StatusKanban.vue` | 列等分撑满、卡片四行重排、状态色条、原因标记、拖拽中列反馈 |
| 统计 | `src/components/ApplicationStats.vue` | 新增（堆叠占比条 + 计数图例） |
| 原因选择 | `src/components/CloseReasonDialog.vue` | 新增（拖到已结束时选原因） |
| 表单 | `src/components/ApplicationFormDialog.vue` | 编辑已结束记录时可更正原因；城市字段改级联选择（含列表外原值的兜底项） |
| 趋势图 | `src/components/TrendChart.vue` | y 轴过滤小数刻度、高度 240→180、配色同步 |
| 常量 | `src/constants/application.js` | `LEGAL_TRANSITIONS` 放开 `OFFER→CLOSED`；新增 `CLOSE_REASONS` |
| 样式 | `src/styles/tokens.css` | 全站配色提亮降饱和（含 Element Plus 主题变量重新推导） |
| 数据 | `src/constants/regions.js` | 新增（全国省市选项 34/747 含县级市，构建期自公开数据包提取后内嵌） |
| 文档 | `docs/01-需求文档/需求规格说明书.md` | v1.2 → **v1.4** |
| 文档 | `docs/03-数据库/数据库设计文档.md` | v1.1 → **v1.2**（`close_reason` 字段 + 枚举） |
| 文档 | `docs/04-接口文档/接口文档.md` | v1.1 → **v1.2**（DTO / PATCH / 列表筛选 / PUT 口径） |
| 文档 | `docs/05-测试文档/测试计划.md` | v1.4 → **v1.5**（TC-02 / TC-26 补验收点） |
| 文档 | `docs/02-设计文档/项目结构.md` | v1.11 → **v1.12**（登记 `CloseReasonDialog.vue`） |
| 文档 | `docs/README.md` | 版本清单同步 4 项 |

> **数据库迁移**：开发库 `backend/app.db` 的 `close_reason` 列由前端侧手动 `ALTER TABLE` 补上（`create_all()` 不给已存在的表补列），迁移前备份、迁移后核对 24 条记录未变。

### 验证

| # | 验证项 | 命令 / 手段 | 结果 |
|---|---|---|---|
| 1 | 拖拽失效修复 | CDP 驱动真实原生拖拽 | 合法落点成功且后端状态流转、非法落点被拒（7/7） |
| 2 | 改版布局 | 无头浏览器 DOM 断言 | 无横向滚动、看板撑满（底部差 20px）、5 列等分 216px、列内滚动生效（15/15） |
| 3 | 文案竖排修复 | 8 档视口（900~1600）扫描窄高元素 | 修复前 1024/900 各 5 处竖排 → 修复后**全部视口 0 处** |
| 4 | 拖拽列反馈 | 拖拽中 DOM 断言 | 源为「待笔试」时 APPLIED / OFFER 两列变灰（精确匹配），松手后清除（5/5） |
| 5 | 流转确认框 | 无头浏览器两条路径 | 取消：后端状态未变 + 卡片回原列；确认：状态正确流转（8/8） |
| 6 | 结束原因全链路 | 无头浏览器 + 接口核对 | 原因框弹出与三选项、落库 `FAILED`、卡片标记「未通过」、取消不落库、编辑更正为 `EXPIRED`、普通流转仍走轻量确认（11/11） |
| 7 | 后端回归 | `cd backend && .venv/Scripts/python.exe -m pytest` | **74 passed** |
| 8 | 城市级联选择 | 无头浏览器 12 项断言 | **12/12**：一级仅省份（34 项）、点省出二级（含县级市 昆山/常熟）、选中后看板卡片数 = 接口按该城市查询数、直辖市为叶子节点且一级直达、搜索县级市命中、表单复用同一选择器并回显已有城市 |

> 验证 1、4、6 依赖 CDP 的 `Input.setInterceptDrags` + `dispatchDragEvent` 驱动**原生 HTML5 拖拽**（sortablejs 用原生拖拽，普通鼠标事件序列驱动不了）——此前步骤 4 曾把"拖拽手势"列为未覆盖项，本轮已补上自动化，不再依赖人工确认。

### 决策与遗留

- **卡片字段的取舍原则**：一行只承载一个信息。三字段挤一行会导致截断——**既不完整又占空间**，是所有处理里最差的一种；宁可少显示、点开看详情；
- **配色是全局的**：`tokens.css` 是唯一出处，本次提亮降饱和会影响后续所有页面，故在设计阶段就确认了方向（而非只改投递页）；
- **"已结束"不作单态**：结束原因是终态的**唯一分类信息**，故做成必填且单独一步选择，而不是给个"是否流转"的确认了事；
- **需求变更已回改上游文档**：`close_reason` 与状态机放开都是**原文档未约定**的功能，按"文档先行"先改 SRS / 数据库设计 / 接口文档 / 测试计划再动代码，因此本次迭代产生了 5 份公共文件的版本变更，需与 `backend` 分支互相拉取同步；
- **遗留（转入后续步骤）**：
  1. 列表接口已具备 `close_reason` 筛选能力，但前端**未加筛选控件**（工具条已有三个控件），留给后续"复盘/统计"类需求；
  2. 历史数据（功能上线前结束的记录）没有结束原因，卡片不显示标记——**非缺陷**，新流转的都会带；
  3. 看板数据量到数千条时需引入虚拟滚动（当前封顶 500 条 + 列内滚动）。

---

## 步骤 4 迭代：投递页体验修订（第二轮试用反馈）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-25 |
| 性质 | 步骤 4 交付后的**试用迭代**（非新步骤），三项均为体验问题，无需求变更 |
| 对应分支 | `frontend` |
| 上游依据 | 试用反馈 / 系统设计文档 **v1.4** / 项目结构文档 **v1.14** |

### 工作内容

试用后提出三项反馈。三项均先实测取证再定方案。

**一、城市选项收敛到地级市**

`constants/regions.js` 整体重新生成：数据源由原来的混合口径改用 `element-china-area-data@6.1.0` 的 `pcaTextArr`（省-市-区三级）**第二级**——该级本身即纯地级市（江苏 13 项），无需按行政区划代码推断；另排除 label 含「直辖县级行政区划」的占位项（河南 / 湖北 / 海南 / 新疆各 1 项）。可选中城市 **747 → 346**（二级选项 741 → 340，一级直达 6 项不变）。筛选栏与表单同源导入，改数据即两处生效。

**二、边框可见性**

试用反馈"卡片边框看不见，趋势图的边界也是"。实测：看板列 / 卡片 / 统计条 / 趋势图容器边框均为 `1px #ECEBF5`，对白色卡片**对比度仅 1.18:1**——不是"偏淡"，是实际不可辨。

- `--c-border` `#ECEBF5` → `#CFCBDD`（1.18:1 → **1.59:1**），一处 token 全站生效；
- `--c-divider` `#F3F2FA` → `#EAE8F2`：趋势图水平网格线用的是这个 token，边框加深而网格线不动会让图内依旧发虚；两者仍保持"分隔线比边框弱一档"的层级关系；
- **顺带修根因**：`TrendChart.vue` 内 5 处色值是 token 的**硬编码副本**（ECharts 画在 canvas 上读不到 CSS 变量，当初只能写死），后果是"改 token 图表不跟随"。改为 `buildOption()` 时经 `getComputedStyle` 取实值，回落值保留原色值。

**三、按钮尺寸与层级**

实测发现「查看趋势」**高 32px、热区 86×32**，与筛选输入框等高——**尺寸并不小，问题是 `border: 0px`**：它用的是 `text` 类型，在设计文档 §4.5.4 的三级体系里属「纯图标（工具性）」级，而它是有文案的次操作。按"放大尺寸"处理会改不到病根，故改为**主题色描边按钮**。真正偏小的是顶栏两按钮与趋势图区间切换（24px / small 档），一并提到默认档。

### 产出

| 类型 | 文件 | 动作 |
|---|---|---|
| 代码 | `frontend/src/constants/regions.js` | 重新生成（926 → 529 行） |
| 代码 | `frontend/src/styles/tokens.css` | 2 个色值 + 理由注释 |
| 代码 | `frontend/src/components/TrendChart.vue` | 加 `token()` 读变量；5 处硬编码改引用；去 `size="small"` |
| 代码 | `frontend/src/views/Applications.vue` | 「查看趋势」`text` → `type="primary" plain`；「批量导入」去 small |
| 代码 | `frontend/src/components/AppHeader.vue` | 「＋ 新增投递」去 small |
| 文档 | `docs/02-设计文档/系统设计文档.md` | v1.3 → **v1.4**（§4.5.1 边框 / 列表分隔线两行） |
| 文档 | `docs/02-设计文档/项目结构.md` | v1.13 → **v1.14**（§3 目录树 regions.js 说明） |
| 文档 | `docs/07-工作日志/问题记录.md` | 新增 IS-16 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 城市选项粒度 | Node 断言 | **23/23**：一级 34、二级 340、江苏 13 项、不含昆山/常熟/江阴/宜兴、二级 value 均等于 label 去尾「市」字 |
| 2 | 在库数据不回归 | Node 断言 | 库内 8 个城市值（上海/北京/南京/合肥/成都/杭州/深圳/苏州）全部命中 |
| 3 | 筛选栏实际生效 | 无头浏览器读级联面板 | 一级 34 项；江苏展开 13 项，**无县级市** |
| 4 | 边框色生效 | 无头浏览器读计算样式 | 看板列 / 卡片 / 统计条 `borderTopColor` 均 = `rgb(207,203,221)`；`--c-divider` = `#EAE8F2` |
| 5 | 趋势图随 token | 读 ECharts 实例 option | **5/5**：x 轴轴线 = `--c-border`、网格线 = `--c-divider`、折线 = `--m-application`、轴标签 = `--c-text-3`、已脱离旧硬编码 |
| 6 | 按钮尺寸与层级 | 无头浏览器读 `getBoundingClientRect` | 查看趋势 32px 且 `border: 1px`、「批量导入」32px、「＋ 新增投递」32px、区间切换 30px（default 档，small 档为 22px） |
| 7 | 顶栏两按钮等高 | DOM 断言 | 均 **32px**（改前均 24px，只改一个会高低不齐） |
| 8 | 布局无回归 | DOM 断言 | 无横向滚动（1166/1166）、看板底部差 20px、5 列各 222px |
| 9 | 拖拽未受影响 | CDP 驱动**原生 HTML5 拖拽** | **3/3**：合法落点成功且状态落库（刷新后仍在）、非法落点被拒且不发请求 |
| 10 | 控制台 | CDP 捕获 error / 未捕获异常 | 6 条交互路径（展开趋势 / 切 7·90 天 / 收起 / 开导入框 / 开新增表单）**0 error、0 warning** |
| 11 | 构建 | `npm run build` | 三个任务后各跑一次，**3/3 通过** |

> 验证 9 会用真实数据流转一张卡片，故脚本优先选取「补充测试公司」这类测试记录，避免动到真实投递。

### 决策与遗留

- **「查看趋势」的问题是按钮级别用错，不是尺寸不足**：实测 86×32 与输入框等高，`border: 0px` 才是"显得小"的原因。若按字面"放大尺寸"处理，加到 40px 也治不好"看不出能点"——这是本轮最有价值的一条实测发现；
- **趋势图硬编码色值是"改 token 图表不跟随"的根因**：本次不改，则边框加深后会出现"容器变了、图内网格线还是旧的浅色"的割裂。凡 canvas 渲染（ECharts）都要走 `getComputedStyle` 取实值，此后改 token 自动跟随；
- **色值口径须与文档同步**：`#CFCBDD` / `#EAE8F2` 已写入系统设计文档 §4.5.1，避免下次有人照旧值改回去；
- **「747 城市」与「741 二级选项」是两个口径**：前者 = 二级 + 6 个一级直达（直辖市 4 + 港澳 2）。本次在项目结构文档 v1.14 条目里写明了两者，避免后续再按错误口径对账；
- **遗留**：
  1. 系统设计文档 §4.5.1 **整表仍滞后**（品牌色 / 8 个模块色 / 5 个投递状态色 / 其余中性色均与 `tokens.css` 不符，是"提亮降饱和"改版时未同步），本次只改了动到的 2 行，其余登记 **IS-16** 待双方对齐——其中状态色牵涉「枚举码与色值映射不得改动」的口径与数据库设计文档第 5 章；
  2. 筛选栏对历史县级市数据无兜底（表单侧有 `isKnownCity` 兜底，筛选侧没有）。当前库内无此类数据，若将来出现需补。

---

## 新需求（多账号 / 语音 / 新手指引）前端侧文档同步

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-25 |
| 性质 | 后端大改需求文档（SRS v1.5~v1.7）后的**前端侧文档补齐**，无代码改动 |
| 对应分支 | `frontend` |
| 上游依据 | SRS v1.5~v1.7 / 系统设计 v1.5~v1.7 / 接口文档 v1.3~v1.5 / 开发计划 v2.3~v2.5 |

### 工作内容

后端一次性落地了三批需求（多账号体系、语音方案重定、新手指引），文档里有两处**明确点名归前端侧**的待办，本次一并完成：

| 来源 | 待办 | 处理 |
|---|---|---|
| 项目结构 v1.15 | 「前端目录树（§3）本轮未同步……**由前端侧更新**」 | §3 补 5 个新页面（Welcome/Login/Register/Profile/AiConfig）、`UserMenu.vue`、`GuideTour.vue`、`stores/user.js`、3 个 api 模块（`auth`/`llmProviders`/`voice`）、路由守卫；`Settings.vue` 与 `system.js` 职责随瘦身改写 |
| 系统设计 v1.7 | 「`driver.js` 依赖登记**归前端侧**」 | 项目结构 §5 登记 `driver.js`（P2、步骤 24 才装），并补注「**语音前端不引依赖**」 |
| 开发计划 v2.3 | 「新增四步的测试用例编号**由测试侧分配**」 | 测试计划新增 TC-52~68（17 条）+ 改写 6 条；开发计划三处占位替换为实际编号 |

**语音前端不引依赖**（写进 §5 的说明）：VAD 静音断句用 Web Audio API 的 `AnalyserNode` 自行判定阈值、录音用原生 `MediaRecorder`、播报用原生 `<audio>` 播放后端 mp3——三者均为平台能力，符合设计文档 1.2「最小依赖」原则。

### 产出

| 类型 | 文件 | 动作 |
|---|---|---|
| 文档 | `docs/02-设计文档/项目结构.md` | v1.16 → **v1.17** |
| 文档 | `docs/05-测试文档/测试计划.md` | v1.5 → **v1.6** |
| 文档 | `docs/06-开发计划/开发计划.md` | v2.5 → **v2.6** |
| 文档 | `docs/02-设计文档/系统设计文档.md` | v1.7 → **v1.8**（见决策第 3 条） |
| 文档 | `docs/README.md` | 版本清单联动 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 用例编号连续无缺 | 脚本扫描 | TC-01~68 **全部存在**，表格无重复定义，定义数与总数一致（68） |
| 2 | 版本号联动 | 逐一比对头部与 README 清单 | 8 份文档**逐项一致** |
| 3 | 依赖链检查 | 搜下游文档 | SRS 改动后按「需求→设计→数据库/接口→测试→开发计划」核对，仅接口文档一处引用式提及，自动跟随 |

### 决策与遗留

- **测试用例编号的分配思路**：能覆盖既有功能的走「改写」而非「新增」——TC-47/48/49（连通性测试 / 两级切换 / 模型列表）本是这些功能的用例，随端点迁移改写即可，不另起编号；新增只给真正的新功能。故最终是「新增 17 + 改写 6」，而非全部新增；
- **测试计划里把口径写实**：账号用例的断言对着 SRS 与接口文档错误码表逐条落定（80003 用户名已存在 / 80004 不区分提示防枚举 / 80005 锁定 / 80007 原密码错误），不写「校验正确」这类无法证伪的描述；
- **系统设计 v1.8 只因一处指向错误**：§4.7 写「`driver.js` 需加入前端依赖清单（项目结构文档**第 3 节**）」，但依赖清单在 §5、§3 是目录树，照原指引会找错位置。改了内容就得记，故升版。**若认为此类笔误不必升版，可回退该条目**；
- ~~**遗留**：本文件与测试日志的「后续步骤索引」**步骤编号仍是多账号改造前的口径**（步骤 5 起含义已变：新计划中 5 = 账号与鉴权、9 才是今日概览）。属结构梳理事项，待下次统一校正。~~ **已校正**——文末索引已按**开发计划 v2.6**（24 步 → 25 步，原「步骤 5 今日概览」顺延为步骤 9）重排；

---

## 步骤 4 迭代：状态机跳级（前端部分）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-25 |
| 性质 | 需求变更（状态机放宽），前端先行、**后端待同步** |
| 对应分支 | `frontend` |
| 上游依据 | SRS **v1.8**（FR-004）/ 测试计划 v1.7 |

### 工作内容

试用中提出「已投递应该也要支持直接拖入面试中，有些公司是没有笔试的」。查证后确认：这不是「加一条边」，而是 **SRS FR-004 原文「流转只能沿链前进」被实现收窄成了「逐跳前进」**——`已投递 → 面试中` 本就不违反原规则。故本次是规则的澄清（改为「可前进到链上任意更靠后的状态」），一并放开 `待笔试 → 已获 offer`，避免规则变成「可以跳过笔试、不能跳过面试」这种没有依据的区分。

前端改动**只有一张常量表**：`constants/application.js` 的 `LEGAL_TRANSITIONS` + 两处注释（依据升到 SRS v1.8）。组件层零改动——`StatusKanban.vue` 的 `canTransit` / `canPullFrom` / `canDropTo` 全部读表消费，改表即改行为。

### 产出

| 类型 | 文件 | 动作 |
|---|---|---|
| 代码 | `frontend/src/constants/application.js` | `LEGAL_TRANSITIONS` 两行 + 注释 |
| 文档 | `docs/01-需求文档/需求规格说明书.md` | v1.7 → **v1.8** |
| 文档 | `docs/05-测试文档/测试计划.md` | v1.6 → **v1.7** |
| 文档 | `docs/README.md` | 版本清单联动 |

> 测试侧改动（`backend/tests/test_applications.py` 的 3 处用例）见[测试日志](测试日志.md)同条目。

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 前端表符合目标规则 | 逐条比对脚本 | **✅ 与 SRS v1.8 一致**（当时后端 2 条待同步，现后端已于同日改完并推送 `6bc9b93`） |
| 2 | 构建 | `npm run build` | 通过 |
| 3 | 拖拽预判已放行 | CDP 驱动原生 HTML5 拖拽 | `已投递 → 面试中` 能拖入、弹确认框；拖拽中**无列变灰**（可达目标已覆盖全部列，符合设计预期） |
| 4 | 后端拒绝后前端正确回滚 | 同上 | 卡片**回滚到「已投递」**，提示「非法状态流转：已投递 → 面试中」——前端链路正确，只差后端 |
| 5 | 页面无报错 | CDP 捕获 console | 4 条交互路径 **0 error / 0 warning** |
| 6 | 布局无回归 | DOM 断言 | 无横向溢出、底部差 20px、5 列各 222px |

### 决策与遗留

- **一个可预见的行为变化（已记入 spec）**：拖拽时「放不下的列变灰」这一反馈，在从 `已投递` / `待笔试` 拖动时会**完全消失**——它们的可达目标已覆盖全部列。这是规则放宽的正确结果，**不是 bug**，先记下以免误判；
- **保持显式枚举而非从状态顺序推导**：后者少写几行，但会失去与后端表「逐条对齐、可直接核对」的性质，而这正是当初把常量收敛到单一出处的理由；
- **前端先行是有意的中间态**：前端改完后，拖拽会出现「拖得动但被后端拒绝并回滚」——此刻行为**自洽**（提示清楚、状态不错乱），比等两侧都改完再一起验证更早暴露前端问题；
- **合并后回归已通过**（2026-09-25）：后端同日改完并推送 `6bc9b93`，用户合并 `origin/backend`（`4b2725f`）后回归——三方规则完全一致、`pytest` **81 passed 全绿**、拖拽三项全过：`已投递 → 面试中` 与 `待笔试 → 已获 offer` **均成功落库**（刷新后仍在），`面试中 → 已投递` **被正确拒绝**（目标列变灰、无确认框、未流转），控制台 0 error；
- **回归首轮踩到一个环境坑**：首跑跳级仍被拒、提示「非法状态流转」，但代码与测试均已是最新——**根因是后端进程未重启**（进程启动 11:03:53、服务层文件 12:54:43 才改，uvicorn 不带 `--reload` 不会热重载）。**此后凡合并后「代码已改而行为未变」，先查进程启动时间与文件修改时间的先后**；
- ~~**遗留**：本文件与测试日志的「后续步骤索引」步骤编号仍滞后（见上一条目），待统一校正。~~ **已校正**——随上一条目一并重排完毕；

---

## 步骤 7：账号体系（前端）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-25 |
| 性质 | **开发计划 v2.6 步骤 7**（多账号改造前端）。后端 `/auth` 尚未实现，本步以 **dev-only mock 层**先行，使界面可实际走查并完成 TC-62~66 |
| 对应分支 | `frontend` |
| 上游依据 | SRS **v1.8** FR-016 / FR-017；系统设计 **v1.7** §4.1·4.2·4.4·4.6；接口文档 **v1.5** §3.2；测试计划 **v1.7** TC-62~66 |
| 设计文档 | 设计稿与实现计划（过程文档，不入库） |

### 工作内容

**一、布局分两套壳（`meta.layout`）**

账号页（`/welcome`、`/login`、`/register`）需全屏无侧栏顶栏，业务页沿用现有布局。做法：路由加 `meta.layout: 'blank'`，`App.vue` 按 `route.meta.layout` 条件渲染。**未采用嵌套路由**——账号页是少数派（3 vs 10），把多数派降为 children 意味着重写 `SideNav` 的菜单派生与守卫逻辑，而步骤 4 刚验收的 9 个页面全部受影响；标记"例外"的回归面最小。

**二、dev-only mock 层（本步关键取舍）**

`frontend/mock/` 五个文件 + Vite 插件中间件。`VITE_USE_MOCK=true` 时接管全部 `/api/v1/*`，**同时不注册 `/api` 代理**（二者互斥，避免"谁先命中看实现细节"的脆弱依赖）。

- **全量自洽**，而非"只 mock 账号接口 + 业务接口透传真后端"：后者在两种时刻翻车——后端未做鉴权时能跑通（假象）；一旦后端推上步骤 5 鉴权，mock 签发的假 Token 立刻被判 401、触发全局登出、界面全崩。"验收时好跑、联调时崩"远比"离线自洽"危险；
- mock 实现**真实业务逻辑**而非固定桩：用户名唯一（不区分大小写）、首个账号 `ADMIN`、连续失败 5 次锁 5 分钟、改密作废旧 Token（`password_changed_at` 比对 `iat`）、头像 multipart 字节存取与静态返回；
- 额外提供 `POST /__mock__/reset`（清内存库），使断言脚本可重复执行——否则"首个账号应为 ADMIN"这类断言跑第二次必失败。

**三、账号状态与请求层**

- `useUserStore`：Token 持久化 localStorage（key `jobpilot_token`）、用户信息、`displayName` / `avatarImg` / `fallbackAvatar`（首字 + 哈希底色，纯前端生成）；
- **`reset()` 与 `logout()` 分离**：`reset()` 只清本 store（供 401 拦截器调用，跳转由拦截器统一控制，避免 store 反向依赖 router）；`logout()` 额外 reset 其余 store（对应 TC-66 账号切换无数据残留）；
- `request.js`：请求拦截器注入 `Authorization: Bearer`；响应拦截器识别 `80001/80002` → 清状态 + 提示 + 跳登录页，**带 `silent` 的请求只清状态不跳转**（`/welcome` 的 Token 预检用它）；
- 路由守卫**读 localStorage 而非 store**，避免"路由模块 ↔ store 模块"循环依赖；免登录页用**显式白名单**而非反向推断，避免新增页面时漏判。

**四、入场动画（按系统设计 §4.6 时间轴）**

纯 CSS `@keyframes`，零第三方库：logo 0.5s `scale .8→1` + 淡入、主标题逐字浮现（500ms 起、每字 80ms 交错）、副标题 1.6s 淡入、进度条 2.5s 走完、2.1s 后整体淡出。Token 预检与动画**并行**（不阻塞播放，失败只改跳转目标）；点击任意处 / 按任意键跳过；`prefers-reduced-motion: reduce` 时直接跳转。

**「会话内只播一次」的载体选择**：用**模块级内存标记**而非 `sessionStorage`，由此三方口径同时满足——SPA 内跳转不重播（SRS §3.13.3 第 3 条）、刷新 `/welcome` 重播（设计文档 §4.6 + TC-62）、已登录用户刷新业务页根本不经过 `/welcome`（SRS 第 4 条）。SRS 与设计文档在此处的表述差异因此无需改文档即可自洽。

**五、登录页与注册页**

共用 `AuthShell`（深紫渐变全屏 + 居中白卡片）。登录：用户名 / 密码 /「30 天免登录」勾选（`remember_me` 决定 1 天或 30 天 Token）；错误在**表单内**提示（调用带 `silent`，不弹全局 toast——接口文档规定 80004 的前端动作是"登录表单整体提示"）。注册：密码强度**只提示、不拦截提交**（SRS 明确"给出强度提示但不拦截"），仅"两次密码不一致"这类填错才拦截；80003 回传后用户名输入框标红；注册成功即自动登录进首页。

**六、用户菜单与侧栏调整**

`UserMenu`（头像 + 用户名 + 下拉：个人中心 / 设置 / AI 配置 ｜ 退出登录末位分隔），挂顶栏最右。侧栏**摘掉「系统」组**（`/settings` 加 `hidden`）——设置迁入用户菜单后若侧栏保留会出现两个入口。`/ai-config` 建占位页（步骤 8 填充）。

**七、个人中心**

账号信息卡（128px 大头像 + 可编辑昵称/邮箱 + 角色标签 + 只读 meta）/ 数据概览 3 小卡 / 修改密码 / 求职画像 12 字段。头像走 **canvas 居中裁切压 256×256**（jpeg 0.9）后上传，渲染路径统一为 `/static/avatars/<文件名>`（后端存 `uploads/avatars/x.png`，故取文件名再拼）。改密成功后清 Token 跳登录页。

**八、过程中修掉的两个真 bug**

1. **"会话内只播一次"失效**：`<script setup>` 的顶层代码属于 `setup()` 函数体，**每次组件实例化都会执行**，`let played = false` 写在那里等于每次进页面都重置。CDP 复现：SPA 内第二次进 `/welcome` 仍播动画。修复：移到普通 `<script>` 块（模块作用域）。已记入 IS-17；
2. **刷新业务页时顶栏用户名/头像空一截**：store 随页面重建后 `user` 为 `null`，而顶栏依赖它渲染。修复：`AppHeader.onMounted` 在"有 Token 且无 user"时补拉一次 `fetchMe`（非 silent，Token 已失效时由拦截器统一跳登录页）。

**九、交付后即时修订：入场动画时长（试用反馈）**

试用反馈："动画时长再缩短一些，感觉改成当前时间的四分之三或者三分之二比较好"。取**四分之三档 → 1.8s**，并**顺带修正一处实现偏差**：

| 项 | 原 | 现 |
|---|---|---|
| 动画主体 | 2.5s | 1.4s（计时到 `TOTAL_MS - LEAVE_MS` 即开始收尾） |
| 末段整体淡出 | 在 2.5s **之外**再加 0.4s | **含在 1.8s 之内** |
| **实测「进入到跳转」** | **约 2.9s** | **1718ms** |

原实现把「总时长约 2.5s（末段 0.4s 淡出）」读成了「2.5s 播完**再**淡出 0.4s」，实际比口径长了整整 0.4s——用户感受到的"偏长"里有一半来自这处偏差。本次一并纠正：**1.8s 即"进入到跳转"的总时长**。

逐字节奏同步加密（间隔 80ms → 60ms、起始 500ms → 400ms）；进度条时长改读 CSS 变量 `--welcome-duration`（由 JS 的 `TOTAL_MS` 下发）——**时长参数由此收敛为单一出处**，下次再调只需改 `TOTAL_MS` 一个数，不会再出现"JS 计时器与 CSS 动画各写一个数"的漂移。

**文档联动**（按"文档先行 + 版本号联动"）：SRS **v1.9**（§2.2 与 §3.13.3 两处时长）、系统设计 **v1.9**（§4.6 时间轴重排；**顺带把「`sessionStorage` 标记」更正为模块级内存标记**——该表述与 §4.6 自身"刷新页面重播"的要求直接冲突，实现按后者落地后文档未同步，本次一并回改）、测试计划 **v1.8**（TC-62 时长口径）、项目结构 **v1.18**（§3 前端目录树按实际落地逐项校正）、`docs/README.md` 版本清单 4 行同步。

### 产出

| 项 | 位置 | 说明 |
|---|---|---|
| mock 层 | `frontend/mock/{index,utils,db,auth,misc}.js` | 新增（dev-only，不进生产构建） |
| 开关 | `frontend/.env.development`、`vite.config.js` | 新增 / 改（mock 与代理互斥） |
| 状态 | `src/stores/user.js`、`src/stores/app.js` | 新增 |
| 请求层 | `src/api/request.js` | 改（注入 Token + 401 全局登出） |
| 接口封装 | `src/api/{auth,profile,overview}.js` | 新增 |
| 工具 | `src/utils/avatar.js` | 新增（默认头像 + 256×256 压缩） |
| 路由 | `src/router/index.js` | 改（+5 路由、+守卫、`/settings` 转 hidden） |
| 布局 | `src/App.vue` | 改（`meta.layout` 分流两套壳） |
| 页面 | `src/views/{Welcome,Login,Register,Profile,AiConfig}.vue` | 新增 |
| 组件 | `src/components/{AuthShell,UserMenu}.vue`、`AppHeader.vue` | 新增 / 新增 / 改（接入用户菜单 + 补拉账号） |

> **文档改动**：编码阶段**未修改**接口与数据库相关文档（无接口、无数据库变更，契约完全按接口文档 v1.5 实现）；**交付后按试用反馈修订动画时长**，联动 4 份文档（SRS v1.9 / 系统设计 v1.9 / 测试计划 v1.8 / 项目结构 v1.18）+ `docs/README.md` 版本清单，详见「九」。

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | mock 层行为对齐接口文档 | Node 断言脚本直连 dev server（21 条） | **21/21**：注册返回 Token / 首个 ADMIN / 重名 80003（不区分大小写）/ 短密码 80006 / 非法用户名 10001 / 失败 5 次 80005 / 无 Token 401+80001 / **改密后旧 Token 80002** / 头像字节往返一致 / 恢复默认置空 |
| 2 | 路由守卫 | CDP 内 SPA 导航断言 | 无 Token 访问 `/`、`/applications`、`/profile` 均落 `/welcome`；`/login`、`/register` 放行（5/5） |
| 3 | 入场动画 | CDP DOM + 计算样式 | 6 字延迟 500/580/…/900ms、进度条 2.5s、动画后跳转正确；点击跳过立即跳；有 Token 进首页、无 Token 进登录页；**SPA 内再进不重播**、新页面加载重播；`reduced-motion` 下不播动画直接跳 |
| 4 | 注册与登录 | CDP 表单交互 | 5 个输入框、强度显示「中」/「强」、注册即登录进首页、密码不一致被拦截、错误密码提示「用户名或密码错误」、**勾选免登录后 Token TTL 实测 30 天** |
| 5 | 用户菜单与侧栏 | CDP DOM 断言 | 下拉四项齐全、点「个人中心」跳 `/profile`、退出登录有确认框且登出后清 Token、**侧栏仅剩「求职流程」「能力提升」两组**（「系统」组已移除） |
| 6 | 个人中心 | CDP 全流程 | 4 张卡片、角色标签「管理员」、昵称保存后**顶栏同步**、头像上传后顶栏与个人中心均显示且真实加载（`naturalWidth>0`）、恢复默认回退首字色块、画像保存回读一致 |
| 7 | 修改密码 | CDP + 接口核对 | 原密码错 80007、两次不一致前端拦截、成功后跳登录页且清 Token、**旧密码登录被拒 / 新密码可用** |
| 8 | 账号切换无残留（TC-66） | CDP 双账号 | A 设置画像 → 登出（用户菜单消失、Token 清除）→ B 登录：昵称/用户名/画像均为 B，**无 A 残留** |
| 9 | 构建 | `npm run build` | 通过（chunk 体积警告为 Element Plus 全量引入的既有项，非本次引入） |
| 10 | 控制台 | CDP 全程采集 | 有效登录态路径 **0 error**；仅"用已失效 Token 打开 `/welcome`"场景出现 1 条浏览器对 401 响应的网络层记录——**属正确鉴权行为**，非 JS 错误 |
| 11 | 截图 | 无头 Edge 1440×900 | 5 张：入场动画（中间态 + 完整态）、登录页、注册页（含强度条）、首页（顶栏用户菜单）、个人中心 |
| 12 | 动画时长（修订后复验） | CDP 实测 | 逐字延迟 400/460/…/700ms（60ms 间隔）、进度条 `animationDuration` **1.8s**、CSS 变量 `--welcome-duration: 1800ms`；**「进入到跳转」实测 1718ms**；点击跳过 **11ms** 内跳转；构建 exit 0 |

> 前端无单元测试框架（步骤 1 已确立），以上以 CDP 无头浏览器断言 + Node 直连脚本为准。断言脚本写在系统临时目录，未入仓库。

### 决策与遗留

- ~~**本步的产物是"可走查的界面"，不是"已联通的系统"**：验收全部**基于 mock 层**，报告与测试日志均如实标注。后端步骤 5 推送后必须补一次真联调走查（`VITE_USE_MOCK=false` 重跑 TC-62~66）；~~ **已完成**（2026-09-26 核实：真联调复核已跑，TC-62~66 全过 + Token 失效全局登出补充验证 + 控制台 0 error，详见[测试日志](测试日志.md)「真联调复核（步骤 7 前端 ↔ 步骤 5 后端）」节）；
- **mock 的 HTTP 状态码是推断值**（如锁定用 423），接口文档只规定响应体 `code`，前端亦只依赖 `code` 分支，故不影响联调；真后端以自己实现为准（已记入 IS-18）；
- **不引第三方动画库 / mock 库**：入场动画用纯 CSS（演示环境稳定优先），mock 用手写中间件（零依赖、零生产影响、删文件即还原）；
- **`AuthShell` 抽为共用外壳**：登录与注册的卡片、背景、品牌行完全相同，抽出来避免两处各写一遍配色——但**表单本身不抽**（两页字段与校验差异大，强行共用会引入分支参数）；
- **遗留（转入后续步骤）**：
  1. `/ai-config` 为占位页，步骤 8 填充；
  2. 个人中心「重看新手指引」入口留待步骤 24；
  3. ~~`.env.development` 的 `VITE_USE_MOCK` 在后端就绪后需手动置 `false`；~~ **已完成**（2026-09-26 核实：该文件现为 `VITE_USE_MOCK=false`，真联调已按此跑通）；
  4. mock 层保留在仓库中（联调期与离线演示仍可用），仅切开关。

---

## 投递表单标签换行修复（试用发现）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-25 |
| 性质 | 步骤 4 页面（投递表单）的 **UI 缺陷修复**，试用时截图报告 |
| 对应分支 | `frontend` |

### 问题与根因

「新增投递」对话框里 **「下次笔试/面试」标签换行**（"试"字被挤到第二行）。

根因：表单 `label-width="100px"`，减去 Element Plus 的 `padding-right: 12px` 后**可用宽度只剩 88px**；而该标签是 6 个汉字 + 斜杠，14px 字体下约 **91px**——刚好超出即换行。同表单其余标签均为 4 字（约 52px），故只有它出问题。

### 修复

`label-width` **100px → 120px**（对话框 560px 宽，输入框仍余约 400px，不影响布局）。

### 验证

CDP 实测对话框内 9 个标签的渲染高度：**全部 32px（单行）**。「下次笔试/面试」修复前为两行、修复后与其余标签等高（`targetIsWrapped: false`）。

---

## 投递删除入口迁移（试用发现）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-25 |
| 性质 | 步骤 4 页面（投递）的**交互调整**，试用时提出 |
| 对应分支 | `frontend` |

### 改动

删除投递的入口由**看板卡片右上角**（hover 才显现的小图标）**迁到表单对话框**底部左侧（仅编辑态显示）：

| 位置 | 改前 | 改后 |
|---|---|---|
| 卡片 | hover 出现删除图标 | **移除** |
| 对话框 | 无 | 编辑态底部左侧「删除」（`type="danger" plain`，与「保存」分列两端） |
| 新增态 | — | 不显示删除（无记录可删） |

**理由**：卡片上的图标热区小、悬停才显现，容易被当成装饰，且"删的是哪一条"要靠卡片内容确认；移入对话框后，删除与该记录的其余编辑动作同处一地，标题与表单内容都在眼前，指向更明确。

**删除流程保持单一出处**：对话框只 `emit('delete', {id, company, position})`，确认框、接口调用、列表刷新仍由 `Applications.vue` 的 `onDelete` 执行；该函数改为返回布尔值，对话框据此"删成功才关闭、取消或失败保持打开"。

### 产出

| 文件 | 变化 |
|---|---|
| `components/ApplicationFormDialog.vue` | 加 `delete` emit、底部左侧删除按钮（`v-if="isEdit"`）、底部操作区两端对齐样式 |
| `components/StatusKanban.vue` | 移除卡片删除图标、`Delete` 图标 import、`delete` emit 与 `.kanban-card__del` 样式 |
| `views/Applications.vue` | `onDelete` 返回布尔值；新增 `onDeleteFromDialog`；`@delete` 由看板改绑对话框 |

### 验证

CDP 实测（真实前后端）：

| # | 项 | 结果 |
|---|---|---|
| 1 | 新增对话框底部按钮 | 仅「取消 / 保存」，**无删除** ✓ |
| 2 | 卡片上删除图标 | `cardHasDeleteIcon: false`（已移除）✓ |
| 3 | 编辑对话框底部按钮 | 「删除 / 取消 / 保存」，**删除位于保存左侧** ✓ |
| 4 | 删除流程 | 二次确认（文案含公司 · 岗位）→ 确认 → 看板卡片数 1→0 ✓ |
| 5 | 删除后对话框 | 自动关闭 ✓ |
| 6 | 控制台 | 0 error ✓ |
| 7 | 构建 | exit 0 ✓ |

---

## 步骤 8：AI 配置页 + 设置页瘦身（前端）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | **开发计划 v2.12 步骤 8**。两页均从 `el-empty` 占位页起步（设置页此前的"含 LLM 字段的旧版"从未实现过，故本步是**从零建**而非改造）。后端接口已在步骤 6 全部落地，**本步全程走真联调，无需 mock 先行** |
| 对应分支 | `frontend` |
| 上游依据 | SRS **v1.10** §3.12（FR-015）·§3.15（FR-018）；系统设计 **v1.13** §4.3·4.4·4.5·5.4；接口文档 **v1.10** §3.3·§3.12；测试计划 **v1.13** TC-67 |
| 设计文档 | 设计稿与实现计划（过程文档，不入库） |

### 工作内容

**一、两项形态决策（开工前经讨论拍板）**

| 决策点 | 选定 | 理由 |
|---|---|---|
| 12 张卡片 | **折叠卡片，点击展开编辑** | 每张卡要容纳 Key + base_url + 模型 + 四个按钮，全展开页面长达 2500px+ 且 12 个 Key 输入框同屏很吵；折叠后每张约 60px，状态一屏半看全 |
| 设置页保存 | **统一「保存设置」按钮** | 与个人中心画像表单一致；含讯飞凭据这类敏感输入，显式提交语义更清晰；实现上是「快照 diff → 一次 PUT」，不产生半保存状态 |

**二、AI 配置页 `/ai-config`**

- 页面持有 `providers` 列表为唯一数据源，按后端返回的 `group` 分组（国内/国外/聚合/本地/自定义，5/4/1/1/1）；卡片动作完成后 emit `changed` 由页面**整体重拉**——`is_active` 是互斥关系，局部更新会在切换后留下两个「当前使用中」；
- **卡片与页面的职责线**：改变持久化状态的动作（保存/激活/删除）与只读探测（拉模型列表/测连通性）都由卡片发起，前者完成后通知页面刷新全局数据，后者结果**就地展示**（`silent`，不弹全局 toast）；
- 状态标签严格取 SRS 三态，形态用**色点 + 文字**（系统设计 §4.5.4：一屏 12 张卡片若都是彩色胶囊会互相叫喊）；
- 模型列表**展开时才拉取**（否则进页面就打 12 个请求），同一次页面停留内不重复请求，「刷新」按钮带 `refresh=true` 强制重拉；`source=builtin` 时标注「内置列表（上次拉取失败）」；
- 页面级三态：加载失败给「重试」按钮；`active=null` 时顶部提示「尚未配置可用的 AI 供应商，AI 功能暂不可用」。

**三、实现中发现的一处设计缺陷（本步最有价值的发现）**

首轮联调断言暴露：`custom` 卡片点「测试连通性」报**「请先选择模型」**，链路根本走不通。根因是三方约束叠加——① `custom` 在注册表里 `default_model=None`；② 后端 `test_connection` 无模型可用时直接拒绝；③ 模型列表接口**只认已保存的 Key**（未保存则 10012）。于是形成死结：**测试连通性要选模型 → 选模型要先保存 Key → 保存 Key 前又拉不到模型列表**。

修复：模型下拉加 `allow-create`，**支持直接手输模型 ID**。这既解开了死结，也顺带覆盖「断网回退内置表不含新模型」「列表外模型」等场景——自定义端点上的模型 ID 本就不可预知，只给下拉选择是不完整的。设计文档已同步该调整。

**四、设置页 `/settings`**

- 三组：语音交互 / 模拟面试 / 就业网抓取；**LLM 相关字段全部移除**（本步验收项）；
- 统一保存 + 快照 diff：只提交变更字段（接口为部分更新语义），布尔严格传布尔（接口对非布尔值一律 10001，不做隐式转换），讯飞凭据留空 = 不修改；
- 联动：`asr_provider=xunfei` 时才显示讯飞凭据；`tts_enabled=false` 时音色禁用；`crawl_enabled=false` 时目标地址禁用；就业网抓取组标注「系统级配置：任一账号修改后对全部账号生效」；
- 加载后写入 `useAppStore`，供后续步骤（13 陪练题量、22 语音开关）取用——`stores/app.js` 无需改动，现有 `setSettings` 即可。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 接口层 | `src/api/llmProviders.js` | 新增（6 方法：列表/保存/删除/激活/模型列表/连通性测试） |
| 接口层 | `src/api/settings.js` | 新增（2 方法：GET/PUT） |
| 组件 | `src/components/ProviderCard.vue` | 新增（折叠头 + 展开编辑区 + 就地结果） |
| 页面 | `src/views/AiConfig.vue` | 占位 → 完整实现 |
| 页面 | `src/views/Settings.vue` | 占位 → 完整实现 |
| mock | `mock/misc.js` | `getSettings` 补 `asr_key_set`（一处字段遗漏，非新功能）。**未扩充 llm-providers 的 mock**——mock 的定位是"后端未就绪时的界面走查"，现后端已跑在前面 |

> **`stores/app.js` 未改动**：计划里预估要改，实际现有的 `setSettings` 直接够用，未产生多余改动。

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过；`AiConfig` 7.47 kB（含卡片组件）、`Settings` 5.25 kB |
| 2 | **断言脚本自检** | 先跑一版把「供应商数量」期望值写成 99 | **如期报 FAIL**——证明断言确实在测东西，而非永远通过的假测试 |
| 3 | **真联调全量断言** | CDP 无头 Edge（1440×900）+ 真后端 + 本地 mock OpenAI 兼容供应商（8011，免真实 Key） | **37/37 通过** |
| 4 | 接口核对 | Key 经接口保存后查库 | 库中为 Fernet 密文（`gAAAAAB…`），接口只回 `key_set: true`，**无明文回显** |
| 5 | 截图留存 | 无头 Edge 1440×900 | 4 张：配置页全览（三家状态并存）、卡片展开态（含连通结果）、模型下拉、设置页 |

第 3 项覆盖的断言（节选）：12 家分组 5/4/1/1/1 · 折叠/展开 · **连通性测试成功**（`连通正常（模型：mock-model-a）`）· 模型列表拉到 mock 模型 · 首个保存的供应商自动生效 · **两家来回切换无需重填 Key**（TC-67 核心）· 激活位互斥 · 保存不改变激活项 · 删除当前激活项的二次确认文案含「删除后 AI 功能将不可用」· 取消不生效 · **无效 Key 就地报「API Key 无效或无权限」**（错误色）· 设置页三组且**不含任何 LLM 字段** · 讯飞凭据条件显示 · **题量保存后刷新回显**（落库）· **控制台 0 JS 错误**。

> **验证环境的坑（已记入 IS-24）**：首轮断言与截图跑在 **500×450** 的视口下——headless Edge 的 `--window-size` 参数未生效，而 226px 宽的内容区里中文逐字换行，一度看起来像布局缺陷（顶栏标题竖成三行）。改用 CDP `Emulation.setDeviceMetricsOverride` 固定视口后复验；并补了三条布局断言（视口宽度、卡片头部单行高度、无横向溢出）防复发。

### 决策与遗留

- **「配好就能用 AI」的入口独立成页了**：AI 配置页是步骤 10 之后所有 AI 功能的唯一前置——本步把 12 家供应商的 Key / 模型 / 连通性 / 切换全部打通，后续功能只需读当前激活配置；
- **折叠形态的连带收益**：模型列表因折叠而只需在展开时拉取，避免了进页面打 12 个网络请求；
- **手输模型是必要能力而非便利**（见「三」）：只做下拉选择会让 `custom` 供应商无法完成配置，这是设计阶段未预见的约束叠加；
- **音色下拉是过渡实现**：`GET /tts/voices` 属步骤 22，本步按 SRS §3.12 列举的三个音色硬编码，并对账号已存值做兜底（避免回显被静默清空）；步骤 22 落地后改为接口拉取；
- **遗留（转入后续步骤）**：
  1. **契约缺口**：`GET /llm-providers` 响应不含 `needs_key`，前端只能按 `provider === 'ollama'` 约定判断免 Key 供应商——已记入**问题记录 IS-25**，转后端评估是否补字段；
  2. **设置页的语音 4 项**（语音作答 / ASR 供应商 / 讯飞凭据 / 播报音色）对应的**功能本体在步骤 22**，本步只做配置读写——改了不会有即时反馈，属预期；
  3. **`/tts/voices` 音色列表接口**待步骤 22；
  4. **项目结构文档需登记 2 个新文件**（`components/ProviderCard.vue`、`api/settings.js`）——该文档属②类（交后端维护）。**经事先同意（2026-09-26）由后端写入**，已登记为 **IS-26** 转出（后端做台账回标时可一并处置）。

---

## 后端台账处置与新需求「校招情报」文档同步

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | **非开发计划步骤**：①处置后端新立的[前端待做事项](../07-工作日志/前端待做事项.md)台账；②新需求「校招情报」（SRS v1.11 FR-021/FR-022）的文档同步 |
| 对应分支 | `frontend` |
| 上游依据 | SRS **v1.11** / 系统设计 **v1.14** / 数据库设计 **v1.8** / 接口文档 **v1.12** §3.16 / 开发计划 **v2.13**（27 步 → 29 步） |

### 工作内容

**一、新需求：校招情报模块（27 步 → 29 步）**

后端把原「步骤 21 就业网抓取 + 宣讲会」重写为**多源采集层**，其后新增**步骤 22「校招情报（后端）」**与**步骤 23「校招情报（前端）」**，原 22~27 顺延为 24~29。前端侧的活主要在**步骤 23**：校招情报页 `/campus`（宣讲会 / 双选会 / 岗位三类 Tab + 列表·日历视图切换）、站内日历月视图、我的订阅管理、设置页源管理区块、概览区块改造，以及与投递 / JD 分析的业务打通；步骤 21 只有一处小改（概览宣讲会区块补「已变更」角标与来源站点标签）。

**二、处置后端待做事项台账（5 项完成、3 项前置未满足）**

| # | 事项 | 处置 |
|---|---|---|
| 23 | IS-23 的用例编号分配 | 分配 **TC-86**（登记入测试计划 v1.15 §2.2），IS-23 同步结案 |
| 26 | `api/profile.js` 与 `settings.js` 重复定义 | 核查确认**两个方法已无任何引用**（`Settings.vue` 走 `settings.js`、`Profile.vue` 只用画像两方法），按死代码**直接删除**——未采用"改从 settings.js 转出"方案，那只是给无人使用的导出加一层转出 |
| 28 | 测试计划随校招情报同步 + 测试日志索引重排 | 见下方产出表 |
| 29 | 设置页移除废弃的 `crawl_url` | `Settings.vue` 四处 + `mock/misc.js` 一处移除；该组标题由「就业网抓取」改「**信息采集**」（对齐 SRS v1.11 §3.12 措辞） |
| 30 | 项目结构 §5 的 VAD 旧口径 | 改为「按语速自适应：快 1.6s / 正常 2.65s / 慢 3.6s」并指向系统设计 §5.6 |
| 11 / 17 / 27 | 枚举计数用例、TC-65 补概览验证点、校招情报目录树登记 | **前置依赖未满足**（分别为步骤 26 练习模式、步骤 9 的 `stats`、步骤 23 前端落地），保持待处理并在台账「当前待处理」行注明原因 |

**三、测试计划新增 14 条用例（编号自 TC-73 起）**

按台账 #28 的要求分配编号并落成可执行的断言口径——**LLM 全 mock、抓取走本地 HTML/JSON 快照 fixture、全程不联网**：

| 分组 | 编号 | 覆盖 |
|---|---|---|
| 服务层 | TC-73~77 | 跨源去重与合并、变更检测（`content_hash`）、过期归档（**源失败时不归档**）、三厂商解析器离线断言、采集合规（NFR-016：UA / 限频 / robots / 不存正文） |
| 服务层 | TC-78~79 | 匹配打分纯函数（**字段缺失不加分也不隐藏**）、订阅匹配语义（维度间 AND、维度内 OR，同日不重复提醒） |
| API | TC-80~82 | 投喂三条路径（成功预览不入库 / 抽取失败 70003 / 链接被拒 70003）、**跨账号隔离**（投喂岗位换账号不可见、订阅不得命中他人投喂）、日历区间聚合（四类事件 / 空区间 / 边界） |
| 前端手工 | TC-83~85 | 校招情报页、源管理与手动触发、站内日历 |
| 补分配 | TC-86 | `/settings` 字段闭包与部分更新语义（归属**步骤 6**） |

同时**改写 TC-23**：原「就业网抓取失败静默降级」是单站点口径，改为「**多源采集与单源隔离**」——原口径完全没有单源隔离这条语义，而它恰是 FR-021 的核心失效行为。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 文档 | `docs/05-测试文档/测试计划.md` | v1.14 → **v1.15**（新增 14 条 / 改写 1 条 / TC-51 表数 19 → 22 / §6 改 AC-1~AC-15；**用例总数 72 → 86**） |
| 文档 | `docs/07-工作日志/测试日志.md` | 步骤索引按 **29 步版**重排（整体验收 = 步骤 29，语音 = 24、表达力 = 25、练习 = 26、新手指引 = 28） |
| 文档 | `docs/02-设计文档/项目结构.md` | v1.28 → **v1.29**（§5 VAD 口径，台账 #30） |
| 文档 | `docs/README.md` | 版本清单联动 2 行（项目结构 v1.29、测试计划 v1.15 且用例数 86） |
| 文档 | `docs/07-工作日志/问题记录.md` | IS-23 → ✅ 已解决（编号 = TC-86）；状态统计 22/2/2 → **23/1/2** |
| 文档 | `docs/07-工作日志/前端待做事项.md` | #23 / #26 / #28 / #29 / #30 回标已完成，「当前待处理」行更新为 #11 / #17 / #27 |
| 代码 | `frontend/src/views/Settings.vue` | 移除 `crawl_url`（`DIFF_KEYS` / `form` / `applySettings` / 模板），组标题改「信息采集」 |
| 代码 | `frontend/mock/misc.js` | `getSettings` 移除 `crawl_url` |
| 代码 | `frontend/src/api/profile.js` | 删除重复导出的 `getSettingsApi` / `updateSettingsApi` |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过（838ms） |
| 2 | 残留引用 | `grep -rn "crawl_url" src/ mock/` | 仅剩 2 处**注释**（说明该键废弃），无代码引用 |
| 3 | 删除安全性 | `grep -rn "from '.*profile'" src/` | 只剩 `Profile.vue` 引用画像两方法，被删的两个设置方法确无引用 |
| 4 | 编号连续性 | 逐一核对新增编号 | TC-73~TC-86 **连续无缺、无重号**，最大编号与用例总数一致（86） |
| 5 | **界面回归** | CDP 无头 Edge 1440×900 + 真后端 | **8/8 通过**：设置页三组（第三组已改「信息采集」）、**无「目标地址」与 `crawl_url` 字样**、抓取开关仍在、**保存链路仍工作**（题量 12 → 15 → 刷新回显 15，验后改回 12）、个人中心与画像区块正常（`profile.js` 改动无影响）、控制台 **0 JS 错误** |

### 决策与遗留

- **新需求的前端大活尚未开工**：校招情报页 + 日历 + 订阅 + 源管理是一整页新功能（步骤 23），本条目只做文档同步与台账处置，**未动实现**——按「文档先行」待后端步骤 21/22 的接口落地后再开工；
- **项目结构文档的两次改动是后端在台账里明确转出的**（#30 注明「该行描述前端实现、属前端章节，后端未动」；v1.27 变更记录亦写明「§3 前端目录树归前端侧」），故本次按文档规范改动并升版 v1.29 + 联动 README，而非先问——**若这不符合约定请告知**；
- **`crawl_url` 前端先移除、后端后移除**：前端已不读不提交，后端接口暂仍返回该字段（无害），其移除随步骤 21 落地；
- **遗留**：台账 #11 / #17 / #27 三条待前置依赖满足（见上表）。

---

## 步骤 8 迭代：AI 配置页试用反馈（模型列表 / 自动探测 / 删除提示）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | 步骤 8 交付后的**试用迭代**，三条反馈：模型列表的 Key 前置、保存时应自动测连通、删除提示信息不足 |
| 对应分支 | `frontend` |
| 上游依据 | 试用反馈 / 接口文档 §3.3 / 问题记录 **IS-27** |

### 工作内容

**一、模型列表为什么非要 Key（对设计的疑问，先查证）**

**不是前端加的锁，是接口契约**：`GET /llm-providers/{provider}/models` 的 Key 只取当前账号**已存配置**（`get_models` 里 `decrypt_text(row.api_key)`），不接受传入；而**同组的** `POST /llm-providers/test` 是接受传入的——同一页面两个探测接口口径不一，容易被读成前端限制。技术上模型列表也确需认证：供应商的 OpenAI 兼容 `GET /models` 要 Bearer Key（`ollama` 本地不需要、OpenRouter 列表公开，属少数派）。

**处置**：前端能做的先做（保存后自动拉取，见下），**根治转后端**——登记 **IS-27**，请后端给该接口加可选 `api_key`（与 `/test` 同口径），具体形式（query 参数 / 改 POST）由后端定，前端不猜实现。

**二、保存后自动探测（本次核心改动）**

原先要手动点「测试连通性」，现在**保存成功后自动拉模型列表 + 自动测连通性**，结果就地展示——用户填完 Key 点一次保存就能看到「连通正常（模型：xxx）」和可选模型，不必再点两下。

两处实现细节：

- **不把测试合并进保存请求**：保存应当只负责"存"（快速返回）。合并的话，测试超时会拖累保存，且测试失败会让人误以为配置没存上。现在是保存成功后**并行**触发两个探测，各自更新各自的状态；
- **`loadModels` 加 `force` 参数**：保存成功那一刻 Key 刚落库、但 `props` 还没随页面重拉刷新，照常检查 `key_set` 会被误拦——`force=true` 跳过该前置检查。

**三、删除提示补全**

原文案只说「删除后 AI 功能将不可用」——反过来说，**没说删掉什么、能否恢复**。改为两段式：

- 固定段：将一并删除该供应商的 **API Key、所选模型、自定义端点与模型列表缓存**；
- 后果段：激活项 →「该供应商当前正在使用，删除后 AI 功能将不可用」；非激活项 →「删除后需重新填写才能使用」。

多行提示用 `dangerouslyUseHTMLString` 渲染，但 **HTML 内不含任何动态内容**（供应商名只出现在纯文本标题里），无注入面。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 代码 | `src/components/ProviderCard.vue` | `onSave` 加自动探测；`loadModels` 加 `force`；删除提示改两段式；模型列表提示改「需凭 Key 查询」 |
| 文档 | `docs/07-工作日志/问题记录.md` | 新增 **IS-27**（转后端），⏳ 统计 1 → 2 |
| 文档 | `docs/05-测试文档/测试计划.md` | v1.15 → **v1.16**（TC-67 补三处口径：删除范围 / 自动探测 / Key 前置） |
| 文档 | `docs/README.md` | 版本清单联动 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过（829ms） |
| 2 | **自动探测** | CDP 真后端 + 本地 mock 供应商 | **11/11 通过**：保存后**未点任何按钮**即出现「连通正常（模型：mock-model-a）」；模型下拉自动出现 `mock-model-a` / `mock-model-b`（保存前 `key_set=false`，同时验证 `force` 生效） |
| 3 | 删除提示 | 同上，读确认框 DOM | 标题「删除「自定义」的配置」；正文含「API Key、所选模型、自定义端点与模型列表缓存」与「删除后 AI 功能将不可用」；取消后卡片仍在 |
| 4 | 未配 Key 提示 | 同上 | 展开未配置的供应商 → 提示含「需凭 Key 查询」 |
| 5 | 控制台 | CDP 全程采集 | **0 JS 错误** |

### 决策与遗留

- **自动探测是"验证刚存的配置"，不是"保存的一部分"**：这个区分决定了实现方式——并行触发、不阻塞保存反馈、失败也不影响保存结果；
- **删除提示的 HTML 渲染是安全的**：多行需要 HTML，但动态内容只走纯文本标题，HTML 段全是固定字符串；
- **遗留**：IS-27 待后端给 `/models` 加可选 Key 参数；接口定稿后前端放开「未保存也能拉」——提示文案已写成中性表述（「填写 Key 后会自动拉取」），届时无需再改。

---

## 台账 #31：模型列表「临时探测」前端跟进（IS-27 后端已落地）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | 后端修复 IS-27 并合并后的**前端跟进**（[前端待做事项](../07-工作日志/前端待做事项.md) **#31**） |
| 对应分支 | `frontend` |
| 上游依据 | 接口文档 **v1.13** §3.3（`GET /llm-providers/{provider}/models` 新增可选 query 参数 `api_key` / `base_url`） |

### 工作内容

**一、背景**

上一条迭代里提出「模型列表为什么非得输入 Key 后才能拉」，查证是接口只认已存 Key。**后端已按 IS-27 闭环**：给该接口加两个可选 query 参数，**传任一即「临时探测」——不读缓存、不写缓存、不落库**；不传则行为完全不变。未采纳「改 POST」方案（GET 只读语义成立、向后兼容、前端改动最小）。

**二、前端三项改动**

| 改动 | 说明 |
|---|---|
| `listModelsApi` 支持临时探测参数 | 入参改为对象（`{ refresh, apiKey, baseUrl }`），非空才拼进 query——接口约定「传空串等同未传」 |
| `loadModels` 携带参数并放开拦截 | **只在尚未保存配置时携带**：`key_set=true` 不传（传了会绕过 24h 缓存、每次实时拉取）；`base_url` 与生效值相同时不传。原「需凭 Key 查询」的前置拦截改为「需 Key 却既没存也没填」才拦 |
| Key 输入框**失焦即拉** | 粘贴 Key 后点别处，模型列表自动出来——「填了就能拉」的入口，不必先保存 |

**保存后的自动拉取沿用 `force` 路径**（保存成功那一刻配置刚入库、但 `props` 未刷新），且**不带临时参数**——实测请求 URL 无 `api_key`，走的是已存配置与缓存。

**三、测试侧同步**

测试计划 **v1.17** 新增 **TC-87**（台账要求"编号由测试侧分配"）：覆盖未保存配置传 Key 可拉取、无效 Key → 10013 不回退、`custom` 需一并传端点、**探后不污染缓存**、不传参行为不变、空串等同未传。用例总数 **86 → 87**。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 代码 | `src/api/llmProviders.js` | `listModelsApi` 对象入参 + 两个临时探测参数 |
| 代码 | `src/components/ProviderCard.vue` | `loadModels` 条件传参、放开前置拦截；Key 输入框 `@blur` 自动拉取 |
| 文档 | `docs/05-测试文档/测试计划.md` | v1.16 → **v1.17**（新增 TC-87） |
| 文档 | `docs/README.md` | 版本清单与用例数联动（87 条） |
| 文档 | `docs/07-工作日志/前端待做事项.md` | **#31 回标已完成**，「当前待处理」行更新为 #11 / #17 / #27 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过（833ms） |
| 2 | 后端临时探测已生效 | curl 直连（**重启后端后**） | 传 `api_key` + `base_url` → `code=0`、`source=remote`、拉到 mock 模型；**不传参** → 仍 `10001`（行为不变） |
| 3 | **前端临时探测链路** | CDP 真后端 + 本地 mock 供应商 | **12/12 通过**：失焦后请求带 `api_key` 与 `base_url`、**未保存状态下已能选到模型**、此时状态标签仍是「未配置」、保存后自动拉取**不带**临时参数、控制台 0 JS 错误 |
| 4 | 编号与版本 | 脚本扫描 | TC-01~**TC-87** 连续无缺；变更记录版本行顺序正确 |

> **首轮验证失败及其教训**：首跑时失焦未触发请求，排查中发现 **8000 端口跑的还是旧后端进程**——`uvicorn` 不带 `--reload` 不会热重载，合并新代码后必须重启。这与步骤 4 迭代记过的坑**完全相同**（当时表现为"代码已改而行为未变"）。重启后端后重跑全过。**此后凡合并后验证，第一步先确认服务进程的启动时间晚于代码改动时间。**

### 决策与遗留

- **「临时探测」只在未保存时启用**，不是"总是带上表单值"：已存配置传参会让每次都绕过缓存，把 24h 缓存的收益丢掉；
- **失焦即拉而非输入即拉**：`input` 事件会在每次击键触发，对"粘贴 Key"这个主场景反而多余；`blur` 的时机正好是"填完了"；
- **遗留**：无。IS-27 已由后端闭合，前端跟进完成；测试计划侧 TC-87 的实现随步骤 10 之后的 AI 用例批次落地。

---

## 步骤 9：今日概览（前端）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | **开发计划步骤 9 的前端部分**。后端 `GET /overview` **尚未实现**，本步**前端先行**——按接口文档 v1.13 §3.4 实现，用 mock 层支撑界面走查，后端就绪后补真联调（同步骤 7 的做法） |
| 对应分支 | `frontend` |
| 上游依据 | SRS **v1.11** §3.1（FR-001）；系统设计 v1.14 §4.2·4.4·4.5；接口文档 **v1.13** §3.4；测试计划 v1.17 TC-27 |
| 设计文档 | 设计稿与实现计划（过程文档，不入库） |

### 工作内容

**一、布局：分区纵列（经讨论确定）**

统计卡横排 → 待面试/笔试 → 跟进提醒 → 三栏（错题 / 校招情报 / 快捷入口）。理由是列表类区块需要横向空间（公司 · 岗位 · 天数要同行），挤进窄栏必截断；统计卡本就是"一行指标"。

**二、概览页六个区块**

| 区块 | 本步的状态 |
|---|---|
| ① 状态统计卡 | 5 张横排，左侧状态色条（取 `constants/application.js`，不新造色）；点击进投递管理 |
| ② 待面试 / 笔试 | **明日项置顶**（前端排序 + 浅色底 + 「明天」标签），其余保持后端升序 |
| ③ 跟进提醒 | **按天数降序**（拖得越久越该跟进，排最前） |
| ④ 错题复习 | 显示 `wrong_question_count`，0 时空态；内容随步骤 14/20 填充 |
| ⑤ 校招情报 | 占位「将在后续步骤上线」；随步骤 21/23 填充 |
| ⑥ 快捷入口 | **本步即做**——纯静态跳转、零依赖，SRS §3.1 已列 |

**三、`useOverviewStore`（新建）**

建立它的实际理由不是"设计文档里有"，而是**侧栏「今日待办」与概览页要读同一份数据**——两处各调一次会在同一屏发两个相同请求，数值还可能不一致。

**轮询范围与设计原文的差异**：系统设计 §4.2 写的是"含轮询刷新，30s 间隔"，未限定范围。实现为**仅概览页挂载期间轮询**——按字面做成 store 内全局轮询的话，用户在投递页 / JD 分析页操作时也会持续请求一个没人看的接口。设计意图（页面数据不陈旧）保留，无谓请求避免。

**四、侧栏「今日待办」接上数据（IS-06 前端侧就绪）**

`SideNav.vue` 改为读 store（并删掉自己那份 `/overview` 请求与 `request` import）。侧栏是全局的，故在 `onMounted` 时若 store 无数据则触发一次 fetch——用户直接落在投递页时也要有待办。**行为不变**：无数据即整块隐藏（系统设计 §4.4），接口未就绪期间它仍是隐藏的。

**五、移除概览页的「后端连接状态」卡片**

那是**步骤 1 的产物**——当时它是验证 `request.js` 全链路（axios → baseURL → Vite 代理 → 拦截器 → 解包）的唯一手段，工作日志中已注明"步骤 5 实现真正概览页时在此基础上扩展，非废弃代码"。现在整页数据都来自 `/overview`，**联动全链路**——链路若断，页面自己就给错误态与重试，那张卡片的存在意义已被覆盖。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 状态 | `src/stores/overview.js` | 新增（`useOverviewStore`：data / loading / error + fetch(silent) + reset） |
| 页面 | `src/views/Overview.vue` | 占位 → 完整实现（六区块） |
| 组件 | `src/components/SideNav.vue` | 待办改读 store；删去自身的 `/overview` 请求 |
| 接口层 | `src/api/overview.js` | 支持传入 axios config（侧栏要 silent） |
| 状态 | `src/stores/user.js` | 登出时一并 `useOverviewStore().reset()`（账号切换无残留） |
| mock | `mock/misc.js` | `overview` 返回有代表性的样例数据 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过（869ms）；`Overview` 由占位 0.28 kB → **4.91 kB**（+ CSS 2.46 kB） |
| 2 | **断言脚本自检** | 先跑一版把「统计卡数量」期望值写成 99 | **如期报 FAIL**——排除"断言永远通过"的假测试 |
| 3 | **界面走查（mock 层）** | CDP 无头 Edge 1440×900 + `VITE_USE_MOCK=true` | **17/17 通过** |
| 4 | 错误态（真后端下） | `VITE_USE_MOCK=false` 打开概览 | 显示错误文案 + **重试按钮**（后端 `/overview` 未实现 → 404），非白屏、不卡死 |
| 5 | 截图留存 | 无头 Edge 1440×900 | 3 张：概览页整页、视口、错误态 |

第 3 项的关键断言（**mock 数据刻意造序**，用来验证前端排序逻辑真的在起作用，而非照搬后端顺序）：`upcoming_events` 里**明日那条排在后面** → 断言首条是它且带「明天」标记 ✓；`follow_ups` 的 **4 天排在 6 天前面** → 断言首条是 6 天那条 ✓。其余：五张卡计数与接口一致（5/2/3/1/4）、错题空态、校招占位、快捷入口 5 个、**侧栏待办接上 2 条**、统计卡与「新增投递」跳转、控制台 **0 JS 错误**。

> **一处断言写错并修正**：最初断言「点新增投递 → URL 带 `action=new`」失败——实际 URL 是 `/applications`。原因是投递页消费该 query 后**立即 `router.replace` 清掉**（步骤 4 迭代的既定行为，为的是"再次点击仍能触发"）。改为断言「进入投递页 **且新增表单已打开**」，这才是该交互的可观察结果。

### 决策与遗留

- **前端先行的边界**：本步的"通过"全部基于 **mock 层**，证明的是"前端链路与接口契约一致"，**不证明与真实后端联通**；
- **轮询只在本页生效**（见上），是对设计原文的一处收窄，理由已写入设计文档；
- **统计卡的"对应筛选"未额外做控件**：SRS 写"点击跳转投递管理对应筛选"，而投递页是看板形态——5 列即 5 个状态，跳过去对应列就在眼前。为此在已验收的投递页上加一套 status 筛选，既与列语义重复又要担回归风险，故不做；
- **遗留（转入后续）**：
  1. **真联调待后端**——`GET /overview` 就绪后须以 `VITE_USE_MOCK=false` 重跑第 3 项的断言（**本步最关键的遗留**）；
  2. **IS-06 前端侧已就绪**，结案条件（后端接口落地）未满足，状态维持 🚫 不变；
  3. `top_job_postings` / `match_reminder_count` 两个字段随步骤 22/23 填充；
  4. 概览页后续还会被改两次（步骤 20 接入错题/面试提醒、步骤 21/23 改造校招区块）——本步已把六个区块的位置一次排好，后续只填内容。

---

## 步骤 9 迭代：真联调与时间格式修复

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | 步骤 9 的**真联调**（补齐此前 mock 走查留下的遗留项）+ 联调中发现的前端缺陷修复 |
| 对应分支 | `frontend` |
| 上游依据 | 接口文档 **v1.14** §3.4（含「区块判定口径」四条）/ 台账 **#36** |

### 工作内容

**一、真联调（补齐遗留）**

后端当日交付 `GET /overview` 后，关闭 mock 以真后端重跑——概览页与侧栏待办 **12/12 通过**：统计卡计数与接口一致（3/1/1/0/1）、待面试升序且明日项带「明天」标记、跟进按 5→3 天降序、侧栏待办 2 条、个人中心三张卡显示真实计数（6/0/0）、控制台 0 error。

**二、联调暴露的缺陷：页面直接输出后端原始时间**

待面试列表显示成 `2026-09-27T14:00:00`——**换 MySQL 后 Pydantic 的序列化变成了 ISO 格式**（SQLite 时代是空格分隔）。

**mock 为什么没发现**：mock 数据是手写的，当时用的是空格格式，与真后端已经不一致——**mock 与真实契约的偏差会掩盖问题**，这正是真联调不可替代的地方。

**修复**：抽公共工具 `src/utils/datetime.js`（`shortDateTime` / `datePart`），概览页、侧栏待办、投递看板三处统一使用（投递看板的 `shortTime` 原本是局部实现，恰好因位置切片对两种分隔符都成立而没出问题，本次一并收敛为单一出处）。同时把 **mock 数据改为 ISO 格式**，与真后端对齐，避免再次掩盖。

**三、顺带发现：本机后端进程跑的是旧代码**

联调首轮 `/overview` 返回 404——排查发现 8000 端口上的实例启动于 **17:35**，而步骤 9 代码落地于 **17:47**（差 12 分钟），uvicorn 不带 `--reload` 不会热重载。这与步骤 4 迭代记过的坑**同类**。重启后正常。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 工具 | `src/utils/datetime.js` | 新增（`shortDateTime` / `datePart`，含两种分隔符的说明） |
| 页面 | `src/views/Overview.vue` | 待面试时间改用 `shortDateTime`；`isTomorrow` 改用 `datePart` |
| 组件 | `src/components/SideNav.vue` | 待办时间改用 `shortDateTime` |
| 组件 | `src/components/StatusKanban.vue` | 删局部 `shortTime`，改用公共工具 |
| mock | `mock/misc.js` | 时间改 **ISO 格式**（与真后端对齐） |
| 文档 | `docs/05-测试文档/测试计划.md` | v1.18 → **v1.19**（TC-65 补「数据概览卡片真实计数」验证点；新增 `tests/test_overview.py` 的登记） |
| 文档 | `docs/README.md` | 版本清单联动 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过（932ms） |
| 2 | **真联调** | CDP + 真后端 | **12/12 通过**（见上）；截图 2 张（概览页 / 个人中心） |
| 3 | 时间格式修复 | 同上 | 显示为 `09-27 14:00`，不再是 ISO 原始值 |
| 4 | **投递页未受影响** | CDP 打开 `/applications` | 6 张卡片、5 列；排期显示 `笔试 09-29 10:00` / `面试 09-27 14:00`，无 ISO 原始值与完整年份；控制台 0 error |
| 5 | **mock 模式未受影响** | 切 `VITE_USE_MOCK=true` 重启后重跑步骤 9 走查 | **17/17 通过**（侧栏待办格式同样正确） |
| 6 | 后端测试 | `pytest -q`（测试侧） | **161 passed / 0 failed** |

### 决策与遗留

- **时间格式收敛为单一出处**：三处显示逻辑原本各自为政（投递页局部函数、概览页无格式化、侧栏无格式化），本次抽为 `utils/datetime.js`；`shortDateTime` 对 ISO 与空格两种分隔符都成立，故历史数据与新数据都能正确显示；
- **mock 必须与真实契约对齐**：本次的教训已写进 mock 注释——mock 的价值在于"离线也能走查真实形态"，一旦它与契约脱节，就会把问题藏到联调才暴露；
- **遗留**：无。步骤 9 至此前后端闭环（前端页面 + 后端接口 + 用例 + 真联调）。

---

## 步骤 9 迭代：概览页空态引导（试用反馈）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | 步骤 9 交付后的**试用迭代**（反馈：「今日概览没有数据时的页面不是很好看啊，太单调了」） |
| 对应分支 | `frontend` |
| 上游依据 | 试用反馈 / SRS §3.1（FR-001）/ 测试计划 **v1.20** 的 TC-93 |

### 工作内容

**一、问题定位**

截图确认：新账号（零投递）打开首页时，5 张统计卡全是 `0`、两个列表各剩一行灰字、快捷入口被挤在右下角——**唯一的"可用入口"面积最小，而没有信息量的部分占据了首屏最显眼的位置**。

根因是布局照「有数据」的情形设计：统计卡的设计前提是"一屏看到各状态分布"，无数据时这个前提不成立。

**二、方案（经讨论确定：空态引导卡）**

| 项 | 设计 |
|---|---|
| 触发条件 | `stats.application_count === 0`——**不是**「统计卡恰好全 0」（那可能只是各状态暂无记录） |
| 替换范围 | 统计卡 + 待面试 + 跟进列表 → **一张引导卡**；快捷入口也省去（引导卡里已是主要入口） |
| 引导卡内容 | 标题「开始你的求职管理」+ 一句价值说明 + 三个入口（新增投递 / JD 匹配分析 / 模拟面试）+ 批量导入提示 |
| 恢复 | 有投递后自动回到常规布局（同一份数据驱动，无额外状态） |

**三、过程中的一处实测发现**

首版引导卡下方留白仍大，加大 `min-height` 撑满内容区后反而**整页出现滚动**（卡片底部溢出到视口外）。凭肉眼只看到"内容偏下"，**实测几何**才定位到真因：`box-sizing` 默认 `content-box`，`min-height` 只管内容区，加上上下 padding 80px 后总高变成 860 > 视口 900 的内容区高度。

修复：显式 `box-sizing: border-box` + `min-height: calc(100vh - 100px)`（视口 − 顶栏 56 − 内容区上下 padding 40，留 4px 余量）。复测：卡片高 800、底部 876（视口 900 内）、上下留白各 312（完全对称）。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 页面 | `src/views/Overview.vue` | 新增 `isEmpty` 判定与 `.starter` 引导卡（含 6 条样式）；常规布局包进 `v-else` |
| 文档 | `docs/05-测试文档/测试计划.md` | v1.19 → **v1.20**（新增 **TC-93**，用例总数 92 → 93） |
| 文档 | `docs/README.md` | 版本清单与用例数联动 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过（935ms） |
| 2 | **空态** | CDP + 真后端（新注册账号） | **15/15 通过**：引导卡显示、统计卡与列表**不渲染**、标题/说明/三按钮/导入提示齐全、**页面无纵向滚动**、点「新增投递」进投递页并打开表单 |
| 3 | **有数据态** | 同上（link9 账号，6 条投递） | 常规布局正常（统计卡在、引导卡不在、五个区块都在），**无回归** |
| 4 | 控制台 | CDP 全程采集 | 0 JS 错误 |

> **首轮 2 条"失败"是我断言写反了**：用了 `!document.querySelector(...)` 又期望 `false`（双重否定），实际行为一直是对的。

### 决策与遗留

- **空态判定用投递总数而非"统计卡全 0"**：后者在"刚清空所有投递记录"等场景会误判；
- **空态与常规布局互斥、由同一份数据派生**：没有引入额外的"是否已看过引导"之类的本地状态，故不会出现两者同时显示或都不显示；
- **`box-sizing` 的教训**：`min-height` + `padding` 在默认 `content-box` 下会叠加，凡"撑满可用高度"的容器都要显式 `border-box`——这一条靠肉眼看不出来，是实测几何才定位到的；
- **遗留**：无。

---

## 步骤 9 迭代：概览页布局调整 + IS-29 登记（招聘信息归属讨论）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | 试用中提出**设计层问题**（「那些招聘信息该放在哪里？会不会和投递管理功能重复？」）后的分析、登记与布局调整 |
| 对应分支 | `frontend` |
| 上游依据 | SRS §3.2（投递管理）·§3.18（校招情报业务打通）；问题记录 **IS-29** |

### 工作内容

**一、分析结论：不重复，是「候选池 → 进度追踪」的两段式**

| | 投递管理 | 校招情报 |
|---|---|---|
| 回答的问题 | 我投的走到哪一步了 | 外面还有什么、我还没投的 |
| 数据来源 | 我录入（表单 / 一句话 / 导入） | 自动抓取 + 我投喂的 |
| 主角 | **状态**（5 态流转） | **信息**（时间、地点、匹配度、是否改期） |

转换点即 SRS 已约定的「加入投递」（预填表单）+ 岗位卡片的「已投递」回标。

**二、但"看起来像"的风险是真的，来源很具体**

①同一条信息投递前后出现在两个页面；②两页控件形似且**都有名为 `status` 的字段**（`ACTIVE/CHANGED/EXPIRED` vs 投递 5 态）——同名不同义最易混；③概览页若同时展开「待面试」与「匹配岗位」并排放着。

**三、登记 IS-29 转后端写设计文档**（经讨论确定：先登记、由后端落笔）

五条建议：位置（招聘信息归 `/campus`，不进投递页）/ 视觉语言分离（看板+状态色 vs 列表·日历+时间匹配度）/ **岗位信息状态不复用投递状态色**（`--s-*` 为投递专属）/ **已投递岗位沉底或弱化** / 概览页只放入口不展开。

**四、概览页本次只调一处：校招情报从三栏提为独立区块**

理由：它的最终形态（SRS §3.1）是「宣讲会/双选会列表 + 匹配度最高的若干条岗位 + 命中订阅计数」，**属列表级内容量**；塞在 1/3 宽度（约 370px）里会又高又窄，且与旁边的「错题复习」（只有一句数字）体量根本不匹配。

调整为：统计卡 → 待面试 → 跟进 → **校招情报（满宽，与待面试同级）** → 两栏（错题复习 / 快捷入口）。占位文案同步改为「多校宣讲会 / 双选会与匹配岗位将在后续步骤上线」，并预留了「查看全部 → `/campus`」入口（在 `campusEvents.length` 分支内，数据到位后才渲染，届时该路由已建成）。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 页面 | `src/views/Overview.vue` | 校招情报提为独立区块、底部三栏改两栏（新增 `.overview__grid--two`） |
| 文档 | `docs/07-工作日志/问题记录.md` | 新增 **IS-29**（转后端写设计文档），⏳ 统计 2 → 3 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过（855ms） |
| 2 | 区块顺序 | CDP + 真后端 | **6/6 通过**：顺序为「待面试 / 该跟进 / **校招情报** / 错题复习 / 快捷入口」；底部**两栏**；校招区块**与待面试同宽（1166px，满宽）**；无横向溢出；控制台 0 error |
| 3 | 空态未受影响 | 同上 | 引导卡仍正常显示 |

### 决策与遗留

- **布局反映最终形态，避免二次改动**：本次调整的直接收益是——**步骤 23 只需往校招区块里填内容**，不必再动一次布局；
- **「查看全部」入口现在不渲染**（在 `campusEvents.length` 为真的分支里），所以无需提前建 `/campus` 路由——步骤 23 建路由时它自然生效；
- **IS-29 的五条建议待后端写入设计文档**，前端在步骤 23 按文档实现；
- **遗留**：无。步骤 9 的页面至此稳定（常规布局 + 空态引导 + 校招区块就位）。

---

## 空态补校招情报入口 + 校招情报路由与模块色落地（试用反馈）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | 试用反馈驱动的空态迭代 + 为步骤 23 预置入口 |
| 对应分支 | `frontend` |
| 上游依据 | 试用反馈 / 系统设计 §4.4（菜单分组）·§4.5.1（色彩）/ 问题记录 **IS-29** |

### 工作内容

**一、试用中指出的问题**

> 「没数据的时候是不是也要显示校招信息啊，没数据让我导入或者投递，我总得知道有啥能投的吧」

这个反馈点到了一个**流程顺序**问题：原先空态引导卡只给「新增投递 / JD 分析 / 模拟面试」——都是**记录与加工**类入口，而新用户真正卡住的地方是**「我投什么」**。真实求职顺序是「先找到岗位 → 再投 → 再记录」，空态却把它倒过来了。

**二、空态引导卡改为双路径**

主按钮换成 **「查看校招情报」并排在最前**（找机会是第一步），次按钮保留「＋ 新增投递」「JD 匹配分析」；底部提示改为说明校招情报的作用（聚合多校宣讲会/双选会/岗位、可从岗位一键加入投递）与批量导入路径。

**三、配套：让「校招情报」入口从现在就可达**

若只改文案，「查看校招情报」就是个死链。故一并落地：

| 项 | 内容 |
|---|---|
| `/campus` 路由 | 新建 `Campus.vue` 占位页（同 `AiConfig` 在步骤 7 的做法），说明对应开发计划步骤 23 |
| 侧栏项 | 由路由表派生，自动出现在**求职流程组、概览之后、投递之前**——与系统设计 §4.4 分组表的位置一致 |
| 模块色 | **`--m-campus: #93AE55`**（黄绿）写进 `tokens.css` |

**模块色的选取依据**：现有模块色相分布为 15°（陶橙）/ 40°（琥珀）/ 150°（陪练绿）/ 180°（青）/ 215°（蓝）/ 270°（紫）/ 340°（玫红），**黄绿一带为空缺**；且与陪练的青绿 `#5BAE84` 区隔明显，符合「提亮降饱和」的全站调性。

> **顺带补上了系统设计的两处「待登记」**：§4.4 菜单分组表与 §4.5.1 色彩表中「校招情报」两行都标着「色值以 `tokens.css` 为准，待前端侧登记后回写本节」——该值现已定义，已记入 **IS-29** 请后端回写。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 页面 | `src/views/Campus.vue` | 新增（步骤 23 的占位页） |
| 路由 | `src/router/index.js` | 新增 `/campus`（求职流程组、概览之后） |
| 样式 | `src/styles/tokens.css` | 新增 `--m-campus: #93AE55` |
| 页面 | `src/views/Overview.vue` | 空态引导卡改双路径（主按钮 = 查看校招情报） |
| 文档 | `docs/05-测试文档/测试计划.md` | v1.21 → **v1.22**（TC-93 同步空态口径） |
| 文档 | `docs/07-工作日志/问题记录.md` | IS-29 补充「模块色已定义，待回写系统设计」 |
| 文档 | `docs/README.md` | 版本清单联动 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过（856ms），`Campus` chunk 0.35 kB |
| 2 | 空态 | CDP + 真后端（新注册零投递账号） | **11/11 通过**：引导卡按钮为 `["查看校招情报","＋ 新增投递","JD 匹配分析"]`（校招为主按钮）、提示含「宣讲会」说明、**侧栏含校招情报且顺序为 概览 → 校招情报 → 投递管理**、点主按钮落 `/campus` 且占位页渲染 |
| 3 | 有数据态 | 同上（demo 账号） | 区块顺序不变、引导卡不显示、**无回归** |
| 4 | 控制台 | CDP 全程采集 | 0 JS 错误 |

### 决策与遗留

- **主按钮给了「未实现」的功能**：校招情报页目前是占位，但**引导方向是对的**——用户需要先知道「有什么能投」；占位页也如实说明了它对应的步骤，不会让人以为坏了。步骤 23 落地后此入口自然生效，无需再改；
- **模块色的取舍**：不选与投递蓝/品牌紫相近的靛蓝（易混），选黄绿——色相空缺且语义贴合「机会、新鲜信息」；
- **遗留**：系统设计 §4.4 / §4.5.1 两处「待登记」待后端回写（已记入 IS-29）。

---

## 概览页信息架构调整（试用中提出「应该展示什么」）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | 用户就概览页的信息架构提问（「你觉得今日概览界面应该需要展示什么呢」）后的分析与调整 |
| 对应分支 | `frontend` |
| 上游依据 | SRS §3.1（FR-001）；测试计划 **v1.23** 的 TC-94 |

### 工作内容

**一、分析用的判据**

SRS §3.1 给概览页的定位是「**打开就知道今天该干什么**」，页面又名「**今日**概览」。故每个区块都该自问：**它回答的是"干什么"，还是别的？**

按此审视原六个区块，发现两处问题：

| 区块 | 判定 |
|---|---|
| 待面试 / 笔试 | ✓ 行动项（明天有面试）——**核心** |
| 该跟进一下了 | ✓ 行动项——**核心** |
| 错题复习 | ✓ 行动项，但 0 道时不该占位置 |
| 校招情报摘要 | ✓ 新机会（增量信息）|
| 投递进度统计 | ✗ **现状**，不指示行动；且统计全部历史，与「今日」不符 |
| 快捷入口 | ✗ **导航**，5 项中 4 项与侧栏重复 |

**二、五处调整**（经讨论确定）

1. **移除首屏 5 张大统计卡**——它是现状而非行动，且占了最显眼的位置；
2. **移除「快捷入口」区块**——其中 JD 分析 / 模拟面试 / 八股陪练 / 错题抽问**侧栏已有**，而侧栏是常驻的，再放一遍没有信息增量；唯一有独立价值的「新增投递」顶栏本就有按钮；
3. **新增底部状态条**（紧凑一行：错题到期 + 投递各状态计数）——投递进度**不能删**（"今天没事可做"是常态，总得有东西看），但正确的位置是**兜底内容**而非首屏主角；
4. **各内容区块改为「有内容才显示」**——概览页每天都要打开，空区块（「暂无到期错题」之类）就是噪音；
5. 新增一句「今天没有需要处理的事，安心准备下一场面试吧」作为全空的兜底文案。

**调整后的形态**：待面试（明日置顶）→ 该跟进 → 校招情报 → 底部状态条。呈现的是"**今天有几件事**"，而不是"一堆面板 + 几行 0"。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 页面 | `src/views/Overview.vue` | 五处调整；删 `.stat-*` / `.shortcuts` / `.overview__grid*` 与三个不再使用的样式（`__empty` / `__meta` / `__metric`），新增 `.strip-item*` / `.overview__quiet` / `.overview__more` |
| 文档 | `docs/05-测试文档/测试计划.md` | v1.22 → **v1.23**（新增 **TC-94**，用例总数 93 → 94） |
| 文档 | `docs/README.md` | 版本清单与用例数联动 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过（890ms） |
| 2 | **有内容态**（demo） | CDP + 真后端 | **13/13 通过**：首屏无统计卡、无快捷入口、有内容的区块才渲染（校招与错题因无内容/未实现而不出现）、底部状态条两条（错题「2 道到期」+ 投递五状态计数） |
| 3 | **「今天没事」态** | 同上（造「昨天投递一条、无未来事件、无错题」的账号） | 四个内容区块**全部不渲染**（0 张卡）、显示「今天没有需要处理的事」、**底部投递进度条仍在**、且**不是**空态引导卡（它有投递记录） |
| 4 | **空态**（零投递） | 同上 | 引导卡仍正常（未受影响） |
| 5 | 控制台 | CDP 全程采集 | 0 JS 错误 |

### 决策与遗留

- **判据是「行动 vs 现状」**：概览页不是仪表盘，而是**待办清单 + 机会提示**——这是本次调整的核心依据，也解释了为什么"统计卡好看但不该占首屏"；
- **投递进度保留但降级**：它确实有价值（一眼看到求职全局），只是位置错了。放在底部状态条既保留价值又不抢焦点，还顺带解决了「今天没事时页面全空」；
- **「有内容才显示」的代价**：用户可能一时找不到某个功能——但侧栏是常驻的导航，概览页不必兼任导航；
- **遗留**：无。概览页至此完成信息架构层面的收敛（三种状态：空态引导 / 有事可做 / 今天没事）。

---

## 概览页修正：校招情报区块改为常驻（前端自纠）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | **上一轮「信息架构调整」引入的缺陷，实测发现后自查修正** |
| 对应分支 | `frontend` |
| 上游依据 | 试用反馈 / 测试计划 **v1.24** 的 TC-94 / 问题记录 **IS-30** 补充 |

### 试用反馈（一针见血）

> 「这样这个功能不还是跟投递管理冲突吗，我想要的是这个今日概览不只是提醒我今天干啥，还有能够获取一手招聘新闻啥的入口」

**问题成立**：上一轮做「有内容才显示」时，**把校招情报区块也套了进去**——而它是本页唯一的**信息获取入口**，结果整页只剩投递相关内容（待面试 / 跟进 / 投递统计），与投递管理确实看不出区别。

### 根因：两类区块的显隐规则被混为一谈

| 区块类型 | 没内容时 | 为什么 |
|---|---|---|
| **待办类**（待面试 / 跟进 / 错题） | 该**收起** | 没待办就是没待办，空区块是噪音 |
| **入口类**（校招情报） | **也该在** | **入口本身就是内容**——藏起来等于砍掉一条路径 |

上一轮只想着"减少噪音"，把两类一视同仁了。

### 修正

1. **校招情报区块始终显示**（去掉 `v-if`）；
2. 无数据时以一句说明承担"讲清这是干什么"的职责——「聚合多所高校就业网的宣讲会 / 双选会 / 岗位，按画像匹配度排序」，入口按钮文案为「**去发现机会**」（有数据时是「查看全部」）；
3. **「今天没事」的判定排除校招**（它是常驻入口，不算待办），提示文案补一句引导——「也可以去校招情报看看新机会」。

### 产出

| 项 | 位置 | 变化 |
|---|---|---|
| 页面 | `src/views/Overview.vue` | 校招区块常驻、无数据说明与入口文案、判定排除校招；新增 `.overview__hint` 样式 |
| 文档 | `docs/05-测试文档/测试计划.md` | v1.23 → **v1.24**（TC-94 改写为「区块显隐规则」，区分待办类与入口类） |
| 文档 | `docs/07-工作日志/问题记录.md` | IS-30 补充「入口类区块该常驻」，并请后端补进系统设计 §4.8 |
| 文档 | `docs/README.md` | 版本清单联动 |

### 验证

| # | 验证项 | 手段 | 结果 |
|---|---|---|---|
| 1 | 构建 | `npm run build` | 通过（839ms） |
| 2 | **有数据态**（demo） | CDP + 真后端 | **7/7 通过**：区块为「待面试 / 该跟进 / **校招情报**」三块；校招区块无数据时显示说明 +「去发现机会」 |
| 3 | **今天没事态** | 同上（造「昨天投递、无未来事件、无错题」账号） | 待办区块收起、**校招情报仍在**；提示文案引导去看校招情报 |
| 4 | 控制台 | CDP 全程采集 | 0 JS 错误 |

### 决策与遗留

- **「有内容才显示」这条规则本身没错，错在适用范围**：它适用于待办，不适用于入口。已在 TC-94 里把两类分开写明，避免以后再混；
- **与 IS-30 是同一主题的两面**：IS-30 说「空态判定要看是否真的什么都看不到」（零投递时怎么办），本条说「入口类区块即使没内容也该在」（有投递但无信息时怎么办）——两条已一并请后端补进系统设计 §4.8；
- **后端已闭环前两条登记**：系统设计 **v1.17 新增 §4.8 校招情报界面设计**（五条分离约定 + 空态口径 + 模块色 `#93AE55`），IS-29 / IS-30 的原始口径已写入；
- **遗留**：无。

---

## 步骤 11：SSE 流式基建（前端）

| 项 | 内容 |
|---|---|
| 完成日期 | 2026-09-26 |
| 性质 | **开发计划步骤 11 的前端部分**。后端协议层与自检端点已随 `backend` 分支合入（自测 A 套 30/30 + B 套 18/18），本步交付前端流式出口与打字机组件 |
| 对应分支 | `frontend` |
| 上游依据 | 系统设计 v1.18 §5.1；接口文档 v1.17 §1.4 · §3.1；开发计划 v2.16 步骤 11；台账 **#42** |
| 设计文档 | 设计稿与实现计划（过程文档，不入库） |

### 工作内容

**一、`utils/sse.js`：回调式 API**

```js
const { abort } = streamSSE('/stream/demo', { message }, { onStart, onDelta, onDone, onError })
```

选回调式而非 async generator：后续四条链路的用法都是「`delta` 累积到某个 ref、`done` 后刷新列表、`error` 后显示重试」这种分散更新，回调最直接；中止只需调 `abort()`，不必把 `AbortSignal` 一路透传给调用方。`onToolCall` 一并预留（步骤 19 的确认卡片用）。

**二、跨 chunk 边界是唯一的真坑**

SSE 的事件块可能被 TCP 拆到两个 chunk 里，按 chunk 边界解析就会把半截 JSON 喂给 `JSON.parse`。实现维护一个缓冲区，**只在 `\n\n` 处切分**：

```js
buffer += decoder.decode(chunk, { stream: true })
while ((idx = buffer.indexOf('\n\n')) !== -1) {
  emit(parseEvent(buffer.slice(0, idx)))
  buffer = buffer.slice(idx + 2)
}
```

非 SSE 响应单独给提示——`vite.config.js` 的 mock 与代理互斥，开着 mock 时 `/stream/demo` 无人应答，提示里直接点明「关闭 mock」。

**三、`StreamText.vue`：三档吐字**

纯展示组件（只收 `text` + `streaming`），用 rAF 追赶目标文本。吐字速度分三档：落后 ≤60 字每帧 1 字（60 字/秒，与 LLM 常见速率相当）、≤240 字每帧 2 字、更多每帧 5 字（长报告不至于永远追不上）。

**首版写的是 `lag / 8` 无上限提速，被联调证伪**：91 字时每帧吐 12 字、0.13 秒追平——打字机等于没做。无上限的「追赶」在短文本上退化成瞬移。

**四、`views/DevStream.vue`：不进导航的调试页**

路由 `/dev/stream` 带 `meta.hidden`（与 `/settings` 同款标记），侧栏不显示、只能手输 URL 访问。保留而非用完即删：步骤 12 起每条 AI 链路联调都要靶子，后端也已把 `/stream/demo` 定为常驻端点。页面内含事件日志（`start` / 首个 `delta` / `done` 的时间戳与载荷），核对首字延迟与事件顺序时不必开 DevTools。

### 验证

CDP 驱动 Edge + 真后端，三项实测：

| 场景 | 结果 |
|---|---|
| 打字机 | 页面内按帧记录：**895 帧有文本、86 种不同长度**，前 40 帧 `[1,2,2,2,3,4,5,…]` 逐字递增，最终 86 字与后端 `chars: 86` 一致；首字 1244ms |
| 中止 | 状态条「已中止（客户端主动断开）」、**无错误提示**、事件日志追加 `abort`、中止按钮转禁用 |
| 错误路径 | 未配供应商的临时账号：`start` 文案先出 → `error 10012`，事件序列 start → error |

另有 `npm run build` 通过、后端全量 **219 passed / 0 failed**。

> 首轮判「未通过」是**验证方法**的问题：CDP 每 0.2 秒采样一次正文长度，采到 `[0,0,0,0,0,24,91,91,…]`，看着像一步到位。改成页面内 rAF 逐帧记录才看清真实节奏——批量采样对「逐帧变化」这类现象本就无能为力。

### 结论与遗留

- 前端流式出口就绪，步骤 12 起四条 AI 链路直接复用 `streamSSE` + `StreamText`（`section` 分段由各链路在调用方处理，组件不用改）；
- 项目结构 **v1.34** 登记三个新文件，并订正两处旧登记（`sse.js` 原写在 `api/` 段、`StreamText.vue` 注释写「按 section 分段」）与补登 `utils/datetime.js`；
- **遗留**：无。

---

## 后续步骤索引

> 编号已按**开发计划 v2.13**（27 步 → **29 步**：步骤 21 改写为「多源采集层」，其后新增步骤 22「校招情报（后端）」与步骤 23「校招情报（前端）」，原 22~27 **顺延为 24~29**）校正。

| 开发步骤 | 前端任务 | 状态 |
|---|---|---|
| 步骤 5~6 | —（纯后端） | — |
| 步骤 7 | 账号体系（入场动画 / 登录注册 / 个人中心 / 用户菜单） | **已完成**（2026-09-25） |
| 步骤 8 | AI 配置页 + 设置页瘦身 | **已完成**（2026-09-26） |
| 步骤 9 | 今日概览页 | **已完成**（2026-09-26；mock 走查 17/17 + 真联调 12/12） |
| 步骤 10 | —（纯后端） | — |
| 步骤 11 | `sse.js` + `StreamText` 组件 + 调试页 `/dev/stream` | **已完成**（2026-09-26；CDP 真后端联调——打字机逐帧 86 种长度、中止无错误提示、未配供应商走 `error 10012`） |
| 步骤 12 | JD 分析页 | 待开始 |
| 步骤 13~14 | 陪练页 / 错题本页 | 待开始 |
| 步骤 15~16 | 面试列表 + 进行页 / 总结页 + 详情页 | 待开始 |
| 步骤 17 | 面经页 | 待开始 |
| 步骤 18 | —（纯后端） | — |
| 步骤 19 | Agent 悬浮球（`AgentFloatBall`） | 待开始 |
| 步骤 20 | 概览接入错题 / 面试提醒 | 待开始 |
| 步骤 21 | 概览宣讲会区块改造（补「已变更」角标与来源站点标签） | 待开始 |
| 步骤 22 | —（纯后端） | — |
| 步骤 23 | **校招情报页** `/campus`（三类 Tab + 日历视图 + 我的订阅）+ 设置页源管理区块 + 概览区块改造 + 投递 / JD 分析打通 | 待开始 |
| 步骤 24 | `VoiceInput` + 语音播报（P2） | 待开始 |
| 步骤 25 | 表达力指标卡片（P2） | 待开始 |
| 步骤 26 | 练习模式页：我的题目列表 + 题目详情（P2） | 待开始 |
| 步骤 27 | 交互细节打磨（加载态 / 空态 / 错误态 / 文案） | 待开始 |
| 步骤 28 | `GuideTour` + 个人中心「重看新手指引」入口（P2） | 待开始 |
| 步骤 29 | 整体验收 + 演示准备（与后端共同） | 待开始 |
