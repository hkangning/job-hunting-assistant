# 个人求职助手 —— 接口文档

| 项 | 内容 |
|---|---|
| 文档版本 | v1.5 |
| 文档状态 | 定稿 |
| 维护人 | 胡康宁 |
| 最后更新 | 2026-09-25 |
| 上游依据 | 需求规格说明书 / 系统设计文档 / 数据库设计文档 |
| 关联文档 | 项目结构 / 测试计划 |

## 变更记录

| 版本 | 日期 | 修改内容 | 修改人 |
|---|---|---|---|
| v1.0 | 2026-09-23 | 首版：通用规范 + 错误码 + 47 个接口（REST 41 + SSE 6）完整定义 | 胡康宁 |
| v1.1 | 2026-09-24 | AI 接入改"供应商+模型"两级：新增 GET /settings/llm-models（REST 41→42，共 48 个接口）；头部"上游依据"字段去掉版本号（避免上游升版后反复同步）；3.3 补投递列表默认排序与 PUT 全量更新口径；1.1 补在线接口文档（/docs、/redoc）入口地址 | 胡康宁 |
| v1.2 | 2026-09-24 | **投递结束原因落地**（对应 SRS v1.4 / 数据库设计 v1.2）：①application DTO 新增 `close_reason` 字段；②`PATCH /applications/{id}/status` 请求体新增 `close_reason`（转 `CLOSED` 时**必填**，转其他状态时忽略并清空）；③`GET /applications` 新增 `close_reason` 筛选参数；④PUT 口径补明 `close_reason` 仅在记录处于 `CLOSED` 时可改。**接口总数不变**（无新增端点，仅字段扩展） | 卢世君 |
| v1.3 | 2026-09-25 | **多账号体系接口**（对应 SRS v1.5 / 系统设计 v1.5 / 数据库设计 v1.3）：①1.1 新增鉴权约定（Bearer Token、白名单三端点、有效期 1 天 / 30 天）；②1.3 错误码表新增 **8xxxx 账号与鉴权段**（80001~80007）；③新增 **§3.2 账号与鉴权**（`/auth/*` 7 个）与 **§3.3 AI 供应商配置**（`/llm-providers/*` 6 个）；④原 `/settings/llm-test`、`/settings/llm-models` **迁移**至 `/llm-providers` 组（语义不变、路径与参数调整，详述见 §3.3）；⑤**§3.12 设置节瘦身**（原 §3.10，LLM 相关字段全部迁出；同时节标题改为「画像与设置」）；⑥**接口总数 48 → 59**（REST 42 → 53，SSE 6 不变）；⑦原 §3.2~§3.12 章节号顺延为 §3.4~§3.14 | 胡康宁 |
| v1.4 | 2026-09-25 | **语音方案变更**（对应 SRS v1.6 / 系统设计 v1.6 / 数据库设计 v1.4）：①**§3.13 语音节重写**——说明长时长交互的实现方式（前端 VAD 断句 + 逐句转写累积回填）；`POST /asr/transcribe` 语义明确为**单片段**转写（非整场录音），供应商由账号配置决定（FunASR 本地 / 讯飞云端）；②**新增 `POST /tts/synthesize`**（返回音频流、不走统一响应体、sha1 缓存）与 **`GET /tts/voices`**（音色列表）；③错误码新增 **60002**（TTS 合成失败，前端静默降级）；④§3.12 设置节新增 `asr_provider`（默认 `funasr`）/ `tts_voice`（默认 `zh-CN-XiaoxiaoNeural`）两个账号级字段，`asr_key_set` 语义改为"仅讯飞时有效"；⑤**接口总数 59 → 61**（REST 53 → 55，SSE 6 不变） | 胡康宁 |
| v1.5 | 2026-09-25 | **新增新手指引标记字段**（对应 SRS v1.7 FR-019 / 系统设计 v1.7 / 数据库设计 v1.5）：§3.12 设置节 `GET /settings` 响应体与 `PUT /settings` 可更新字段均新增账号级布尔字段 `guide_done`（默认 `false`）。**接口总数不变（61）**——复用现有设置接口，不新增端点、不新增错误码。**动因**：新用户指引需按账号记录"是否已读" | 胡康宁 |

---

## 1. 通用规范

### 1.1 基础约定

- Base URL：`http://localhost:8000/api/v1`（前端开发代理 `/api` → 8000 端口）
- 在线接口文档（FastAPI 自动生成，服务启动后可访问）：Swagger UI `http://localhost:8000/docs`（可交互调试，展开接口 → Try it out → Execute）、ReDoc `http://localhost:8000/redoc`（只读版）
- 数据格式：JSON（UTF-8）；文件上传 `multipart/form-data`；流式接口 `text/event-stream`（SSE）
- 时间格式：`YYYY-MM-DD HH:mm:ss`（本地时间）；日期 `YYYY-MM-DD`
- 分页：请求 `page`（从 1 起）+ `page_size`（默认 10，最大 50）；响应 `data: {total, items}`
- **鉴权**：除白名单 **`POST /auth/register`、`POST /auth/login`、`GET /health`** 三个端点外，**全部接口（含 SSE）均需携带请求头 `Authorization: Bearer <token>`**；Token 由登录/注册接口返回，有效期 = 勾选"30 天免登录"时 30 天、未勾选时 1 天；缺失/无效 → **401 + 80001**，过期 → **401 + 80002**（前端均清本地 Token 跳登录页）
- **数据隔离**：账号私有数据一律按 **Token 对应的登录账号**过滤与写入，请求体/查询参数中**不接受** `user_id`（传入亦忽略）；访问他账号资源一律返回 **404 + 10002**（与"资源不存在"完全一致，不暴露存在性）

### 1.2 统一响应体

```json
{ "code": 0, "message": "ok", "data": {} }
```

`code=0` 成功；非 0 见错误码表。SSE 接口不走此格式。

### 1.3 错误码表

| 码 | 含义 | 前端动作 |
|---|---|---|
| 0 | 成功 | - |
| 10000 | 系统内部错误 | 通用错误提示 |
| 10001 | 参数校验失败 | 表单标红提示 |
| 10002 | 资源不存在 | 提示后刷新列表 |
| 10003 | 资源冲突（重复数据） | 提示已存在 |
| 10010 | LLM 调用失败（超时/网络） | "AI 服务暂不可用" + 重试按钮 |
| 10011 | LLM 输出格式异常 | 降级纯文本 / 提示重试 |
| 10012 | 未配置 LLM Key | 引导跳转 **AI 配置页**（/ai-config） |
| 10013 | LLM 连通性测试失败 | 展示失败原因（Key 无效/网络不通/模型不存在） |
| 20001 | 批量导入文件格式错误 | 提示文件要求 |
| 20002 | 批量导入存在非法行 | 展示行级错误清单 |
| 30001 | 指定方向题库无题 | 提示换方向 |
| 30002 | 错题不存在或已掌握 | 提示刷新 |
| 40001 | 面试会话状态非法（已结束等） | 提示会话已结束 |
| 40002 | 面经结构化提取失败 | 提供"仅保存原文"入口 |
| 50001 | Agent 工具参数缺失 | 追问补全参数 |
| 60001 | 语音转写失败（本地模型未就绪 / 云端不可用） | "转写失败，可重试或直接打字" |
| 60002 | 语音合成失败（TTS 不可用） | **静默降级**：该句不播报，文字正常显示 |
| 70001 | 就业网抓取失败 | 概览区块显示降级文案 |
| 80001 | 未登录 / Token 无效 | 清除本地 Token → 跳登录页，提示"登录状态已失效" |
| 80002 | Token 已过期 | 同 80001 |
| 80003 | 用户名已存在 | 注册页用户名输入框标红提示 |
| 80004 | 用户名或密码错误 | 登录表单整体提示（**不区分**账号不存在与密码错误） |
| 80005 | 账号已锁定（连续失败 5 次，锁 5 分钟） | 提示剩余锁定时间，倒计时结束可重试 |
| 80006 | 密码强度不足（< 6 位） | 新密码输入框标红提示 |
| 80007 | 原密码错误 | 修改密码表单中原密码框标红提示 |

### 1.4 SSE 事件协议（全部流式接口通用）

```
event: start
data: {"message": "正在分析…"}                       // 连接建立后立即推送（感知延迟优化）

event: delta
data: {"text": "增量片段", "section": "match_score"}   // section 可选，标识报告段落

event: tool_call
data: {"tool_name": "create_application", "args": {...}}  // 仅 Agent 接口

event: done
data: {"record_id": 123, "extra": {...}}                  // 落库 id 与附加数据

event: error
data: {"code": 10010, "message": "..."}                   // 中途失败，已渲染内容保留
```

## 2. 接口总览

| 分组 | 方法 | 路径 | 说明 |
|---|---|---|---|
| 账号 | POST | /auth/register | 注册（成功即登录，返回 Token） |
| 账号 | POST | /auth/login | 登录（可选 30 天免登录） |
| 账号 | GET | /auth/me | 当前账号信息（启动校验 Token 用） |
| 账号 | PUT | /auth/profile | 更新账号信息（昵称/邮箱） |
| 账号 | PUT | /auth/password | 修改密码（成功后旧 Token 作废） |
| 账号 | POST | /auth/avatar | 上传头像（multipart） |
| 账号 | DELETE | /auth/avatar | 恢复默认头像 |
| AI 配置 | GET | /llm-providers | 供应商列表与配置状态 |
| AI 配置 | PUT | /llm-providers/{provider} | 保存某供应商配置（Key/base_url/model） |
| AI 配置 | DELETE | /llm-providers/{provider} | 删除某供应商配置 |
| AI 配置 | POST | /llm-providers/{provider}/activate | 设为当前使用 |
| AI 配置 | GET | /llm-providers/{provider}/models | 该供应商可用模型列表 |
| AI 配置 | POST | /llm-providers/test | 连通性测试 |
| 通用 | GET | /health | 健康检查（**免鉴权**） |
| 概览 | GET | /overview | 今日概览聚合 |
| 投递 | GET | /applications | 投递列表（筛选+分页） |
| 投递 | POST | /applications | 新增投递 |
| 投递 | GET | /applications/{id} | 投递详情 |
| 投递 | PUT | /applications/{id} | 编辑投递 |
| 投递 | DELETE | /applications/{id} | 删除投递 |
| 投递 | PATCH | /applications/{id}/status | 状态流转 |
| 投递 | POST | /applications/import | 批量导入（Excel/CSV） |
| 投递 | GET | /applications/template | 下载导入模板 |
| 投递 | GET | /applications/trend | 投递趋势统计 |
| JD 分析 | POST | /stream/jd-analysis | JD 匹配分析（SSE） |
| JD 分析 | GET | /jd-reports | 报告列表 |
| JD 分析 | GET | /jd-reports/{id} | 报告详情 |
| 面试 | POST | /interview-sessions | 新建面试会话 |
| 面试 | GET | /interview-sessions | 会话列表 |
| 面试 | GET | /interview-sessions/{id} | 会话详情（含 Q&A） |
| 面试 | POST | /stream/interview-chat | 作答+点评+下一题（SSE） |
| 面试 | POST | /stream/interview-summary | 生成总结报告（SSE） |
| 陪练 | POST | /practice/questions | 抽题 |
| 陪练 | POST | /stream/practice-review | 陪练点评（SSE） |
| 陪练 | GET | /practice/records | 陪练历史 |
| 题库 | POST | /questions | 新增题目（AI 生成入库/手动） |
| 错题 | GET | /wrong-questions | 错题列表 |
| 错题 | POST | /wrong-questions | 手动添加错题 |
| 错题 | DELETE | /wrong-questions/{id} | 删除错题 |
| 错题 | POST | /wrong-questions/{id}/review | 复习判定（提交答案） |
| 面经 | POST | /experiences | 保存面经原文 |
| 面经 | GET | /experiences | 面经列表 |
| 面经 | GET | /experiences/{id} | 面经详情（含条目） |
| 面经 | DELETE | /experiences/{id} | 删除面经 |
| 面经 | POST | /stream/experience-extract | 结构化提取（SSE） |
| 面经 | GET | /experience-items/search | 条目关键词检索 |
| Agent | POST | /stream/agent-chat | Agent 对话（SSE） |
| Agent | POST | /agent/tools/{tool}/execute | 执行确认卡片动作 |
| Agent | GET | /agent/conversations | 会话列表 |
| Agent | GET | /agent/conversations/{id}/messages | 会话消息 |
| 画像 | GET | /profile | 获取画像 |
| 画像 | PUT | /profile | 更新画像 |
| 设置 | GET | /settings | 获取设置（系统偏好，已瘦身） |
| 设置 | PUT | /settings | 更新设置 |
| 语音 | POST | /asr/transcribe | 语音转写（单片段，P2） |
| 语音 | POST | /tts/synthesize | 语音合成（返回音频流，P2） |
| 语音 | GET | /tts/voices | 可选音色列表（P2） |
| 定时 | POST | /reminders/run | 手动触发每日任务（演示用） |
| 定时 | GET | /reminders | 提醒列表 |
| 定时 | PUT | /reminders/{id}/check | 标记已读 |
| 定时 | GET | /campus-events | 宣讲会列表 |

## 3. 接口详述

### 3.1 通用

**GET /health**

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| - | - | - | - | - |

响应：`{"code":0,"message":"ok","data":{"status":"ok"}}`。**免鉴权**，不含任何账号信息（原 `llm_configured` 字段随多账号改造移除，"是否已配置 AI"改由 `GET /auth/me` 返回，见 §3.2）。

### 3.2 账号与鉴权

**POST /auth/register**（**免鉴权**）

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| username | body | string | 是 | 登录名，3~20 位字母/数字/下划线，全站唯一（不区分大小写） |
| password | body | string | 是 | 密码，**≥6 位**（不强制字符类型） |
| nickname | body | string | 否 | 昵称，默认取用户名，可在个人中心修改 |
| email | body | string | 否 | 邮箱，填写时校验格式且全站唯一（为后续高级功能预留） |

响应 `data`：`{"token":"<JWT>","expires_at":"2026-09-26 12:00:00","user":{"id":1,"username":"hkn","nickname":"hkn","avatar":null,"role":"ADMIN","plan":"FREE","created_at":"..."}}`

- 注册成功**自动登录**，直接返回 Token（**有效期 1 天**，注册流程不含免登录选项）；
- 注册时自动创建该账号的画像空记录与账号级默认配置（见数据库设计 §4）；**首个注册账号 `role=ADMIN`**，其余 `USER`（本期无权限差异）；
- 用户名重复 → **80003**；密码 <6 位 → **80006**。

**POST /auth/login**（**免鉴权**）

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| username | body | string | 是 | 登录名 |
| password | body | string | 是 | 密码 |
| remember_me | body | bool | 否 | 勾选"30 天免登录"传 `true`（默认 `false`） |

响应 `data`：同注册（`token` / `expires_at` / `user`）。`expires_at` 按 `remember_me` 计算：**true = 30 天，false = 1 天**。

- 用户名或密码错误 → **80004**（统一文案，**不区分**用户名是否存在，防枚举）；
- 连续失败 5 次 → 锁定 5 分钟，期间即使密码正确也返回 **80005**（`message` 含剩余锁定时间）；登录成功清零计数并更新 `last_login_at`。

**GET /auth/me**

无参数。响应 `data`：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | int | 账号 id |
| username | string | 登录名（个人中心只读展示） |
| nickname | string | 昵称 |
| email | string/null | 邮箱 |
| avatar | string/null | 头像相对路径，null = 用默认头像 |
| role | string | `USER` / `ADMIN` |
| plan | string | `FREE` / `PRO`（预留） |
| created_at | datetime | 注册时间 |
| last_login_at | datetime/null | 上次登录时间 |
| **llm_configured** | bool | 当前账号是否已有生效的 AI 供应商配置（false 时概览提示"未配置 AI"） |

**前端启动校验入口**：路由守卫以本接口判断本地 Token 是否仍有效（401 → 80001/80002 → 清本地 Token 跳登录页）。

**PUT /auth/profile**

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| nickname | body | string | 否 | 昵称 |
| email | body | string | 否 | 邮箱（格式校验 + 全站唯一，重复 → 10003） |

响应 `data`：更新后的账号信息（同 `GET /auth/me` 结构）。**用户名不可修改**。

**PUT /auth/password**

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| old_password | body | string | 是 | 原密码 |
| new_password | body | string | 是 | 新密码，**≥6 位** |

- 原密码错误 → **80007**；新密码 <6 位 → **80006**；
- 成功返回 `data: null`，**当前 Token 立即失效**（含"30 天免登录"的旧 Token）——前端收到成功后清除本地 Token 并跳登录页重新登录。

**POST /auth/avatar**（`multipart/form-data`）

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| file | form-data | file | 是 | 头像图片：jpg / jpeg / png / webp，**≤2MB** |

响应 `data`：`{"avatar":"uploads/avatars/1_a3f9c2d1.png"}`（相对路径，前端拼接后端地址渲染）。

- 前端先压缩为 **256×256** 再上传，后端**仍校验**类型与体积（不符 → 10001）；
- 新头像落盘成功后再更新库并**删除旧文件**；上传失败保留原头像。

**DELETE /auth/avatar**：恢复默认头像（删除自定义文件、`avatar` 置空），`data: {"avatar": null}`。

### 3.3 AI 供应商配置

**GET /llm-providers**

无参数。响应 `data`：

```json
{"active":"deepseek",
 "providers":[
   {"provider":"deepseek","name":"DeepSeek","group":"国内",
    "base_url":"https://api.deepseek.com","key_set":true,
    "model":"deepseek-flash","is_active":true},
   {"provider":"ollama","name":"Ollama（本地）","group":"本地",
    "base_url":"http://localhost:11434/v1","key_set":false,
    "model":null,"is_active":false}]}
```

- 返回**注册表全部 12 项**（未配置的也在列表中，供前端渲染卡片），`group` 取值：国内 / 国外 / 聚合 / 本地 / 自定义；
- `key_set` 仅表示当前账号在该供应商下**是否已存 Key**（**永不回显明文或片段**）；`ollama` 无需 Key，`key_set` 恒为 false 不影响使用；
- `model` 为该账号在该供应商下所选模型（null = 用注册表默认）；`active` 为当前生效供应商标识（无则 null）。

**PUT /llm-providers/{provider}**

`provider` 为注册表标识之一（非法 → 10001）。

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| api_key | body | string | 否 | 密钥；**省略或留空 = 不修改已存 Key**（首次配置必填，`ollama` 可空）；入库前 Fernet 加密 |
| base_url | body | string | 否 | 覆盖注册表默认端点（应对端点变更）；传空串 = 恢复默认 |
| model | body | string | 否 | 该供应商下所选模型 ID（候选来自模型列表接口） |

响应 `data`：该供应商的配置项（结构同 GET 列表中的单条）。约束：每账号每供应商一行（`user_id + provider` 唯一），重复保存走更新；**保存不改变当前激活项**（激活见下）。

**DELETE /llm-providers/{provider}**

删除当前账号在该供应商下的全部配置（Key / base_url / model / 模型缓存），`data: null`。**仅影响该供应商**，其余不受影响。

- 若删除的是**当前激活项**，`active` 变为 null，AI 功能进入"未配置"状态（后续 LLM 调用返回 10012），前端须二次确认后提交。

**POST /llm-providers/{provider}/activate**

无请求体。把该供应商置为**当前生效**（同一事务内取消该账号其他行的激活位，保证至多一行生效），`data: {"active":"deepseek"}`。

- 切换后**立即生效**，无需重启、无需重填 Key（已存配置保留，切回即用）；
- 该供应商尚未配置 Key（`key_set=false` 且非 `ollama`）→ 10001，提示先填写并保存 Key。

**GET /llm-providers/{provider}/models**

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| refresh | query | bool | 否 | `true` = 强制重拉（默认走 24h 缓存） |

响应 `data`：`{"models":[{"id":"deepseek-flash","display_name":"DeepSeek V4.1 Flash","is_free":true}],"source":"remote","fetched_at":"2026-09-25 12:00:00"}`

- Key 取**当前账号已存配置**（无需前端传入）；结果缓存 24h（存 `llm_provider_config.models_cache`，按 provider + base_url 维度）；
- `source="builtin"` 表示拉取失败**回退内置模型表**（前端标注"内置列表（上次拉取失败）"）——拉取失败本身不报错；
- 已存 Key 无效 → **10013**。

**POST /llm-providers/test**

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| provider | body | string | 是 | 待测供应商（注册表标识） |
| api_key | body | string | 否 | 待测 Key；**省略则用当前账号已存 Key**（均不落库、不改配置） |
| base_url | body | string | 否 | 同 PUT 语义（`custom` 必填） |
| model | body | string | 否 | 待测模型，缺省用注册表默认 |

行为：向供应商发送一条最小消息验证连通性（**不落库、不保存配置**）。成功 `data: {"message":"ok","model":"实际模型名"}`；失败 **10013**，`message` 携带具体原因（Key 无效 / 网络不通 / 模型不存在）。

### 3.4 今日概览

**GET /overview**

响应 `data`：

| 字段 | 类型 | 说明 |
|---|---|---|
| upcoming_events | array | 待面试/笔试：`[{application_id, company, position, event_at, status}]`，7 日内按时间升序 |
| application_stats | object | `{APPLIED: n, WRITTEN: n, INTERVIEW: n, OFFER: n, CLOSED: n}` |
| follow_ups | array | 3 天无进展：`[{application_id, company, position, applied_at, days}]` |
| wrong_question_count | int | 到期未复习错题数 |
| campus_events | array | 近期宣讲会 `[{id, title, company, event_date, location, source_url}]` |
| last_crawl_at | datetime/null | 就业网最后抓取时间（null=未抓取/失败） |

### 3.5 投递管理

**GET /applications**

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| status | query | string | 否 | 状态过滤（枚举值） |
| close_reason | query | string | 否 | 结束原因过滤（枚举值，仅对 `status=CLOSED` 的记录有意义） |
| city | query | string | 否 | 城市 |
| company | query | string | 否 | 公司关键字（LIKE） |
| page / page_size | query | int | 否 | 分页 |

响应 items 字段与本章 application DTO 一致（不含 remark，列表精简）。默认按投递日期倒序（applied_at DESC，同日期按 id DESC）。

**POST /applications**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| company | string | 是 | 公司名称（≤100） |
| position | string | 是 | 岗位名称（≤100） |
| city | string | 否 | 城市 |
| expected_salary | string | 否 | 期望薪资（自由文本） |
| applied_at | string | 否 | 投递日期，默认当天 |
| channel | string | 否 | 投递渠道 |
| status | string | 否 | 状态，默认 APPLIED |
| next_event_at | string | 否 | 下次笔试/面试时间 |
| remark | string | 否 | 备注 |

响应：`data = application DTO`（含 id）。

**application DTO**（列表/详情/创建通用）：

```json
{"id":1,"company":"浩鲸科技","position":"Java 开发","city":"南京",
 "expected_salary":"13k","applied_at":"2026-09-23","channel":"官网",
 "status":"APPLIED","close_reason":null,"next_event_at":null,"remark":"","created_at":"...","updated_at":"..."}
```

`close_reason` 为结束原因（`FAILED` 未通过 / `DECLINED` 主动放弃 / `EXPIRED` 无消息），**仅当 `status=CLOSED` 时有值**，其余状态恒为 `null`。

**GET /applications/{id}**：同上 DTO，含 remark 全文。

**PUT /applications/{id}**：请求体同 POST（全量更新，除 status 外均可改；status 变更走 PATCH）。全量语义下未传的可选字段按空处理，未传 `applied_at` 时取当天。`close_reason` 仅在记录当前处于 `CLOSED` 时可改（用于选错原因后更正）；其余状态下传该字段返回 10001。

**DELETE /applications/{id}**：响应 `data: null`。

**PATCH /applications/{id}/status**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| status | string | 是 | 目标状态（枚举值，合法流转见 SRS FR-004 状态机） |
| close_reason | string | 条件必填 | 结束原因（`FAILED` 未通过 / `DECLINED` 主动放弃 / `EXPIRED` 无消息）；**`status=CLOSED` 时必填**，缺失返回 10001；转其他状态时忽略并清空该字段 |
| event_at | string | 否 | 转为 WRITTEN/INTERVIEW 时可同时更新下次考试/面试时间 |
| remark | string | 否 | 流转备注 |

**POST /applications/import**（multipart）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| file | file | 是 | .xlsx 或 .csv，≤2MB |

表头：`company,position,city,applied_at,channel,status,remark`（模板接口下载）。响应：`data:{success_count, errors:[{row, reason}]}`；全部行非法返回 20002（errors 非空）。

**GET /applications/template**：返回 xlsx 文件流。

**GET /applications/trend**（对应 FR-005）

| 参数 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| days | query | int | 否 | 统计最近 N 天，默认 30，最大 90 |

响应：`data:{items:[{date:"2026-09-23", count:3}]}`（按投递日期 GROUP BY，含 0 值日期）。

### 3.6 JD 匹配分析

**POST /stream/jd-analysis**（SSE）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| jd_text | string | 是 | JD 原文（≤10000 字） |
| application_id | int | 否 | 关联投递记录 |

事件流：`delta`（section 依次为 `match_score/scores/strengths/gaps/advice`）→ `done`（`record_id` = jd_analysis_report.id）。用户中途断开：已生成内容落库并标记。

**GET /jd-reports**（query：application_id 可选、page、page_size）

items：`[{id, application_id, company?, score, created_at}]`（company 联表显示）。

**GET /jd-reports/{id}**：`data:{id, application_id, jd_text, report_text, score, created_at}`。

### 3.7 AI 模拟面试

**POST /interview-sessions**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| application_id | int | 否 | 关联投递（有则 company/position 从中带入） |
| company / position | string | 条件必填 | 未传 application_id 时两者必填 |
| direction | string | 否 | 面试方向，默认 GENERAL |
| question_count | int | 否 | 题量 3~15，默认 8 |

响应：`data = session DTO`：

```json
{"id":1,"application_id":1,"company":"浩鲸科技","position":"Java 开发",
 "direction":"JAVA","question_count":8,"status":"ACTIVE","summary":null,
 "created_at":"...","finished_at":null}
```

**GET /interview-sessions**（query：status 可选、page、page_size）。

**GET /interview-sessions/{id}**：session DTO + `qa_list`：

```json
{"id":1,"seq":1,"question":"...","answer":"...","is_voice":0,
 "score":8,"review":"...","skipped":0,"created_at":"..."}
```

**POST /stream/interview-chat**（SSE）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| session_id | int | 是 | 会话 id |
| answer | string | 否 | 本题作答；会话第一题传空串触发开场提问 |
| skip | bool | 否 | true=跳过上一题（不计分，直接下一题） |

事件流：`delta`（section 依次 `review/next_question`；第一题为 `next_question`）→ `done`（`record_id` = 新 interview_qa.id，含 seq）。会话已结束（40001）或已满题量（done 事件 `extra:{session_finished:true}`，前端引导生成总结）。

**POST /stream/interview-summary**（SSE）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| session_id | int | 是 | 会话 id（须含 ≥1 条已完成 Q&A） |

事件流：`delta`（section=`summary`）→ `done`（`record_id`=session.id，会话置 FINISHED）。重复调用返回已有 summary（不重复计费）。

### 3.8 八股陪练

**POST /practice/questions**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| directions | array | 是 | 方向数组（可多选），元素为枚举值 |
| count | int | 否 | 抽题数，默认 1，最大 5 |

响应：`data:{items:[{id, direction, content, qtype}]}`。注意：**不含 answer**（防止先看答案）。

**POST /stream/practice-review**（SSE）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| question_id | int | 是 | 题目 id |
| answer | string | 是 | 用户作答 |

事件流：`delta`（section 依次 `score/review/reference_answer`）→ `done`（`record_id`=practice_record.id；`extra:{wrong_question_id?}`，评分 <6 自动入错题本时返回——该题已在错题本则更新档位并返回原 id）。

**GET /practice/records**（query：question_id 可选、page、page_size）：陪练历史。

**POST /questions**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| direction | string | 是 | 方向 |
| content | string | 是 | 题干 |
| answer | string | 是 | 标准答案 |
| qtype | string | 否 | 默认 SUBJECTIVE |
| source | string | 否 | 默认 AI_GENERATED |

### 3.9 错题本

**GET /wrong-questions**（query：status=`PENDING`待复习/`MASTERED`已掌握、page、page_size）

items：`[{id, question_id, content, direction, source_type, review_stage, next_review_at, wrong_count, mastered_at}]`（content 联表 question）。

**POST /wrong-questions**：两种请求形态（对应两类入本来源）：

- 题库题：`{question_id}`（须存在于题库且未入本，冲突返回 10003）；
- 面试知识点（不在题库）：`{content, answer, direction, source_type:"INTERVIEW"}`——后端先自动创建 question（source=AI_GENERATED）再入本。

**DELETE /wrong-questions/{id}**：`data: null`。

**POST /wrong-questions/{id}/review**

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| answer | string | 是 | 复习作答 |

响应 `data`：

```json
{"correct":true,"explain":"判定解析/标准答案","review_stage":2,
 "next_review_at":"2026-09-26 00:00:00","mastered":false}
```

判定：CHOICE 题规则比对；SUBJECTIVE 题 LLM 判定（非流式 JSON 输出）。档位推进规则见数据库设计 3.9。

### 3.10 面经整理

**POST /experiences**：`{company?, position?, source?, original_text}`（original_text ≤10000 字）。响应 experience DTO（含 id）。**只存原文，不触发提取**。

**GET /experiences**（page、page_size）：items：`[{id, company, position, source, item_count, created_at}]`（不含原文，列表精简）。

**GET /experiences/{id}**：experience 全文 + `items:[{id, question, answer_points, source_type}]`。

**DELETE /experiences/{id}**：级联删条目。

**POST /stream/experience-extract**（SSE）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| experience_id | int | 是 | 已保存的面经 id |

事件流：`start`（提取开始状态文案）→ `delta`（提取进度状态文字，不逐字展示 JSON）→ `done`（`extra:{items:[{question, answer_points}]}`，条目已落库）。提取失败发 error（code 40002），原文仍在，前端提供重试/仅存原文。

**GET /experience-items/search**（query：keyword 必填 ≤50 字、page、page_size）

items：`[{id, experience_id, company, question, answer_points}]`（company 联表 experience）。

### 3.11 全局 Agent

**POST /stream/agent-chat**（SSE）

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| conversation_id | int | 否 | 不传则新建会话（done 事件回 id） |
| message | string | 是 | 用户输入 |

事件流：

```
delta      → Agent 文字回复增量
tool_call  → {tool_name, args}：写操作类工具（新增投递/状态流转），
             前端渲染"确认卡片"，用户确认后调 3.10 执行接口
done       → {record_id: agent_message.id, conversation_id, extra:{...}}
```

查询类工具（查投递/搜面经/出题等）由后端直接执行，结果以文字+结构化数据（delta 事件 `text` 带 JSON 块）回显。

**POST /agent/tools/{tool}/execute**

| tool | 请求体 | 说明 |
|---|---|---|
| create_application | 同 POST /applications 请求体 | 执行入库，返回 application DTO |
| update_application_status | `{application_id, status, remark?}` | 状态流转，返回 application DTO |

**GET /agent/conversations**（page、page_size）：`[{id, title, updated_at}]`。

**GET /agent/conversations/{id}/messages**：`[{id, role, content, tool_name?, created_at}]`。

### 3.12 画像与设置

**GET /profile / PUT /profile**：PUT 请求体为 user_profile 表全部业务字段（name/school/major/degree/gpa/english_level/resume_text/target_position/target_city/skills/weaknesses/note）。skills/weaknesses 为逗号分隔字符串。画像**按账号隔离**——每账号一份，请求体不接受 `user_id`（归属由登录态决定）。

**GET /settings**：

```json
{"tts_enabled":false,"voice_enabled":false,"default_question_count":8,
 "asr_provider":"funasr","tts_voice":"zh-CN-XiaoxiaoNeural","asr_key_set":false,
 "guide_done":false,"crawl_enabled":false,"crawl_url":""}
```

- `tts_enabled` / `voice_enabled` / `default_question_count` / `asr_provider` / `tts_voice` / `guide_done` 为**账号级**配置（随账号各自保存）；
- `guide_done` 为新手指引已读标记（默认 `false`）：走完引导或点「跳过」由前端置 `true`，之后登录不再自动弹出；个人中心「重看新手指引」只重播、不改本字段（P2）；
- `asr_provider` 取值 `funasr`（默认，本地模型，无需 Key）/ `xunfei`（云端备选）；
- `tts_voice` 为 edge-tts 音色名，默认 `zh-CN-XiaoxiaoNeural`（晓晓），可选值见 **§3.13 `GET /tts/voices`**；
- `crawl_enabled` / `crawl_url` 为**系统级**配置（面向整站公共数据，任一账号修改即全局生效，见 SRS 4.1）；
- `asr_key_set` 仅告知是否已配置讯飞语音 Key，**不回显明文**；默认 `asr_provider=funasr` 时恒为 `false`（本地模型无需凭据）；
- **LLM 供应商相关字段已全部迁出本接口**（原 `llm_provider` / `llm_model` / `llm_base_url` / `llm_key_set`），统一由 **§3.3 `/llm-providers`** 组管理。

**PUT /settings**：可更新 `tts_enabled` / `voice_enabled` / `default_question_count` / `asr_provider` / `tts_voice` / `guide_done` / `crawl_enabled` / `crawl_url`；`asr_app_id` / `asr_api_key` 传值则以 Fernet 加密后覆盖，传空串不修改。`asr_provider` 传非法值 → **10001**；`guide_done` 传非布尔值 → **10001**。

### 3.13 语音（P2）

模拟面试的长时长语音交互由**前端 VAD 断句 + 逐句调用转写**实现：前端累计检测到约 1.2s 静音即切出一个 3~15s 片段并上传，转写文本**累积回填**到作答框，用户全程连续说话、整场不限轮次与总时长。播报侧由后端合成音频返回，前端 `<audio>` 播放、可随时打断。

**POST /asr/transcribe**（multipart：`file`=音频片段，webm/ogg/wav，单片段 ≤2MB）

响应：`data:{text:"转写文本",duration_ms:1200}`（失败 **60001**）。仅转写回填，**不落库**。

- 供应商由账号的 `asr_provider` 决定：`funasr` 走**本地流式模型**（进程内推理，不出网，首次调用需加载模型故首句略慢）；`xunfei` 走云端接口（需已配 Key，未配 → 60001）；
- 单次请求对应**一个语音片段**而非整场录音——长时长的实现方式，故服务端不限制轮次与累计时长。

**POST /tts/synthesize**

```json
{"text":"这道题可以从三个层面回答","voice":"zh-CN-XiaoxiaoNeural"}
```

- `voice` 可省略，缺省用账号的 `tts_voice` 配置；
- 响应：**音频流**（`audio/mpeg`，直接作为 `<audio src>` 播放），**不走统一响应体**；
- 失败 → **标准响应体 + 60002**（前端静默降级，该句不播报）；
- 服务端按 `sha1(text + voice)` 缓存合成结果（`backend/cache/tts/`），同一文案 + 同一音色**只合成一次**；命中缓存直接返回。

**GET /tts/voices**：返回可选音色列表 `data:[{id:"zh-CN-XiaoxiaoNeural",name:"晓晓",gender:"Female",style:"温暖亲和"},...]`，供设置页下拉选择（默认音色置顶）。

### 3.14 定时引擎

**POST /reminders/run**：立即执行每日任务（开发/演示用），返回 `data:{generated: n}`。**多账号下遍历全部账号执行**——每个账号按其自身投递记录与错题生成提醒（提醒文案用该账号自己的 AI 配置），`campus_events` 抓取为全站一次。

**GET /reminders**（query：date 可选、checked 可选、page、page_size）

items：`[{id, reminder_type, ref_id, content, remind_date, checked}]`。

**PUT /reminders/{id}/check**：标记已读，`data: null`。

**GET /campus-events**（query：date_from 可选、page、page_size）：`[{id, title, company, event_date, location, source_url}]`。
